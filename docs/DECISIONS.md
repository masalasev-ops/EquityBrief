# DECISIONS.md

A decision belongs here when a later session could reasonably choose differently **and** the wrong choice would be invisible. Mechanisms with one obvious implementation, and anything a test already enforces, do not.

A decision is identified by its **bold name**, cited exactly. In code: `// see: Code owns every number`. In a document: `(see: Code owns every number)`. Same string either way, so `decision-resolves` covers both. Names carry no terminal punctuation, because a name ending in a period invites the paraphrase the check exists to reject.

A decision is changed only by another decision. Work that changes one writes a new decision, names what it supersedes, and moves the old entry to **Previously decided** in the same commit with its reasoning intact. Nothing here is struck through.

---

## Scope

**The universe is the S&P 500, and membership is fetched, not maintained** A published index is a fixed list rather than a filtered one, which keeps this a reporting tool rather than a screen. Membership is stored with join and leave dates so a name added last month is not shown as present in an older window.

**An announced index change takes effect on its effective date, and a joining name is stored from the announcement** The membership feed carries a rebalance before it takes effect, with the dates it takes effect on, and until the phase 5 sign-off every nightly stage and the read surface took a name for a member whenever its row had no leave date. The rebalance effective 21 September 2026 was on the feed by the 10th, so three names still in the index were stored nothing for and listed nowhere, and four names not yet in it were listed. A name is a member on a session when its join date is on or before the session, or is unknown, and its leave date is after the session or absent, which is the reading the membership loader already gave a past date. The stages that list, count or show the index read that. The stages that store what later stages read, the bars and their adjustment, read the wider set of every name that has not left by the session, which includes a name announced to join. That half is the choice a later session could reverse without seeing why: a backfill runs once, for a name holding no bars, so a name backfilled on its announcement and then not stored until its effective date would carry every session between the two as a hole nothing fills, and storing it early costs no nightly request, since the bulk file already carries it.

**A ticker the index feed stops listing leaves the index on the night it goes unlisted** Ruled on 2026-09-18, when the operator was given this and the alternative below and asked for everything to be fixed. The feed dates a leave when the index drops a name, and dates nothing when the provider renames a ticker: it lists the new ticker and stops listing the old one, with no end date on either. EQR became VMRK on 2026-08-18, and the joiner of 2026-09-21 that the price file carries as P was listed as P, then PSTG, then P_old, so a loader that closed only the spans the feed dated kept every one of them open, and from that rebalance the store's index would have held 506 tickers where the feed listed 503. So the night whose feed does not list a span still open on its session closes that span on the session, whether the ticker is gone from the feed or listed under another join date, and the upsert that writes a span the feed lists again reopens it. The alternative was a map, kept by hand, from the feed's tickers to the ones the price file carries, which would have kept the joiner's prices under P and is membership maintained rather than fetched; taking the feed as the index makes the joiner a member as P_old, holding no prices until the provider's two files agree, and its page says so. A feed that stops listing more than ten tickers in one night is read as a fault in the feed rather than as ten departures, since the index changes a handful of names at a rebalance and dates them, and a provider renames one company at a time: that night closes none, and the membership step ends partial with every ticker named, which puts it on the run page's stale-and-failed region.

**One universe now, the seam for more built now** Every stored row belongs to a name, not to a universe, and every screen takes the selected universe as a filter. That is a table and a where clause today, and retrofitting it later would mean reconstructing membership for every stored night.

**Long-only share plans** The operator does not short and does not trade options, so the ladder builder has no branch for either.

**Section 4 of the architecture defines the report region by region, and nothing outside the corpus does** The regions the name page draws, in the order it draws them, what produces each, whether each costs money and how often each changes are stated there, each named as the page's contents names it. A region is added, removed, renamed or reordered by amending that table, and `read-surface` reads the table against the contents of the name pages drawn over the fixture: every region a page draws is a row, in the rows' order, every row is a region some page draws, and the count the section states is its count of rows. A hand-written example illustrates the rules and is never the thing to be reproduced: where an example and a rule disagree, the rule is right.

Supersedes **Section 4 of the architecture defines the report, and nothing outside the corpus does**, moved below with its reasoning, all of which stands but two statements: that the report has eleven sections, when the page had come to draw up to nineteen regions while the table stood as it was, and that `architecture-conformance` checks the code against the table, which no check did, the phase report placing section 4 outside the claim scope. The operator found the table out of date on 2026-09-24.

## Cost and when work happens

**The nightly run is arithmetic only** No model calls and no per-name network requests. Bars arrive in one bulk file and news in one feed request, so a night costs the same whether the universe is fifty names or five hundred.

**The night's zero-model-call rule bounds the arithmetic, and the overnight queue is carved out of it by name** Section 14's list runs to seventeen steps and step 17 calls the local model for every listed name whose research is missing or stale, while section 17's row read zero model calls in the nightly run, section 5's band said no model, and section 14's own closing note said no model is called. Four statements, and one of them is the step. The rule is kept and the step is carved, which is what 1.2 did for the backfill against the per-name limit rather than loosening a rule to fit its own exception. What the rule bounds is the arithmetic, being steps 1 to 16: those make no model call at all, and a night costing the same for fifty names as for five hundred follows from that rather than from anything the queue does. Step 17 runs after the arithmetic has closed and recorded its counts, makes local calls only, spends nothing, and no part of the arithmetic depends on it, so a queue that did not run leaves every figure above it unchanged and the run page says it did not. The carve is by name rather than by scope, so the scan that finds a model client keeps reading every other shipped file and the file the queue's client sits in is listed where a reader can see it (see: The outward-request scan names the files that may hold a client rather than dropping the patterns). A paid call from the queue is not carved and does not become so: the queue's own limit row says it makes none, and the local lane refuses a key, so the enforcement sits on the lane rather than on the step.

**Everything expensive happens when a name is opened** Reports are not generated for names nobody reads. A name never opened costs nothing beyond its share of the nightly arithmetic.

**Bars come from EODHD, bulk nightly and per ticker for the backfill** The provider is named in the architecture and the runbook and was never a decision, which left the route through it open to being reversed by a later session reading only the zero-per-name-call rule. The arithmetic is what settles it and belongs here rather than in a runbook table: bulk end-of-day for a whole exchange costs 100 weighted calls and a single-ticker historical request costs 1, so the nightly fetch takes the bulk endpoint at 100 against 500, and the one-year backfill takes the per-ticker endpoint at 500 against the 25,000 that replaying a year of past sessions through the bulk endpoint would cost. The two run opposite ways round for the same reason, which is that the bulk call is priced per request and the historical call is priced per name.

**Research is a stored document with an as-of date, not a cache** It costs money and produces a different answer each time it runs, so discarding it would change the report for no reason and leave nobody able to tell whether the analysis moved or the wording did. Every version is kept.

**Nothing expires on a timer** Research is rewritten when a filing appears, an earnings date passes, the name's news volume jumps above its own baseline, or the operator asks. A timer either refreshes work nobody needed or serves analysis written before a print.

**Deciding not to spend must not cost anything** The four staleness questions are answered from data the nightly run already computed, which is why the news pulse counter runs nightly and for free.

**Research goes stale on an event dated after the section was written, and a news spike is dated by the session it began** The two decisions above name the four things that make research stale and say none of them may cost anything, and neither says what "after" means, which is the whole of the judge. So each question is a date compared with a section's own as-of date, per section rather than per record because a record is written and dated per section, and a section written after a filing arrived is not stale because another was written before it. A new filing is a stored filing dated after the section. An earnings date has passed where the newest event on or before the night is dated after the section, or on the section's own date unless the provider filed it before the open: a section dated the day of an after-close print was most likely written before the numbers came out, and the cost of reading it that way is one rewrite where it was not, against a report standing on numbers that had already changed. A refresh makes every section stale. The judgement is as of the newest night the store computed for the name, read off its facts rather than off the wall clock, so a name opened on a Saturday is judged on Friday's close.

The news trigger is section 17's figures read as windows. The window is the last five sessions held, and it is above where it holds at least five articles and at least three times the baseline, which is the median of the five-session sums for every window ending in the ninety days before the current window begins, so a spike does not raise the baseline it is measured against. No earlier window is no baseline and no reading, because the floor alone would fire on every name the market writes about, and the pulse table begins the night the counter first ran, so for its first ninety days the baseline rests on fewer windows than it will and carries its count beside it the way a reading over fewer than twelve quarters does. A spike is dated by the session its unbroken run of windows above began, which is what makes section 17's claim true that the five-session window stops a story breaking over several days from firing twice: a section written during the run was written after the spike began, and the windows that stay above because they still hold the story do not make it stale again. A second story after the run ends is a new run with a later date.

A record is at least one accepted section. A name whose sections all fell back has rows and no research, and for staleness it is missing, because nothing written can go stale. (see: Nothing expires on a timer) (see: Deciding not to spend must not cost anything) (see: A research record is written and dated per section, not as a whole)

**Industry research is per theme, not per name** The passage about an industry's own pricing cycle is the same for every company in it, so one paid pass serves them all and refreshes when the first name needing it finds it stale.

**A theme is the industry the index names for a member, and one theme pass serves every member it names** The decision above says one paid pass serves every company in an industry and refreshes when the first name needing it finds it stale, and leaves open what an industry is, which a later session could draw anywhere with nothing showing it. The index's own constituents payload, which the membership loader fetches every night, names an industry for every current member beside its sector at no extra request, so a theme is that industry: the membership row carries it from 6.9, a theme pass searches for it by the name the index gives it, and the theme record lists it as the industry that maps to the theme. The company financials name an industry too, but only for the names someone has opened, so they cannot say which members an industry holds, and a theme has to be answerable for every member rather than for the ones read so far. A configured map folding several industries into one theme was the alternative, and the corpus's own example of a theme, memory-chip contract pricing, is narrower than an industry as the index names them, which are of the order of Consumer Electronics and Scientific & Technical Instruments; it was not taken, because a map with no measurement behind it is a guess at which industries share a cycle, and the list holds one industry until a decision folds several on evidence. A name's industry cycle is therefore never one of its own sections. It is judged by the theme's newest version, refreshed by the theme research runner before anything of the name's own where it is missing, left out on an earlier day, refused, or accepted before a trigger the staleness judge fired for the name, and read by the name page off the theme store for the name's industry. The triggers are the name's because the theme has none of its own, so a member opened for the first time fires nothing and reads the theme its industry already holds, and a member rewritten on a filing that arrived after the theme was written refreshes it for every member. (see: Industry research is per theme, not per name)

**The membership row carries the company's name the index feed states** The membership row carries the company's name, read from the span the index feed lists for the ticker, beside the sector and the industry it already carries. Two things read it. The name page states it beside the ticker, which it could not until now: the operator's first sight of the screens, on 2026-09-18, found a name page headed by a ticker alone. And a research pass reads it to tell an article about the company from one that only mentions it, because the provider attributes a document to every company its text names: the first live pass handed NVDA's calendar section another company's product launch, which the provider had attributed to NVDA alone and whose title named Xeal and not NVIDIA. The span rather than the snapshot object, because the spans carry a name for every ticker they list, current and departed, where the snapshot carries current members alone; coalesced as the sector is, so a night whose span states none keeps the name the row holds. The name is the provider's, with its legal suffixes, and anything reading it takes what it needs from it rather than the store holding a shortened form nobody can check against the feed.

**A theme section states no figure, because nothing the store holds is computed for an industry** The number rule holds every figure in prose to the facts file, and a facts file is one per name, so as 6.4 built the checker a theme section is held against none and every figure in one is refused, which 6.4 carried to the checkpoint that writes the first theme section. The lane table says every figure in the industry cycle is one code computed for the industry. The store holds prices and filings for companies and the index's membership, and nothing about an industry's own prices: contract prices, order books and capacity are published by research firms and trade bodies and arrive as prose inside the documents a search returns, so no code in this system owns one. A theme's facts file is therefore empty by rule rather than by omission, the checker holds a theme section against it, and the section is asked for with no figure and no full date: which way the industry's prices are moving and what is moving them, in words, every sentence citing a document in the theme record. The alternative was a facts file for a theme made of medians over the facts files of the members it covers, and it was not taken because that is a figure code computes about the members' shares rather than about the industry's prices, so a sentence quoting it would be true of something other than what the section is about. (see: Code owns every number) (see: Every number in written prose must exist in the facts file)

**A pass for a name with no facts file refreshes its industry's theme and says so** The theme is the industry's and every member reads it, so a pass that reaches a name the night computed no facts file for still refreshes the cycle its industry is read under, and the cost is the twelve searches and the one call a theme pass makes, under two cents at the off-peak rate. What was wrong was not the order but the page: the name page said the pass did not run while a paid call had been made inside it, so the one surface a reader has said nothing happened and something had. It now states what the pass did and that nothing of the name's own was written, because no facts file is stored. Measured at 6.11 on AAPL, whose bars end before the fixture's night, in each of the three runs whose store is in hand: one theme call or two. Ruled at 8.0.

**A theme refresh runs off-peak, and a name opened at peak is written without one** Research rates double inside the peak windows the research model's configured prices name, and paid work nobody is waiting on runs outside them (see: Queued work runs off-peak, and every schedule is written in UTC). A theme refresh is started by one name's open and serves every member of the industry, most of whom nobody opened, so it is the part of an interactive pass that nobody is waiting on. The theme research runner therefore does not start at peak: its row on the run log says the pass was at peak and names the UTC instant the windows next close, nothing is searched or written, and the stored theme record is left as it was. The name's own sections are written as they would be, with its industry cycle named as not written with that line, and the next open of any member of the industry after the window closes refreshes the theme. Holding the name's whole pass until off-peak was the alternative, and it would leave an operator reading a name after the close waiting hours for sections that do not depend on the theme, which is the failure section 18 already refuses for a theme refresh that fails. The windows are read from the prices the configuration states, so a provider whose windows move is a change of configuration and not of this rule.

**The spend cap is a stop, not an allowance** At the cap, research pauses and the page says so. A cap that can be exceeded is not a cap, and one that fails silently is worse than none.

**The spend cap counts a UTC day and a UTC month, and refuses a call that could take spend past either** The decision above says research pauses at the cap and leaves open what a day is, what a month is, and what "at the cap" means for a call whose price nobody knows until it has been made, and each has a reading a later session could pick that would let the cap be passed with nothing showing it. A day is a UTC calendar day and a month a UTC calendar month, because the research model's own peak windows are written in UTC and a day read in the operator's zone moves against them twice a year (see: Queued work runs off-peak, and every schedule is written in UTC). A rolling twenty-four hours was the alternative, and it has no instant at which research resumes that a page can state; a calendar period has one, the next UTC midnight for the day and the first instant of the next UTC month for the month, and where both caps stop research the month is named, because it resumes later. Spend is summed from the run log's own rows by the instant each started, so the ledger is what the store holds rather than a figure a caller kept. The two caps refuse on their own. And a call is judged by the most it could cost as well as by what has been spent: a gate asking only whether the cap had been reached would let the call that reaches it spend past it, since a price is known only once the provider has counted the tokens, so a call is refused where spend plus its ceiling would pass either cap. The ceiling is computed before the call from what cannot be exceeded, being every byte of the prompt counted as a token, the answer's whole budget, and the peak rate, which is the bound a tokeniser over bytes permits. The cost is research refused inside the last call's worth of a cap that a real call would have fitted into, and that is the cost of a stop. (see: The spend cap is a stop, not an allowance)

**Every paid call is made through the spend cap, which holds the research model** The decisions above say research pauses at the cap and how the cap is counted, and neither says where the check sits, which is the choice that decides whether the cap can be passed at all. Checked by each runner before it calls the model, the cap holds only while every caller remembers to ask, and the runners are two components written at two checkpoints. So the cap is a component, and it is the only one that holds the research model: a runner hands it a request and gets back an answer or a refusal, and has no model of its own to call instead. Before a call it reads what the run log says was spent today and this month and judges the call by the most it could cost; after it, it writes what the call did cost on a run log row of its own, under the pass's run, one row per section asked. The ledger the cap reads is therefore the rows it wrote, measured off the store rather than a figure a runner kept, and a pass stopped at the cap is a row the run page shows rather than a pass that quietly did nothing. The alternative of a check inside each runner was rejected for the reason a feed holds its client: the rule belongs where the thing it governs happens. (see: The spend cap is a stop, not an allowance) (see: The spend cap counts a UTC day and a UTC month, and refuses a call that could take spend past either)

**A research pass does not start where the research model does not answer** Section 18 says the pass does not start and the stored research still renders. A pass that found out at its first paid call would already have fetched its documents and written its local sections, and it would have stopped rather than not started. So a pass whose paid lane has a section to write asks the provider for its list of models first, a request that bills nothing and answers only where the address and the key both do, and does not start where it does not answer: nothing is fetched, nothing is written, and the pass's own row on the run log says why, which is what the name page reads. A pass whose every section is in the local lane asks nothing of the research model and is not stopped by it, because that lane costs nothing and its sections do not wait on the paid one (see: The overnight queue writes the local lane's sections that rest on no document for every name, and the paid model is for names you get serious about).

**A name opened again on the day its research pass ran starts no second pass unless the page asks for one** Section 17 says opening a name twice must never cost twice. A pass decides what to write section by section, off each section's newest version, so a second open writes nothing where every section the first pass warranted got a row. A section the first pass found nothing to write from gets none: no document was handed for it, or no document it was handed was published inside a move, and nothing was inserted, because an empty section citing what was handed would be accepted where it cited an admitted document and would say no admissible source was found where it cited none, which is not why it was not written. A second open would then fetch the year's news and the release again to find the same nothing, which is a second pass costing requests for no section. So a plain open on the session a pass for the name ran to the end starts nothing, and the run log's row for it says why. A pass the cap paused or the research model did not answer did not run to the end, so a later open starts one, since the cap resumes and the model returns. The page's two explicit asks, a rewrite and having the paid model write the local lane's sections, start a pass whatever ran that day, because each asks for something the first pass did not do, and what it writes is still decided section by section, so neither pays again for a section written that day. (see: Everything expensive happens when a name is opened) (see: Deciding not to spend must not cost anything)

**The research model is named only in configuration, and a call is priced at the configured rates its own timestamp falls in** A cap denominated in money needs a price for every call, and the price is the provider's arithmetic rather than this system's: its published rates, per model, for cached prompt tokens, uncached ones and output, multiplied inside the peak windows it fixes. The decision this replaces put the provider's name, its models, its rates and its wire in one feed file, which made changing model a change to code and made a price the provider moves a constant somebody edits. So none of it is in code. The shipped configuration names the wire format, the address the provider answers at, the model, any options the provider takes with a request, the answer's budget, and the rates: dollars per million cached prompt tokens, uncached prompt tokens and output tokens, the UTC hours and the days in which they are multiplied, and the multiple. A different provider or model is different values in that file or in the secrets file beside it, and the feed that sends the request is named for its wire format rather than for anybody's model. A call's price is its response's own counts at those rates, multiplied where the response's own creation instant falls in a peak window, because the provider's clock decides what it bills and this machine's does not. Reasoning is billed as output and a provider in this format counts it inside the completion, so nothing is added for it; a cached prompt is read from the provider's own field where it sends one and from the format's where it does not. A model with no prices is refused at startup rather than called, because a call recorded as costing nothing is spend the cap cannot see, and a response carrying no counts is refused for the same reason. The identity a section records is the model with the options it was asked with, because the same model asked two ways is two writers and the recording key has to tell them apart. The shipped values are the provider's page as read on 2026-09-13 at 14:57 UTC, for the model the shipped configuration names; when the provider moves a price, the configuration is what a change edits, and a call already made keeps the price its run log row carries. The scan that keeps a model off the night still reads every shipped source file for the provider's name, and now finds it in none. (see: Code owns every number) (see: Queued work runs off-peak, and every schedule is written in UTC) (see: The research model is one interface with an implementation per wire format, chosen by configuration and never falling back)

Supersedes **A paid call is priced from the provider's own token counts at the rate its own timestamp falls in**, moved below with its reasoning.

**The night asks for a report on the first name of its list** After its close the night writes one request for the first name drawn on its list, marked as asked by the night, and the pass it leads to is the drain's own, paid at the off-peak rate as every pass is (see: Queued work runs off-peak, and every schedule is written in UTC). One name rather than the whole list, on the operator's ruling of 2026-09-23, "just the top 1st name for now": the name a reader is likeliest to open holds its report when they open it, and any other is asked for. A pass cost between $0.04 and $0.05 over the three the operator's store held that day, so the count is set by what a reader needs rather than by the cap. The count is a constant section 17 pins from 11.4.

## Numbers, claims and the models

**Code owns every number** Every figure in a report is computed from stored data or copied from a provider payload with its filing date. No model is ever asked for a number, a date or an estimate.

**Every number in written prose must exist in the facts file** Enforced by the claim checker. This is what lets a reader trust a paragraph a model wrote: any number in it traces to a computed value.

**A re-run replaces a night's facts file where the store now computes a different one** Until the phase 5 sign-off the assembler inserted with the conflict ignored, so the first file written for a night stood whatever a later run of that night computed, and its comment called that keeping the night idempotent. It was the opposite: a second run exists because the first went wrong, and on 2026-09-10 the attempt that stopped at the change detector had written every name's file from band sets a refetch had doubled, the run that completed after the band repair computed clean files, and every one of them was dropped on the conflict while the run log counted it written. NVDA's file for that night went on naming its immediate support twice. The facts file is what every number in a report is checked against, so it has to be what the store computes rather than what the first attempt computed. The assembler compares the hash of tonight's file with the stored one and, where they differ, deletes that row and inserts tonight's, so a re-run over an unchanged store writes nothing and keeps the change list beside each file, and a replaced file starts with no list until the detector writes it again. The alternative a later session might reach for, an update of the payload, would give the table a second updater, which the rule of one writer per operation forbids; the delete was nobody's. A re-run replaces only the newest night, because every stage computes over the store as it stands, so a night already followed by another cannot be recomputed by running it again.

**A name's facts file is assembled again for its night when an open fetches its fundamentals** A name's fundamentals are fetched when it is opened and its facts file is assembled by the night, so the first open of a name reads a facts file written before its quarter was stored. Over the fixture's KEYS that file held 55 facts and no revenue, margin or segment figure, every section quoting the quarter was refused for figures the file does not hold, and the segment commentary was left out in all three passes of 6.8's first comparison. So where an open's fetch stores a filing the night had not seen, the verb that runs the pass assembles the night's facts again and reads the night's changes again before the pass starts. A re-run replaces only a file that now differs, which is the opened name's (see: A re-run replaces a night's facts file where the store now computes a different one), and the change detector's column on that row is written again rather than left empty. It is arithmetic and no request, so it costs an open seconds rather than money. The alternative was to write from the night's file and let the sections needing the quarter fall back until a later night carried it, which makes a name's first pass its worst and spends on sections that cannot pass.

**Every researched claim must name a stored source document** The claim checker drops any that does not, and the report's source list is generated from what was stored. Numbers are checkable against the facts file; claims are not, so the only available discipline is provenance, and the failure mode of automated research is a confident wrong sentence that nothing else would catch.

**Transcripts are opportunistic, never a dependency** The earnings press release filed with the results announcement carries the guidance, the segment tables and management's prepared framing, and it is free. A verbatim transcript adds the analysts' questions, which is where management is pressed hardest, but only some companies file one. Where it exists it is read from the same free fetch; where it does not, the section is written without it. No provider is bought for it.

**A facts file carries the latest period of the segment table, and says which period it is** The assembler carries the newest period the archive supplies and names the months it covers, so a company whose newest filing is its annual report carries twelve-month segment figures rather than none. It carried only three-month periods until 8.0, and 6.11's production run found the consequence: MSFT's report for the year to 2026-06-30 files a segment table of twelve-month columns alone, so its facts file carried no segment figure and both sections that quote one fell back for a quarter. No fourth quarter is derived from the year and the nine months before it, because those nine months are not stored: the assembler reads the newest filing's own table and nothing else. A filing stored while the archive could not be read keeps its parts marked unread until a newer filing is stored, which is stated as the limit it is rather than repaired, since a stored fundamentals row is never updated and the next filing ends it within a quarter. Ruled at 8.0.

**The facts file carries a quarter's growth, its earnings against the estimate and the filing's own tables with their year-earlier columns** The facts file carries, from the newest filing's row, the quarter's earnings per share and the estimate for it, the quarter's growth on the same quarter a year before and on the one before it as the fetcher computed it, and from the filing's own tables the latest period and the same months a year before, each group's growth on it, and the tables of revenue by market, product or region beside the segment table, each named by what its title says it groups by (see: A quarter's growth is computed on its own row from the filings the provider returned) (see: A group's growth in a filing's own tables is computed from the columns the table states) (see: The filing's other tables of revenue by a grouping are kept beside its segment table). It carries the company's name the membership row holds as well, which every prompt lists and a pass reads to rank a document whose title names the company. The first live research pass, NVDA's, was refused for revenue up 106%, data center revenue of $89.0 billion up 117%, $27.0 billion billed to customers in Taiwan and earnings of $2.22 a share, and nothing it held carried any of them; a figure in prose has to exist in the facts file, so a section can quote what a release headlines only where the file carries it (see: Every number in written prose must exist in the facts file). Every figure is read off the stored row and none is derived here, which is the assembler's rule; the growth figures are the fetcher's.

**A segment figure held for a period longer than a quarter is asked for by that period and refused where its sentence names a period of another length** A facts file carries the newest period a segment table files and names its months (see: A facts file carries the latest period of the segment table, and says which period it is), and that name was all that stood between an annual report's figures and a sentence calling them a quarter's. The segment commentary was still asked what each segment reported for the quarter, and the claim checker matches a figure against the values the file holds, so a year's figure written as a quarter's matched and passed, while the same figure written for its 12 months was refused for the 12. A wrong period is caught by the figure beside it failing to match that period's value (see: A claim is a sentence, and every sentence in a researched section names the document it rests on), and where the file holds a figure for a longer period alone there is no other period's value for it to fail against. So over a table that files no quarter the segment commentary is asked for the months and the end its figures cover, and told to name that period by its months and never as a quarter, a half or any other period. In every section, a figure whose every matching fact is a segment figure for a period longer than a quarter is refused where its sentence names a period of any other length: a quarter by its word, its adjective or Q1 to Q4, a half as a first or second half or as H1 or H2, and three, six, nine or twelve months in words or in digits. The months of a period the file names, the 12 of 12 months, are read as a period and not as a figure, as the twelve months in words already were. The quarter's ask stays word for word, because every recording of the section is keyed on the request it answers. The rule ends in three places. A year named by no count of months, the year to date and a half written alone are not read as periods, the last because it is as often a share. A figure that also matches any other fact passes under any period's name, because the match is on existence rather than attribution. And over a quarter's table no period's name is read, so a quarter's figure called a year's passes as it did. A sentence naming its own period beside another, as twelve months set against a quarter, is refused, which costs a retry and writes no wrong sentence. Nothing a night computes changes: the rule reads only a facts file carrying a period longer than a quarter, and read after the night of 2026-09-16 the operator's store held no fundamentals row and none of its 3,033 facts files carried a segment figure. Ruled at the 8.0 correction.

**The segment table is the first of the filing's segment reports to state revenue by segment** A filing renders several reports whose names mention segments, and the route reads up to four of those in the Details category in the order the filing lists them. It takes the first that states revenue under one of its groupings, and only where none does, the first that carries any figure by segment. It took the first carrying any figure until the first live research pass found the consequence on NVDA: that filing lists a narrative table first, carrying depreciation by segment, the counts of segments and a customer concentration share, and its schedule of reportable segments second, so the name's page showed depreciation and a concentration share as its segment table, with no segment revenue at all, and the segment commentary had no revenue to write about. Revenue is read off the concept a row is filed against, in the three spellings the captured tables file it under, and never off a label, which is the filer's own wording. The preference moves no other captured filer's choice, because the first table each of them takes already states revenue. The same narrative table wrote its share as percentages under a title stating millions, and a row whose every figure is written as a percentage carries the unit % and is left as filed rather than scaled, as a count of segments already was. A filing whose candidates state no revenue by segment costs up to three more page requests than before, on a pass the operator started and never on the night.

**The filing's other tables of revenue by a grouping are kept beside its segment table** A filing states revenue by more than its segments, and the figures a results release headlines are often in those other tables rather than in the segment table. The first live research pass found it on NVDA: its segment table states two segments, while the quarter's data center revenue of $89.0 billion, the figure its release led with, is in the table by market platform, and the revenue billed to customers in Taiwan, $27.0 billion, is in the table by region. The sections written that day quoted both, and the claim checker refused both, because nothing the store held carried them. So past the segment table the route reads the filing's segment reports whose names mention revenue or sales, at most three, and keeps each that states revenue under a grouping, in the segment table's own shape and on the newest filing's row beside it. Read by name rather than by table, which the segment table is not, because these are read only once the segment table is found and a filing naming no such report costs no further request: the two other captured filings name none, so their reads are the pages they were. The cost is a filer whose table by product sits under a name that does not mention revenue or sales, which is not read, and its row says the archive served no such table rather than that there is none.

**An answer that comes back empty or cut short is asked for once more** A model's answer that arrives and cannot be stored, with nothing in it or stopped at its budget, is asked for once more with the same request, and a section whose second answer is unusable too is left out with a reason a person reads on the name page: that the model's answer came back empty or cut short twice. What the model sent, its finish reason and its token counts, stays on the run log's row for the call. The first live research pass found both halves of it: the research model spent a call's whole answer on its reasoning and returned nothing for NVDA's cause of each large move, 3,156 of 3,157 completion tokens reasoning, and the pass left the section out on that one call with the provider's own sentence on the page. An answer spent on reasoning is not a property of the section, since the same request answers on another ask, where a refusal or a model that does not answer is, so those are not asked again. Once and not more, because an unusable answer is billed, and each ask is a call the spend cap judges and the run log prices under a stage of its own. The claim checker's one retry is a different thing and unchanged: it asks again with the reason a draft was refused, where this asks again for a draft that never arrived.

**Guidance is stored as management's own prose, and the facts file carries each figure the passage states as the claim checker reads it** Supersedes **Guidance is stored as management's own prose and never parsed into a figure**, moved below with its reasoning, all of which stands but its last consequence. The archive stores the located passage, its heading, the exhibit and the date it was filed on, and the numbers section shows management's own words, as before. What changes is that the facts file carries every figure the claim checker reads in that passage, each as the checker reads it, a magnitude times the scale its unit word carries, named for its place in the passage and never for what it guides. The first live research pass found the want of it: NVDA's release guided the next quarter's revenue to $108.0 billion, plus or minus 2%, and its gross margin to 74.0%, and the two cases quoting management's own figures were refused for both, because a figure in prose has to exist in the facts file and the passage's figures were in no fact. The superseded entry refused a guided-quarter figure because a pattern that finds a range in one filer's wording misses it in the next, and reads a range as whatever the pattern takes it for; that stands, and nothing here says which figure is revenue, which is a margin or which is a bound. The claim checker's match is on existence and not on attribution, so a sentence that quotes the passage's $108.0 billion as revenue passes, and so would one quoting it as anything else, which is the cost the checker already states for every figure it reads (see: Every number in written prose must exist in the facts file). A passage no heading located carries no figure, because nothing says which paragraph is management's guidance. (owes: The computed fundamental panel, and whether a fundamental state may fire a reason or gate a tranche)

**The local lane's scope is a setting, and whatever it holds is written for nothing** Section 13 assigns work per section rather than per pass, so which sections the local model owns is configuration. On a machine with 24GB of graphics memory that is the short-prompt work, and which of it stays local is what the fixture comparison measures (see: The fixture comparison moved the cause of each large move into the paid lane on this machine). Synthesis is not in it, because a model small enough to leave room for the whole evidence set on that card is the case already named as producing something fluent that says nothing. On a machine that can hold a large model with a long context, synthesis moves into the local lane by changing the setting, and a pass writes it for nothing from the documents that pass fetched. The overnight queue writes the part of the lane that rests on no document, and a synthesis section rests on documents, so moving one left does not put it in the night (see: The overnight queue writes the local lane's sections that rest on no document for every name, and the paid model is for names you get serious about). The build does not change between those two machines, and the fixture comparison rather than the memory figure decides whether a section should move. Supersedes **The local lane's scope is a setting, and the overnight queue writes whatever is in it**, moved below with its reasoning.

**A claim is a sentence, every sentence in a researched section names the document it rests on, and a window written in words is read as its number** The two decisions above say every number in prose exists in the facts file and every researched claim names a stored source, and neither says what a claim or a number is when a checker reads a paragraph, so a later session could draw either line anywhere and nothing would show it. A claim is a sentence. Every sentence of a researched section carries a citation, written as a D and the document's position in the section's own source list in square brackets, and the citation has to resolve to a stored row admissibility admitted. A position rather than an id, because a model copying a thirty-two character id into prose is a model given the chance to get one character wrong. The key under each figure is the one section held to the number rule alone, because the lane table says it is a fixed explanation over known values, and asking it to cite a document would be asking it to invent a source for arithmetic.

A figure is a rounding of a value the facts file holds, at the precision the prose itself states: half a unit at the last decimal the prose writes, scaled by the prose's own unit word, so $109.4 billion is a rounding of 109417000000 and $110 billion is not. A percentage is also tried against a value stored as a fraction, which is how the margins are held. The match is on magnitude and not sign, because the sign of a move is carried by the words around it and a signed match would refuse every "fell 3.2%" written from a stored fall, and it is on existence rather than attribution. Some numbers are not figures. A four-digit year standing alone carries no claim about a value. A quarter or half label, a fiscal year, a time of day and an ordinal from first to tenth say which period or which place, and a wrong period is caught by the figure beside it failing to match. The length of a window, the 200 of a 200-day average, has to be a number the facts file carries in the name of a figure or as a count it holds. A date has to be one the file holds. The S&P 500 and the twelve months a filing reports over are named as the two phrases whose digits and words are neither. A number written in words from eleven up, or a scale word standing on its own, is refused as a figure nothing can match, while one to ten are ordinary language and are not read, which is a stated cost: "two segments" where there are three passes. A window written in words from eleven up, "the twenty-day average" or "fourteen periods", is read as the number its words name and held to the facts file as a window in digits is. The local lane's key under each figure named the averages' lengths in words, and over the operator's store on 2026-09-18, 427 of the 455 reasons it gave for the 151 drafts of that section it refused were a number written in words, most of them an average's length the facts file carries in a figure's name; read this way, 90 of the 151 would have passed, and the rest state a value in words or a figure the file does not hold.

The checker writes two outcomes and two waypoints. A first refusal is `rejected` and the writer may try once more as the next version; a second consecutive refusal on the same as-of date is `fallback` and the section is left out with both attempts' offending text kept on their own rows. A researched section whose source list holds nothing admitted goes to `fallback` on its first check with the reason that no admissible source was found, because a rewrite cannot create a source. A version following a fallback is a fresh first attempt.

The reader was measured before anything rested on it, over the four captured articles, the two filed releases and 411 articles from one live news request, being 15,412 sentences and 11,727 figures, and two findings from that measurement are the reason this entry states its limits in numbers. The sentence reader holds: of the 15,412, 700 are three words or fewer and the ones read are real short sentences and page furniture rather than splits at an abbreviation. The measurement found three misreads and each is repaired and asserted: a percentage before the word week read as a window, a time of day read as two figures, and a strength of 2 matching 200% through the fraction reading. And the number rule is a strong test for a figure stated with decimals and a weak one for a whole number. Against AAPL's facts file of 38 values, 1,392 of the 11,727 figures, written by nobody with that file in mind, pass by coincidence: 1,301 of 7,939 whole numbers, 76 of 2,091 at one decimal, and 14 of 1,547 at two, some of which are AAPL's own figures. A model inventing "revenue grew 12%" passes where any value near twelve exists. That is not repaired here, because what would repair it is attribution, a figure tied to the fact it rounds, and the prompt that could require prose to name its facts is written at 6.6, which is where the first model-written sections exist to measure it against (owes: A whole-number figure tied to the fact it rounds, measured against written sections). (see: Every number in written prose must exist in the facts file) (see: Every researched claim must name a stored source document)

Supersedes **A claim is a sentence, and every sentence in a researched section names the document it rests on**, moved below with its reasoning.

**A cause of a move rests only on a document published inside that move** The decision above requires every sentence of a researched section to cite a stored document that admissibility admitted, and says nothing about whether that document could support the sentence. For every other section that is the provenance discipline the corpus asks for. For the cause of each large move it is not enough, because the sentence is about a dated event and the document is dated too: a release published months after a move cannot be what moved it, whatever it says. The first pass a local model wrote at 6.6 showed it on real prose. Handed the Q3 release KEYS filed on 2026-08-18 and asked for the cause of each move, its first draft said the move of 2026-02-24 was driven by that release, and was refused only because the filing date it quoted happened not to be a date the facts file holds. Its retry cited the same release for eight moves and named no cause at all, and it was accepted, because every sentence cited an admitted document and every figure was in the file.

So a sentence of the cause section names a move by the session it ended on, and every admitted document it cites has to have been published on or after the session the move's change was measured from and on or before the session it ended on, for every move the sentence names. A sentence naming no stored move's session is refused, since there is nothing to check its citation against. The facts file carries each move's start for this, read as the stored session as many sessions before the end as the move spans, which is the close the annotator divided by; before 6.6 it carried only the end, and a span with one edge cannot hold anything. Both edges are inside, because a publish date carries no time: a release filed after the close on the start session is dated that session and is what moved the next one, which is the fixture's own case, and the same reading admits a document dated the last session and published after its close, which cannot have moved it. That is the cost of a date without a time, stated rather than guessed around.

Which document falls inside which move is worked out by code and handed to the model, rather than left for the model to compare. Told each move's span and asked to compare the dates itself, the model cited the August release for the five moves in February and March and wrote nothing for the one move it falls inside, which ran from 2026-08-17 to 2026-08-24; handed the pairs, it wrote one sentence for that move and the checker accepted it on the first draft. Comparing two dates is arithmetic, and code owns it (see: Code owns every number). A cause section whose admitted documents fall inside no move is not sent to the model at all, because asked anyway it wrote a paragraph explaining why it could not write one, which cost a call and the section's one retry.

One limit stays, and it is the one this rule cannot reach. The checker holds when a document was published, what it is, and every figure in the sentence; it does not hold that the document says what caused the move. The accepted sentence reads that the fall into 2026-08-24 was driven by record third quarter results, which is the model's reading of a company's own release about a move of minus fourteen per cent, and a release is where a company states its results rather than why its shares fell. What would carry a stated cause is reporting, which the research runner hands in from 6.8, and nothing here claims more than that the document was there to be read (see: A claim is a sentence, and every sentence in a researched section names the document it rests on).

**A dated calendar item's date rests on a document the sentence cites and falls after the night the facts were computed for** The number rule holds a date in prose to a date the facts file holds (see: Every number in written prose must exist in the facts file), and the facts file carries the provider's next earnings date and nothing a document dates, so as 6.4 built it every date the dated calendar items exist to state was refused, which 6.6 found and carried to the checkpoint that would write the first of them. 6.8 wrote them. The research model's first draft copied four dates from the documents it was handed, two being investor conferences after the night and two being the days the announcements were published, before it; all four were refused, and its retry wrote a sentence saying nothing could be written. So a date in that section is held to the documents and to the night rather than to the facts file: it has to be a date an admitted document the sentence cites states, read by the reader that reads the prose so that the two forms compare, and a full date has to fall after the night the facts file was computed for, which the prompt now states. Reading such dates into the facts file was the other form, and it would have had code choose which dates in a document are events, which is the judgement the model is asked for, in a file whose rule is that everything in it is computed or copied with the filing date it came from. The first pass written under this rule had the section accepted on its first draft, naming the two conferences. A year standing alone is still not a date and is still exempt, so a sentence about a standard timeline set for 2029 passes, which is stated rather than guessed around.

**A research record is written and dated per section, not as a whole** On the current machine some sections are drafted locally overnight and others are written by the paid model days later when a name is opened, so a single as-of date and a single model name for the record would be false. Each section carries its own state, its own date and the model that wrote it, which is what lets the report state exactly what you are reading and stops the staleness judge treating a half-written record as complete.

**The overnight queue writes the local lane's sections that rest on no document for every name, and the paid model is for names you get serious about** After the nightly arithmetic, the local model works through every name in the index whose research is missing or stale, the names on tonight's list first in order of how many reasons fired, and writes the local lane's sections that rest on no document, at no cost (see: The key under each figure is dated by the night whose figures it explains, written for every name each night, and drawn only beside that night's figures). The night fetches nothing for a name: step 17 is carved out of the zero-model-call rule alone and reaches the local model and nothing that fetches (see: The night's zero-model-call rule bounds the arithmetic, and the overnight queue is carved out of it by name). A section that rests on documents rests on ones a pass fetched and stored, and the source store ties a document to a name through nothing but a section citing it, so the queue hands the writer no document and asks it for none of those sections. In this machine's lane what the queue writes is the key under each figure. What the company sells and the segment commentary are written by the pass an open starts, which fetches the company's own filing, so a name nobody has opened opens in the morning with its key written for the night and the rest written when it is opened. A name whose only outstanding work is a section resting on documents is not queued at all, rather than counted every night as a pass that could write nothing. Handing the writer the documents a name's stored research already rests on was the alternative, and it was not taken: a stale section rewritten tonight from documents older than the event that made it stale is dated after that event while reading nothing the event produced, so the judge would read it as research that stands (see: Research goes stale on an event dated after the section was written, and a news spike is dated by the session it began). Opening anything else, or pressing refresh on a name whose local draft is not good enough, runs a paid pass on demand, and the record stores which model wrote each section, so the page offers the paid model's rewrite of a local draft while saying only when each section was written (see: Every part of a page states where it came from and as of when, and a written section when it was written rather than which model wrote it). Supersedes **The overnight queue writes the local lane's sections that rest on no document, and the paid model is for names you get serious about**, moved below with its reasoning, the queue then working through the names on tonight's list alone and the report saying which model wrote what you are reading.

**The key under each figure is dated by the night whose figures it explains, written for every name each night, and drawn only beside that night's figures** Supersedes **The key under each figure is written for each night's facts file**, moved below with its reasoning. That entry wrote the key again each night for the names on tonight's list, and drew a key the queue had not written again beside the night it was written for. After the night of 2026-09-18 the operator's store held that night's key for 424 of 503 names, an earlier night's for 78 and none for one: 70 of the 78 were not on that night's list, so the queue never asked, and the checker refused both drafts of the other eight, so each of those pages drew a paragraph about figures the page above it no longer showed. Three things change. The overnight queue writes the key for every name in the index, the names on tonight's list first in order of reasons fired, which the queue's hour covers at the slowest pass 6.10 measured (see: The overnight queue is bounded by its own limit rather than the night's deadline, and starts no pass once the limit has passed). A key's row is dated by the night of the facts file it was written from rather than by the day it was written, because a pass run in the day writes it from the night before, and dated by that day it would be drawn beside the next night's figures as though it explained them. And a page draws the key only where that night is the session its figures are from; anywhere else the key's card names the night it was written for and says it is not drawn. This is still not a timer: the event is a night's facts file, which is the only thing the key is about (see: Nothing expires on a timer). It costs the local model a pass a name a night, and nothing is spent.

**The key under each figure is handed its facts as a reader reads them, rounded by code** The key explains figures to a reader, and it was handed them as the store holds them, `rsi14 = 47.740381`, and asked to round them itself. Over the 51 names whose first draft the checker refused on the night of 2026-09-18, the local model cut digits off where it meant to round, 271.9963 written as 271.99, and wrote a window in words or as RSI-14, which the checker reads as a figure; and a draft that passed had copied its figures to six places onto the page, 4.6 of them a key over 28 first drafts that passed. So the key is handed each fact as a reader reads it: an indicator named with its window as days before it, as the page labels the averages, money and counts in millions, billions or trillions with the word, a growth or a margin as a percentage to one place, and anything else to two places. Each reading is kept only where the claim checker matches it to the stored value, so a copied figure passes, and the checker still reads every figure in the prose against the file as stored (see: Code owns every number) (see: Every number in written prose must exist in the facts file). Every other section is handed the file as stored, since its recordings are keyed on it and none was refused this way. Measured the same day, asking the local model again at temperature 0, which gave three of those names the same refusal that night's prompt had: with the key as it is now handed, 50 of the 51 and all 28 passed on the first draft with no figure written past two places, and NVDA, the one name whose facts then carried a filing, passed with its revenue written as 96.22 billion and its growth as 105.9 per cent. The one refused was a company whose name carries a number, Phillips 66.

**The overnight queue is bounded by time, not by a count of names** One pass may take four minutes and another twelve, so a name count does not bound anything. The queue works in priority order until a stated number of hours has passed and then stops; whatever is left is first in line the next night or is written on demand when you open it. A busy night degrades by leaving names for later rather than by running into the morning.

**The overnight run holds the machine awake and reports whether it ran** A laptop left alone will sleep, and a nightly job that silently did not happen is worse than no nightly job, because nothing announces it. The run asserts wakefulness while it works, and the run page states the previous night's outcome including how many queued passes completed and how many were left.

**The overnight queue is bounded by its own limit rather than the night's deadline, and starts no pass once the limit has passed** The queue is bounded by time (see: The overnight queue is bounded by time, not by a count of names), and that leaves two things open a later session could read either way. The first is what bounds it. The night has a deadline of three times its wall clock that it cannot move (see: A feed is tried three times with a doubling backoff, and the night has a deadline it cannot move), sized for the arithmetic, and a queue held to it would get whatever the arithmetic left of fifteen minutes, so an hour's limit would be cut to the rest of that quarter hour with nothing saying so. So the deadline bounds steps 1 to 16, step 17 is handed none of it, and the queue is bounded by its own limit alone. The second is what the limit stops. A pass is a name's sections, each a call to the local model and a check, and one cut off inside a call leaves a draft written and never checked, which waits on the checker and keeps the next pass from writing that section again. So the limit is read before each pass starts: a pass started before it runs to its end, a name whose turn comes at or after it is left for the next night, and the queue's row names every name it left. The cost is that the queue runs past its limit by at most one pass, which 6.10 measured at 5.13 seconds at the slowest over 12 passes on this machine.

**A night the overnight queue did not run is a traded session with no queue row, read on the run page against the exchange calendar** Section 18 says a night the machine slept writes nothing and the next run reports the gap, and a gap has two readings a later session could take: the nights whose arithmetic ran and whose queue did not, or every session the exchange traded. A machine asleep at the scheduled instant runs neither the arithmetic nor the queue, so the first reading misses exactly the night the row is about. The run page therefore names each session the exchange traded that carries no queue row, from the one after the newest night before the page's own on which the queue ran, up to and including the page's night, and names none before the first night the queue ever ran, since the store's nights from before the queue existed are not nights it failed to run. Nothing is written for such a night: a queue that ran writes its row, a queue that did not writes nothing, and the page reads the absence against the calendar rather than leaving a reader to infer it from an empty region.

**Two models for two jobs, and which research model answers is configuration** A local model on the operator's GPU writes prose from numbers, which is small and free. A hosted model does research, which is a large-context job a local model does badly. Which hosted model is a setting rather than a decision, so another is chosen by changing configuration and not by superseding an entry here (see: The research model is named only in configuration, and a call is priced at the configured rates its own timestamp falls in). The shipped configuration names DeepSeek V4, chosen on cost: a research pass is a few cents rather than a few tens of cents, which puts a year of normal use in the tens of dollars. The tier, Pro or Flash, is left to the fixture comparison rather than decided here, because the difference between them is a few dollars a year and the only thing worth choosing on is the quality of the two-cases section; the shipped configuration names Flash until that comparison runs. Every section records the model that wrote it, so a change of model shows on each section written after it and on none written before (see: A research record is written and dated per section, not as a whole).

Supersedes **Two models for two jobs, and the research model is DeepSeek V4**, moved below with its reasoning.

**A research pass is split by section difficulty, not run wholesale on one model** The dividing test is whether the answer sits inside one document or has to be built across several that disagree. Classification and extraction stay on the local model at no cost, and each such call carries only the documents that section needs rather than the whole evidence set, which is what keeps it inside a consumer card's memory. Synthesis goes to the paid model, because a small model given contradictory evidence produces something fluent that says nothing. Figure 12.2 shows where the line falls. The line itself is provisional: the frozen fixture makes it measurable, so each section is run both ways against the fixture and the boundary is settled by that comparison rather than asserted here (owes: The research lane boundary measured against the fixture both ways).

**The local model answers at an OpenAI-compatible endpoint, and which model answers is configuration** The lane split was a setting with no shape, so nothing said how a section reaches a model at all. The local model is reached over HTTP at an OpenAI-compatible chat completions endpoint on the operator's own machine, which is the interface LM Studio, `llama.cpp`'s own server and Ollama all serve, so the runtime is the operator's choice and not the build's. Configuration carries the base address, the model identifier and the per-call timeout, and nothing else. The model is Qwen 3.5 9B on the machine this is first built for, and a larger or later one is a setting rather than a change to this entry (see: The local and paid boundary is a property of the machine, not of the design). The identifier is stored on every section the model wrote, which is what makes a comparison between two models a reading of the record rather than a memory of which was running. A local call needs no key, and a key configured for the local lane is refused rather than sent, because a local endpoint that authenticates is an endpoint on somebody else's machine and the queue's zero-cost property rests on this lane being free.

**The local lane is a configured list of section names, and the prose writer writes whatever the list holds** Which sections the local model owns is a setting, and a setting with no shape becomes whatever the first implementation does (see: The local lane's scope is a setting, and whatever it holds is written for nothing). It is a list of section names in configuration, read once per pass, and the writer takes the list rather than deciding section by section, so moving a section between lanes changes a value and touches no code. On the machine this is first built for the list holds the cause of each large move, what the company sells, the segment commentary and the key under each figure, which is the short-prompt work figure 12.2 puts on the left (see: A research pass is split by section difficulty, not run wholesale on one model). The short version is not in it, and that is the list's value rather than a fact about the component: section 4, figure 12.2's table and section 15.12 each put it in the paid lane, while the prose writer's catalogue row said it wrote that section on the local model at no cost. Neither side was a design fact, so neither is superseded and nothing is corrected except the row, which now names the lane where it named the section. A section in the list the machine cannot hold is refused before the pass starts and left for the paid path, which section 18 already promises, so a list edited past what the card holds degrades rather than failing.

**The fixture comparison moved the cause of each large move into the paid lane on this machine** The lane decisions leave which sections the local model writes to a setting, and say the comparison against the fixture decides whether a section moves (see: The local lane's scope is a setting, and whatever it holds is written for nothing). 6.8 ran it: one name's pass over one evidence set, every section asked of the local model and of the research model, each through the claim checker and its one retry. The research model's sections were accepted for all eight it could write. The local model's were accepted for three, what the company sells, the segment commentary and the key under each figure, and left out for the other five, the cause of each large move among them: handed the documents inside the name's eight moves, it wrote figures from the articles that the facts file does not hold, and wrote them again when told why, in every pass it wrote that section in. So the list on this machine is those three, and the cause of each large move is written in the paid lane, at about a cent a pass. The entry that first stated the list named the cause among the local sections before any comparison had run; its rule, that the lane is a list the writer takes whole, stands, and what this changes is the list's value. The four synthesis sections failed locally as that entry expected. A different machine runs the comparison again rather than taking this list (see: The local and paid boundary is a property of the machine, not of the design).

**The research model is one interface with an implementation per wire format, chosen by configuration and never falling back** The endpoint was already configuration and that is not enough, because two research providers do not share a request shape, so the choice of provider is a code path even where the address is not (see: Two models for two jobs, and the research model is DeepSeek V4). The research model is one interface with one implementation per wire format, and configuration names which provider answers. It is resolved in the one place the on-demand feeds are resolved, refusing in both directions, and it never falls back from one provider to the other. A fallback would make the model that wrote a section unknowable from the fact that the section exists, and every section is already dated and stamped with the model that wrote it for exactly that reason (see: A research record is written and dated per section, not as a whole). A provider named in configuration with no key refuses at startup rather than at the first pass, which is the shape the credential path has carried since 2.1. The switch is also what makes the lane boundary measurable rather than asserted, because the recording is keyed on the canonicalised request, so one section over one stored evidence set through two providers is two records a reader can set side by side.

**The archive declares a contact in its user agent, and a blank one refuses at startup** The filings archive is the first provider here that refuses a request on a transport rule rather than on a credential: it answers a request naming no user agent with a refusal, and its fair-access policy asks that the agent carry contact details. So the agent is a credential path in shape and not in kind, and it is built like one. The header is composed from the tool name, a version read off the assembly and a configured contact, and a contact that is absent or blank refuses at startup rather than at the first request, which is the shape the credential path has had since 2.1: a run that reached the archive and came back refused would read as the archive being down. The contact is a dedicated address rather than the operator's own, and the reason is not privacy in the abstract. The value is configuration in the gitignored secrets file beside the provider keys, and a value that identifies a person sitting in that file is one more thing that must never reach a captured fixture or a committed manifest, so the manifest's credential scan covers the agent and a dedicated address narrows what a leak would be. Declining to send contact details at all was considered and rejected: it trades a real refusal risk for one omitted string, since a caller that declines is one the archive may rate-limit, and the corpus would then have to describe and handle a failure mode it created for itself.

**A stored source is not automatically an admissible one** Requiring that a claim name a stored document proves where a sentence came from, and proves nothing about whether that document is worth believing. A search for a company by name returns machine-generated price forecasts, broker marketing pages quoting contract-for-difference prices rather than the stock, year-old articles carrying a previous fiscal year's guidance with no visible date, and summaries written by other AI systems. Each of those would satisfy a bare provenance rule. So admissibility is a second test, applied before a document is stored: deny algorithmic price predictions, broker and platform marketing pages, AI-written summaries, and quote or hub pages carrying no article; require a publish date and reject anything outside the window the pass asked for; prefer the company's own filings and releases, then established financial press, and make the pass state why it used anything else. A document that fails is not stored, so a claim resting on it cannot be written.

**Gated sources are out of scope, and access is ranked before reputation** Paywalled and login-walled sites cannot be read by an automated pass and no attempt is made to work around them. The order of preference follows access rather than prestige: filings and company releases first, which are free, complete and fetched by ticker rather than searched; then the licensed news feed, where the provider delivers article text under its own distribution agreement so the paywall question does not arise; then the open web, which is where gating actually bites. What is genuinely lost is the analyst-opinion layer and primary industry research. Where the substance of a gated report reaches free outlets as secondary coverage, that coverage is the source and is marked as second-hand rather than presented as the original.

**Two source lists govern the open web, and the licensed feed is a channel rather than a list** Filings and company releases need no list, since they are fetched by ticker. A company-news list governs reporting about a company and an industry list governs theme material, because the publishers who write about memory contract pricing are not the ones who write about quarterly earnings, and splitting them keeps a theme search from returning earnings coverage and the reverse. Every list carries a review date, because sites are acquired, paywalled and abandoned, and a list nobody revisits silently narrows what the system can see.

Two is what the previous entry described and three is what it was called, which is the whole of contradiction J. What is new is where the lists apply. The 1.7 measurement found that the licensed news feed delivers 1,173 of 1,253 articles under one domain, and that the domain is the aggregator that carried the article rather than the outlet that wrote it: the writing outlet appears inside the text where it appears at all. So a domain list over the news feed would hold three entries and would filter nothing, and filtering the feed by domain would discard the whole feed or none of it. The lists therefore govern the search tool, which is where a query can return a football club or a forum post, and the news feed is admitted as a delivery channel with the per-document admissibility test doing all of the work on its articles. (see: Admissibility is judged per document, after the fetch, and never per publisher)

**A list of publishers is a noise filter, never a correctness test** The list decides which sites a search may return; the per-document test decides whether a particular article may be believed. Both are needed and neither substitutes for the other, because a publisher that belongs on any reasonable list also runs promotional opinion pieces and syndicates other people's, and a trial search returned exactly that from a well-regarded name. A list also has to be of sites that are reputable and fetchable, which is a smaller set than reputable: the most authoritative names in this field are the gated ones, so a list assembled by prestige is substantially a list of pages that return nothing.

**Admissibility is judged per document, after the fetch, and never per publisher** A publisher that runs ordinary financial journalism also runs machine-generated price forecasts, so denying the domain throws away the good with the bad and allowing it admits the forecasts. A search tool's domain filter cannot express this, since it operates on the site rather than the article, which means search parameters reduce noise and the admissibility test does the actual work, on the document, after it has been retrieved.

**Each denied category is refused by a marker the document carries, and the domain is never one of them** The decision above says the test is per document and says nothing about what the test reads, and what it reads had to be measured rather than guessed, because a rule over words refuses whatever shares a word. Eight pages were asked for by hand on 2026-09-13 UTC, two per denied category, and read against the four articles and two release exhibits the committed fixture already holds. Seven answered and one refused automated access, which produced section 18's unretrievable case as a measurement rather than a construction.

Four findings decided the rules. The first is that the domain says nothing, measured rather than argued: finance.yahoo.com delivered a page that must be refused, a price prediction piece with a named author, a publish date and twelve paragraphs of prose, and a page that must be admitted, an ordinary earnings preview, and it is the same domain the fixture's four captured articles arrive under. The second is that a price forecast page need not say it is one: the forecast site read carries five prose paragraphs, a table of yearly figures out to 2050 and no statement anywhere that an algorithm produced any of it, so the marker is the title and the address rather than the text, and it is a pairing of a price word with a prediction word rather than either alone, because real reporting pairs forecast with revenue and guidance constantly. The third is that three of the five refusable pages carried no publish date at all, which is why the gates are judged with the kind before the date: a build judging the date first would refuse most of what it refuses for the cheapest reason available and would report almost nothing about the kinds, and what is stored is one verdict. The fourth is that the regulatory risk warning is the one reliable marker of a page that exists to open an account, verbatim in two forms on the page read, and that no article warns its own reader about losing money. The fifth came from the live run this checkpoint made after the rules were written, over 411 real articles from one dated request, and it is the reason the rules were changed rather than confirmed: invitation language does not separate an article about accounts from a page selling one. Three of the 411 were refused as marketing and all three are ordinary consumer-finance reporting. Two tripped on one invitation written twice, a piece on what retirees put off saying sign up and signing up about a benefit, which counting kinds rather than phrases repairs. The third survived that repair, an article on how savers lose a retirement pot saying opening an account and sign up, which are two genuinely different invitations inside an article whose subject is accounts. So nothing rests on invitation language alone, and the second net is a leveraged-product term beside an invitation, measured on the same 411 before anything rested on it: contract for difference appears 0 times, cfd 0, spread bet twice and trading platform three times, and not one of those five carries any invitation at all.

So: a price forecast is refused on a price word paired with a prediction word in its title or its address, on an address whose last segment is the forecast itself, or on a stated algorithmic projection in its text. A marketing page is refused on one regulatory risk warning, or on a leveraged-product term beside one invitation to open an account, the pairing rather than the invitation because a single sign-up line is in the navigation of half the web and two of them are in any article about saving. A summary is refused where it declares that a system wrote it, and only then. A quote or hub page is refused where its address or title says it is a symbol page and no paragraph of it runs to at least 3 sentences and at least 40 words, both halves required. The thresholds are measured against the four captured articles and not chosen: the tightest of them carries exactly one paragraph that qualifies, at three sentences and fifty-two words, so a threshold of four sentences would refuse a real article, and that thin margin is why the shape is a second condition rather than the whole rule.

Two limits are stated rather than left to be discovered. Text a system wrote and nobody disclosed is not something a rule over the text can find, so the marker catches what platforms say about themselves and nothing more; what stands behind it is the preference for primary sources and the claim checker, since a number absent from the facts file cannot reach prose whatever wrote the page it came from. And an article about the regulation of leveraged products, quoting the warning, would be refused: that is one article lost against admitting pages whose whole content is marketing.

The preference for primary sources orders and never refuses, which is what keeps it from becoming the publisher list this corpus has twice ruled out. It ranks by the channel rather than by the host, because the host of a news feed article is the aggregator and ranking by it would put every article in the feed in one place and that place would be the wrong one. (see: Admissibility is judged per document, after the fetch, and never per publisher) (see: A stored source is not automatically an admissible one)

**Theme material comes from a search tool, and per-name material never does** A per-name pass reads filings and news fetched by ticker, which is exhaustive over a date range and cannot return the wrong company. An industry's own pricing cycle is published by research firms and filed with nobody, so that one section needs open search. The tool is Tavily, called as one more fetching tool inside the research loop; its results are stored as documents exactly like a filing or an article, so the claim checker and the admissibility test apply unchanged. Volume is a few hundred searches a year against a free allowance of a thousand a month, so cost is not a factor and will not become one. A self-hosted meta-search is the fallback if that allowance changes, since nothing in the design depends on which tool answers.

**A theme search is scoped by parameter, not by hope** Trial searches showed what an unscoped query returns: for a two-letter ticker, results about a football club; for a company name, a machine-generated price forecast, a broker marketing page, a year-old article carrying a previous fiscal year's guidance, and another AI system's summary. So a theme search names the industry rather than a ticker, is bounded by an explicit date range, restricts results to the industry source list, and requests full page text rather than snippets, because a snippet cannot be stored as the document a claim rests on.

**A theme pass searches each site on the industry list alone, and hands the model a bounded set of the pages they return** Measured at 6.11 over the quarter to 2026-09-08. One search restricted to the whole list of twelve sites returned nothing for any of the fixture's four industries, Consumer Electronics, Scientific & Technical Instruments, Software - Infrastructure and Entertainment, nor for Semiconductors, which had returned ten results over the same list at 6.9. Restricted to four sites it returned nothing; to three, nothing once and eight results once; to two, ten results each of three times, and in two of the three one site took all ten; asked at the tool's advanced depth over the whole list, it returned nothing for one industry and ten results for the other, eight of them from sites the list does not carry. Restricted to one site, each of the four industries drew results from five to nine of the twelve sites, and 63, 34, 47 and 21 of them in that order were on the site with their text and a publish date. So the tool answers a restriction to more than one site with nothing, with one site's pages or with pages from outside it, and the form that returns what each site holds is a search a site. A search a site is not steady either: a quarter of an hour later, capturing the fixture's searches in this form, semiconductors.org returned nothing for Semiconductors, and worldsteel.org returned three pages for Scientific & Technical Instruments from three sites that are not it, which a pass drops. What the form changes is that a search fails a site at a time rather than the whole list at once. The third form the question named, searching only the sites a measurement shows covering an industry, is not taken: the map is twelve searches an industry for every industry the index names before a pass saves one, and it goes stale as the tool's answers move, which the two readings a quarter of an hour apart already show. A pass makes one search for each site on the list, asking each for its first three results, which is the fewest at which each of the four industries had ten pages to hand. It hands the model at most ten of the pages it admitted, every site's first before any site's second, so a site whose pages are about something else does not crowd out the rest. And it carries each page as its first 30,000 characters: that holds 135 of the 172 pages with text the searches returned whole, the median page being 9,785 characters, while a government release of a million and a half characters is carried as its opening, and ten pages at the most come to under 110,000 tokens, inside the shipped model's context and under two cents at its off-peak rate. The page is stored whole, so a citation opens the document rather than the part the model read. A pass costs twelve searches rather than one. A basic search is billed at one credit, so the few hundred theme passes a year the tool was chosen for come to a few thousand searches, which is inside a free allowance of a thousand a month. (see: A theme search is scoped by parameter, not by hope) (see: Theme material comes from a search tool, and per-name material never does)

**A theme page is handed to the model only where its text names the industry** A theme pass's searches return pages from the sites on the industry list that are about the industry and pages that only mention it: the first live pass for Semiconductors handed its call ten pages, and four of them were a national productivity release, the advance report on durable goods, a central bank note on Vietnam's exports and another company's credit rating, each naming semiconductors once to eight times in twenty to forty thousand characters, beside industry pages naming it sixteen to sixty-nine times in every ten thousand. The industry cycle written from them cited all four. So a page is handed only where the industry's words, those of its name less its separators and the words the index uses to qualify an industry rather than name it, appear at least five times in every ten thousand characters of its title and text, the pages kept in the order the pass kept them; over those ten pages that keeps the five about the industry and leaves the five that mention it, the credit rating among them at 3.8. A pass whose pages all fall below it hands the model nothing and writes no cycle, saying the searches returned no page about the industry, which costs a call nothing. The count is a floor and not a judgement: a page that repeats an industry's word without being about it passes, and an industry whose name is a common word keeps more than it should, which costs a call's evidence some focus and never admits a page a site outside the list serves. (see: A theme pass searches each site on the industry list alone, and hands the model a bounded set of the pages they return)

**The model never fetches; components fetch and hand it documents** The research model has no web search of its own, and this is the right shape regardless: every document entering a pass is stored as it is fetched, which is exactly what the claim checker needs to verify a claim's source. A model that searched for itself would produce claims backed by documents nothing kept.

**A research pass reads a name's news from the last three months alone, inside each stored move and since the company's own filing, and hands each section the documents code picks from it** Supersedes **A research pass reads a name's news inside each stored move and since the company's own filing, and hands each section the documents code picks from it, the company's own filing first**, moved below with its reasoning, which stands but for four things the first live research pass found on NVDA on 2026-09-18. What a pass reads. The span of each stored move and the days from the company's own filing to the night, overlapping spans read once, as before, and none of it earlier than three months before the night, by the operator's ruling that day. The year's moves are ranked by change and a name's largest are often most of a year old, and that pass read 10,709 of NVDA's articles, 7,117 of them from the last three months, to hand its sections a few dozen. A move older than three months is read with no document, so its cause is not written. The company's own release is read as before however old it is, since it is the filing the sections rest on. What the cause of a move is handed. Stored moves whose spans share a session are one run of the price measured from several starts: NVDA's eight moves of the year were three runs. Each run is an episode, its cause is asked for once, beginning with the session its largest move ended on, and it is handed at most two documents published inside that largest move: a title naming the company first, then the newest, then the fewest companies named. The newest rather than the earliest, because a move's first session is the one before it moved: the pass handed NVDA's largest move, from 2026-07-29 to 2026-08-05, articles about that first day's selloff, while the ones published on 2026-08-04 and 2026-08-05 named the cause. Each move is listed with which way it went and by how much, as its change fact is written, and the model is told to write nothing for a move no document beside it gives a cause for in that direction, because the newest documents inside a fall can be about the rise before it: over the fixture, KEYS's fall of 13.98% in the five sessions to 2026-08-24 was handed the two newest articles inside it, both about the record results that lifted the shares on 2026-08-19, and the cause written from them gave those results as the cause of the fall. What the sections built across the evidence are handed. The company's own filing and at most six documents published since it, one from each of six equal stretches of the days from the filing to the newest, each the best of its stretch by the same order, and where a stretch has none, the next best of the rest. Picked by the fewest companies and then the newest, all six of NVDA's were from the pass's last two days of a stretch that began 23 days before. Which documents are about the company. The provider attributes a document to every company its text names, so a launch by another company that mentions this one is attributed to this one alone and ranked first on the fewest companies named: NVDA's calendar section was handed Xeal's product launch. A title naming the company, its ticker as a word or the first word of its name with a leading "The" taken off, ranks first in every choice above. The first word rather than the whole name, because a title writes Nvidia and Keysight and never the legal name; a name whose first word is a common one ranks every title using that word with it, which costs order and never a document. Of NVDA's 4,927 articles since its filing, 1,134 name Nvidia or NVDA in the title. The rest stands as the superseded entry states it: code picks the evidence, because choosing the evidence is choosing what a sentence may say (see: Code owns every number); what the company sells and the segment commentary are handed the company's own release; a window the provider has more of than a query reads is refused on the feed's side and recorded as unread; and a judgement of relevance by the model was rejected, which a title rule is not. (see: The model never fetches; components fetch and hand it documents) (see: A cause of a move rests only on a document published inside that move)

**Queued work runs off-peak, and every schedule is written in UTC** Research rates double during two fixed windows that fall late at night in Eastern time, so interactive reading after the close is never billed at peak. Non-interactive passes are scheduled into off-peak on purpose, in UTC, because a schedule written in local time moves into peak when daylight saving changes.

**A pass starts only where the longest pass the store holds would end before a peak window opens** Before it takes a request the drain asks whether a pass started now could still be running when a peak window opens, bounded by the longest pass the store holds that ran to its end, and where it could, it leaves the request outstanding and waits for the window to end. The queue page states the same start from the one function the drain calls, so the two cannot disagree. On the operator's ruling of 2026-09-24, over pausing paid calls inside a window: a wait before the claim keeps the request outstanding, which the queue screen reads as waiting, and nothing recovers a request left being written, since the claim takes only outstanding rows, a withdrawal refuses one being written and the night skips a name holding one, so a pass paused mid-way whose process died would leave its name blocked for good. The longest pass rather than the median, because a pass's paid calls come at its end: the four passes the store held on 2026-09-24 took 3.55, 3.97, 5.43 and 26.28 minutes by their run logs, and NVDA's first spent about 21 of its 26 minutes gathering sources before any paid call, so a 26-minute pass started under a median bound of 5 makes its calls inside the window. The bound is measured rather than guessed and rises if a longer pass ever ends; a store holding no finished pass has no bound, and only a start inside a window moves. What it costs, with the 26-minute bound: on a weekday no pass starts after about 00:34 or 05:34 UTC, so the night's own report starts at once when it is asked about 00:13 UTC, as after the night of 2026-09-23, and when it is asked about 00:40 UTC, as when the overnight queue uses its hour, it waits until 04:00 UTC, midnight in New York before 2026-11-01 and 23:00 after, on nights of Monday to Thursday in New York; a Friday night lands on a Saturday in UTC, which is not a peak day. The bound states its end: a pass longer than it can still make a call answered inside a window, which the spend records at the price charged and the run page counts where it states the priced passes, and two operating rows carry the first such call and a review of the bound at twenty passes (see: Queued work runs off-peak, and every schedule is written in UTC).

## Data

**One year of bars, and no more** Every level in the report comes from a sixty-session window, so a longer history was never needed to produce one. A year covers the 200-day average and gives the level window headroom, and costs about ten megabytes for the whole index.

**Bars are never interpolated** A gap stops computation for that name and is reported as a gap.

**A gap is a session the exchange traded and the store does not hold** Detection is against the exchange's trading calendar rather than against the rows themselves, because a run of stored dates is self-consistent whatever is missing from it and a store holding four days of a five-day week looks exactly like a store holding four days of a four-day week. Two behaviours follow and they are different things: the fetcher refuses to store a series arriving with an interior session missing, and every computation over a name whose stored series has a gap stops and reports the gap's date. The state is derived on read from the stored sessions against the calendar rather than kept in a column, so nothing has to be brought back into step with the bars after a refetch fills the hole. Two readings of the calendar serve the two behaviours and the difference is not incidental. The fetcher compares an arriving series against the union of the session dates the names in hand already hold, which is exact over a whole payload and can say nothing over one series. A computed stage runs per name, so it compares that name's own stored sessions against the closure table pinned in code, which answers over one series and refuses a weekday outside its range rather than guessing. A stage handed a series the table cannot place proceeds and counts the name as unchecked on the run log, because a series nobody compared reported as clean is the shape of a check that passes by having no population.

**A session the night finds missing is fetched in bulk before tonight's** A night that does not run leaves every name short the same session, and the calendar the store can observe is the union of the dates its names hold, so a session every name missed is one it reads as a closure: the hole is invisible exactly when it is widest. So the fetcher compares the newest session the store holds with tonight's against a table of the exchange's closures pinned in code, fetches each session between them in bulk before tonight's, and stores all of them in one transaction. The provider serves an older session's bulk file as prices, which the phase 5 sign-off measured rather than assumed. The cost is one request per missed session, which grows with the nights missed and never with the names, so it is not a per-name call. A missed session whose file cannot be fetched, or carries none of the index, stops the night by name, because a gap stops computation rather than being counted across. The table covers a stated range and refuses a weekday outside it rather than guessing, since a closure read as a session is a day asked for and never answered, and a session read as a closure is a hole nobody fills.

**The stored series is adjusted** One price set per bar and it is the provider's adjusted one, which is why `bar` carries four price columns and no adjusted variant. An unadjusted series never diverges from itself, so storing one would make the corporate action checker pointless and leave the levels drifting away from every chart the operator compares them against. The cost is that stored history is restated by the provider whenever an action lands, which is the thing the nightly check exists to catch (see: Adjusted history is re-fetched after a corporate action).

**Adjusted history is re-fetched after a corporate action** Splits and dividends change adjusted closes, so stored history silently diverges from the provider unless a nightly check catches it.

**A suspect name is asked for again on the five nights after it is marked and weekly after that, and its own page, its row on tonight's list and the run page say so until a refetch succeeds** Ruled by the operator at 7.0, on the item the phase 6 sign-off carried: nothing outside the run page read a suspect name's state, so its name page, its row on tonight's list and its exported report drew its figures with nothing beside them, and a name whose retries were spent stayed so until another action landed on it, which for a split on a name paying no dividend may not come. A name whose refetch fails is marked suspect and asked for again on each of the five nights after the one that marked it, counted on its series state row, and a refetch that fails on a night an action lands on the name starts the count again. The five stand for the reason they were first ruled, that every refetch already carries the retries a transient fault needs inside one night (see: A feed is tried three times with a doubling backoff, and the night has a deadline it cannot move), and the operator confirmed them. Once they are spent, the name is asked for again on the first night whose session is seven or more days after the session it was last asked for, counting on in the same column, until a refetch succeeds or the name leaves the index, which ends the asking because the check asks only for the names the night stores. Weekly rather than never, because the provider is the only source of the series and a failure it has cleared is then cleared without anyone acting; weekly rather than nightly, because a failure that lasts costs one request a week rather than one a night, so the night's cost stays bounded by the failures and never by the index. No second provider is taken, and none is needed for the case this covers. Its figures are still computed over its stored series, and this does not withhold them: what changes is that everywhere a person reads them says so. The name page opens with one line saying its prices may not reflect a recent dividend or split, when the refetch was last tried and why it failed, above everything it draws from those prices, and the exported report carries that line because it is the name page's region. The name's row on tonight's list says the same beside the name, with the instant and the reason as its title. And the run page's stale and failed region keeps naming the name with when it was last asked for and why on every night the check does not ask for it. Nothing stored changes shape: the check reads the instant and the count it already writes, and the read API reads the row as stored.

**A name the backfill stored nothing for is asked for again on the five nights after and weekly after that, and its page and the run page say so until one stores its year** Ruled on 2026-09-18, when the operator was given this and the alternative below and asked for everything to be fixed. The backfill asked for every member holding no bar, on every night, so a name the provider returned no year for was asked for on every night it stayed a member: P_old, which the membership feed lists as the joiner of 2026-09-21 and the price file carries under another ticker, was asked for on 2026-09-17 and would have been asked for on every night after, one per-name request a night on a path whose carve-out allows a new member's backfill once. It is asked for on the schedule the operator ruled for a failed refetch instead (see: A suspect name is asked for again on the five nights after it is marked and weekly after that, and its own page, its row on tonight's list and the run page say so until a refetch succeeds): on each of the five nights after the first that asked, and after those on the first night whose session is seven or more days after the session it was last asked for, until a request stores its year or the name leaves the index. The alternative was strictly once, which leaves a new member with no prices for good after one bad answer. The run log is the record the schedule reads, since each night's asking is what a run log row records: the backfill's row names every name it asked for, and every member still holding no year with how many nights it has been asked for and when it is asked next, and the stage ends partial on every night one is left, which puts it on the run page's stale-and-failed region. The name's page opens with a line saying no year came back, on how many nights it was asked for, and when it is asked next.

**Fundamentals are stored with the filing date they came from** Providers restate. Keeping the filing date is what makes it possible to know later what you knew at the time.

**The fundamentals row carries the analysts' ratings the provider files, on the newest filing alone** The company financials endpoint files, beside a name's quarters, what the analysts following it say of it: the mean of their ratings on a scale of one to five, their mean target price, and how many rate it at each of five grades from a strong buy to a strong sell. SCHEMA's payload column has named ratings since 6.1 and nothing stored them, so the name page had none to state, where the operator's reference report gives the analysts a section of their own. They are copied as the provider files them and sit on the newest filing's row alone, for the reason the ratios do: they move with every note an analyst writes, and the row carries the instant it was fetched at, which dates them. They are not carried into the facts file, so no section quotes a target price or a count of ratings: a target is an analyst's estimate, and a section setting it beside the company's own figures would read the one as the other, which nothing the operator has ruled asks a section to do (see: Code owns every number).

**A quarter ends on the date the company's own filing states** The company financials provider labels every quarter with the last day of the month it ends in, and a company whose fiscal quarters end on a weekday ends them up to a week either side of that: NVIDIA's second quarter of fiscal 2027 ended on 2026-07-26 and Apple's third quarter of fiscal 2026 on 2026-06-27, which the provider labels 2026-07-31 and 2026-06-30. A label is not a date. The row stored the label as the quarter's end, the facts file carried it as the latest quarter end, and the first live research pass, NVDA's on 2026-09-18, wrote that the quarter ended on 2026-07-31, which the claim checker accepted because the file held it, while it refused the company's own date in two other sections because the file did not. The filings archive's index states the period each 10-Q and 10-K covers, so a row's quarter end is the period of the periodic report ending within a week of the provider's label, a week never reaching the next quarter, and the label stands only where the archive was not read or indexes no such report, with the row's source naming whose date it is. (see: Fundamentals are stored with the filing date they came from)

**A quarter's growth is computed on its own row from the filings the provider returned** Each stored filing's row carries how its revenue, net income and earnings per share compare with the same quarter a year before, and how its revenue compares with the quarter before it, each as a fraction of the earlier figure. The first live research pass found the want of it: NVDA's two cases were refused for revenue up 106% on a year earlier, and the facts file held the quarter's revenue and not the year-earlier one or the change. It is computed by the fundamentals fetcher, where the margin already is, because a change between two filings is a property of the filing and the provider returns every earlier quarter in the one request the fetch already makes; the facts assembler's rule is that nothing in it is derived, and a screen computes nothing (see: A screen reads and renders, and computes nothing). The earlier quarters are the ones the provider returned ending within a week of a year and of three months before this one's end, which is the tolerance a quarter's own end is matched with (see: A quarter ends on the date the company's own filing states), so a fiscal calendar ending its quarters on a weekday finds its own. A quarter with no such filing returned, or an earlier figure of zero or less, carries no growth rather than a guessed one, and the year-earlier and quarter-before ends are stored beside the figures so a reader can see what each change was measured against. Rows are never updated, so rows stored before this carry none.

**A group's growth in a filing's own tables is computed from the columns the table states** Each group of the segment table and of the filing's other tables of revenue carries its change on the same months a year before, computed by the fundamentals fetcher from the two columns the table itself states, on the newest row beside the tables. The first live research pass wanted it twice: NVDA's two cases were refused for data center revenue up 117%, and a segment's growth is what a reader of the segment table asks next. The table's own columns rather than the filings the provider returned, because the provider files no figure by segment and the table states the year-earlier column beside the quarter's, restated where the filer restated it. The newest period is the newest quarter the table files, or its newest period where it files none, against the period of the same months ending within a week of a year before it; a group is named by its position as well as its label, because two groups of one table can share a label. A figure whose year-earlier value is zero or below grows by nothing, which leaves every cost the tables parenthesise without a growth rather than with a sign a reader would have to undo (see: A quarter's growth is computed on its own row from the filings the provider returned).

**Twelve filings are stored and five are shown** Section 4's numbers table displays five reported quarters and one guided quarter, and that is a display decision rather than a statement about what the store holds. The fetcher stores twelve filings, because the endpoint returns years of quarters in one call, so twelve is the same request at the same weight as five and the difference is only what is written. Twelve rather than five, stated so a later session reading the table does not narrow the store to it: a year-over-year reading costs five quarters before it produces one value, so five stored quarters yields a single comparison, where twelve yields eight, which is enough for a direction to be a direction, and twelve makes a guide-against-actual record a count worth having rather than four observations. Not more, because a company's business changes over five years more than its numbers do, so the earliest quarters describe a different company and widen every range they sit in, which makes today's multiple read as mid-range when it is not. Two details carry the rest of it. The window is twelve stored filings rather than a date range, so a company that missed a filing does not silently get a shorter window than one that did not. And where a later reading is computed over fewer than twelve, it carries its count, the way an indicator row carries its bar count: a reading over four quarters and a reading over twelve are different readings and a figure that does not say which invites the wrong one. What this buys is not built here (owes: The computed fundamental panel, and whether a fundamental state may fire a reason or gate a tranche).

**A calendar event is fetched once for the whole index, and the calendar holds provider events only** Four components declare a `calendar` read, `SCHEMA.md` declared no such table and nothing wrote one, which is contradiction E. The table is written by one component on the nightly path, fetching the whole index's dated events in one request over the horizon rather than one request per name, because the earnings date is needed nightly by the ladder builder and the shortlist builder and a per-name call there would break the rule the whole design rests on. It holds what the provider files and nothing else. A `kind` column carries provider event kinds only and a researched event never acquires one: the report's Dates section draws on the provider calendar and on dated items from the research record, and those two meet at the report surface, which is where section 4 already has them meeting. A researched date written into this table would be a claim with a source that the one instrument built to verify sources cannot see, and it would give a nightly store a second writer against the rule that a store has one writer per operation. The `kind` restriction is stated rather than implied because the first person wanting a product launch in the Dates section would otherwise add a kind for it, and the second origin would arrive without a decision.

**The calendar holds each member's own listing's prints, and each night's answer replaces what its window held** The earnings endpoint answers for every market in one payload, and a ticker the index holds can be listed on another exchange as another company or as the member's own shares, each reporting on dates of its own. The fetcher read every row by its ticker without the exchange, so over a copy of the operator's store on the night of 2026-09-23, 180 of the 503 members held two or more prints for one fiscal period: Quest Diagnostics carried four prints of another company's, with estimates below zero on dates between its own, 432 of the 2,428 earnings reaction rows were other listings' prints, and 15 of the night's 97 earnings soon fires read another listing's date or one the provider had moved, three of them a print dated the night itself. And the fetcher only inserted and updated while the date is part of the key, so a date the provider moved left its old one inside the window. The calendar now reads the index's own exchange alone, and each night's answer replaces what its window held: a row inside the window the answer no longer carries is removed in the same write, a row the window does not reach is left for a window that does, and an answer that stores no member's print removes nothing, since a provider answering nothing for the index is not one saying nothing is scheduled. Judgement: the member's own listing is the only one whose prints are the member's, and no field in the payload tells a foreign listing of the same company from another company, so none is read. The cost is a member whose next print only another exchange's listing carried: over the same night 22 members lost an upcoming date that way and read not on file until the provider files their own, one of them inside the twenty-session horizon, and that one was dated the night itself. Rows written before the correction are kept as written, and nothing in a row tells one read over the old calendar from one read over the new but its session, so earnings soon on a session before 2026-09-24, the first whose night runs the correction, counts toward no share on the run page (see: A calendar event is fetched once for the whole index, and the calendar holds provider events only).

**Every computed table's writer is its own deleter** Section 16 states one year, recomputed nightly and kept for the harness, over indicators, swings, volume profile, levels, ladders and moves, and `SCHEMA.md` gave Delete to nobody for any of them, which is contradiction A and contradiction H a third time in a third table. The four that exist split on grain and the split is what makes this due now: `indicator` keys on ticker, session and name and `swing` on ticker, session and direction, so both replace with the series and grow only as it does, while `volume_profile` keys on ticker, as-of date and band and `level` on ticker, as-of date and low edge, so both write a new set every night and replace nothing. At the bands a name this fixture averages, five hundred names put something of the order of three and a half million rows a year into a store nothing was declared able to reduce. `ladder` is the fifth and keys on as-of date, so it grows the same way, which is why the ruling lands with the checkpoint that adds it rather than at 5.2 where the last of the six arrives. Each builder drops the rows that fall out of the window on the night they fall out, which is what `BarFetcher` and `NewsPulseCounter` already do for the two tables that carry a stated retention and an owner to enforce it. A retention window nobody owns is a table that grows forever while the file says it does not.

**Your own listing history is kept forever** Which names reached which levels on which nights, and what happened next, is a record no provider sells and the only evidence that will ever settle whether a reason is worth keeping.

**News is one dated query, paged to cover the day, and attributed to names locally** The feed is queryable by date with no ticker, which was probed on the operator's key at 1.5 rather than assumed: one request over a date range returns articles for the whole market, every row carries a `symbols` array naming the tickers it is about, and the response shape is identical whether or not a ticker was asked for. So a night makes one dated query and fans the rows out to names in code, and the per-name cost of news is zero calls rather than one call per name. The alternative, a request per ticker, would put five hundred calls on the nightly path for the same articles and would break the rule that a night costs the same for fifty names as for five hundred. The row also carries the article text, so the source document a claim rests on arrives with the row rather than needing a second fetch, which is what makes admissibility judgeable at all without spending again.

One request does not carry a day, and the pages are counted. 2.5 measured it: one dated request for a single session returned exactly 1,000 articles, which is the provider's cap, over 3,232 distinct symbols. A count taken from a capped request is a count over whatever the cap happened to include, which is a figure over a mixed population and is not stated at all. So the query is paged until the day is covered, every page is counted, and a day that reaches a stated maximum page count refuses and says so rather than reporting a truncated count as a whole one.

The page count is a function of the day's news volume and not of the universe, and that is what keeps the cost rule true. One request already attributed 3,232 distinct symbols against an index of 503, so the feed is market-wide and adding names to the universe adds no articles to it. A feed that pages counts every page, and a page count that grows with the index is a per-name call; this one does not grow with the index, and `nightly-cost` measures it over two universe sizes to show that rather than to assert it. What the measurement does falsify is the arithmetic written before it: section 14's step counting today's articles said one request, and section 17's weighted-call row priced news at one request weighing 5. Both now read per page, with the driver named, and ten pages at weight 5 is 50 against an allowance of 100,000.

**A stored price is chosen and ordered by its value and never by the text it is stored as** A price is a decimal in code and text in storage, so the store compares two of them character by character: 95.10 stands above 106.47 because 9 is above 1, and a band at 87 sorts below one at 117 and above one at 1,140. Every query that ordered by a price column, or took the greatest or the least of one, was therefore answering by how a number is spelled. Found on 2026-09-19 by reading a name's page, which stated a move whose high was below its own low: over the index that night the two figures were wrong for 35 of the 508 names carrying a move, 24 of them showing a high under the low, and the bands of 48 of 505 names were listed out of price order. So a query hands back the rows and the choice is made where the values are decimals, which is selection of the kind the read surface already permits and never a computation over what is stored. The same holds for adding them: an aggregate that sums a price column is the store reading text as a floating point number, which is the storage form this corpus refuses in the other direction. `price-storage-form` reads every query in the shipped source and fails one that orders by, aggregates or compares a column SCHEMA marks as money, so the rule is enforced where the queries are written rather than remembered where they are read. The cost is that a query carrying its own order now hands that ordering to its caller, which is the price of the order being a price rather than a string.

**Each print's reaction is read from the nightly calendar and the stored bars, and reaches no reason, gate or plan** The calendar is fetched for the whole index in one request (see: A calendar event is fetched once for the whole index, and the calendar holds provider events only), which makes it the bulk source the fundamentals ruling says would be a new decision, so this is recorded as a new decision and not a reopening of that ruling (see: No reading of the fundamentals fires a reason, gates a tranche or draws a panel). A print with no filed estimate shows its actual and says no estimate was filed, and is never counted as a meet. A print's reaction session is the one the earnings rule takes, read by calling the rule's own function one print at a time, so a print whose timing the provider left unstated is handled exactly as the earnings rule handles it and no pinned source is edited to share the reading. The earnings rule itself keeps its two prints. The operator ruled it on 2026-09-23, one of two additions an SNPS report asked for.

**The numbers section draws the dividend the provider files from the newest filing alone, and nothing for a company paying none** The forward annual rate, the forward yield as the provider states it, the payout ratio, the ex-dividend date and the pay date, all from the provider's SplitsDividends part, as the analysts' ratings are read (see: The fundamentals row carries the analysts' ratings the provider files, on the newest filing alone). No yield is computed at tonight's close. A company the provider files no dividend for draws no dividend part at all, on the operator's ruling of 2026-09-24, since a line saying so told a reader nothing the missing figures do not. A row fetched before the part existed still says the part is absent and why, because that absence is the store's and not the company's, and a payer drawn as though it paid none would be a wrong result. It is presentation in an existing table, which the fundamentals ruling leaves standing.

Supersedes **The numbers section shows the dividend the provider files, on the newest filing alone**, moved below with its reasoning, all of which stands but its line for a company paying none.

## Feeds and the wire

**A feed is unavailable when it does not answer, and wrong when it answers with something else** Section 18 promised a behaviour for a feed that is unavailable and never said what unavailable is, which left four different events sharing one row and no test able to induce it. Unavailable is a refused connection, a request that passes its timeout, a rejected request rate, and a status the provider uses for its own faults. Every one of those is a night that did not happen, and the answer is the one section 18 already gives: keep last night's bars, mark every name stale, still serve the app, and say so on the banner with the data date. Wrong is an answer that arrives and parses and is not the answer asked for. That is a night that would look like it happened, and the answer is the opposite: refuse the payload, leave the stored series exactly as it was, and name what was wrong on the run log. The two are kept apart because folding them would give a payload carrying yesterday's session the "keeps last night's bars" behaviour, which is right, and lose the part that matters, which is that something arrived and was rejected. One is a fact about the network and the other is a fact about the provider, and only the second is worth a person's attention in the morning.

**A feed is tried three times with a doubling backoff, and the night has a deadline it cannot move** Every feed interface accepts a cancellation token and nothing supplied one, so a night that hung on a socket hung until somebody looked. At most three attempts per request, the first two failures waited out for two seconds and then four, and each attempt bounded by a thirty-second timeout. Thirty seconds because the bulk file for a whole exchange is the largest thing the night fetches and is a few megabytes: far outside a healthy fetch and far inside the night. Three attempts because the failure a retry is for is a transient one, and a fourth attempt is a slower way of learning what the third said. What is retried is a refused connection, a timeout, a rejected rate and the provider's own fault statuses; what is not retried is a rejected key or a route the provider does not have, because a wrong key is wrong three times and the retry only delays the message that says so. The night as a whole carries a deadline of fifteen minutes, which is three times the wall clock the limits table states for five hundred names, so the deadline stops a night that has hung rather than one that is merely slow. At the deadline the night stops, names the step it was on, and exits non-zero.

**The night runs at a fixed UTC instant, moved only when a night finds the day's file not yet posted** Section 14 scheduled the night with Task Scheduler after the US close, which named one platform's mechanism and a local time, against two hard rules at once. The schedule is a UTC instant, registered with whatever scheduler the machine has, and the application neither knows nor asks which. The instant is the one `RUNBOOK.md` states, after the close at both ends of the year with a margin for the provider to post the day's bulk file, and it stands while nights succeed. It is not settled by measuring the posting hour, because a night that finds the file already posted says only that it was there by then: five such nights bound the hour and settle nothing, and the obligation that waited for them had a trigger whose firing could not answer its own question. What moves the instant later is a night refused because the day's file carried none of the index at the scheduled instant, which is the provider not having posted yet. That refusal is on the run page's stale-and-failed region the next morning, and the session is not lost, because the next night fetches it as a missed session. The instant is a setting in the runbook with its reason beside it and not a figure in this entry, because a number written into a decision is the shape that gets cited later as though it were settled.

Supersedes **The night runs at a fixed UTC instant set after the provider posts the day's bulk file**, moved below with its reasoning. The line was missing when this entry was written, which the phase 5 sign-off reviewer found: the old entry was moved and nothing here said which it was.

**A night on a day the exchange did not trade fetches nothing and exits clean** The exchange trades on weekdays less its closures, so a night scheduled every day falls on a day with no session every weekend and every holiday. Until the phase 5 sign-off such a night asked the provider for the day's file, was refused at the fetch because the file was dated the session before or held none of the index, and exited 1: the scheduler recorded a failure every weekend, and a missed Friday caught up by a Saturday night was rolled back with the Saturday's refusal. The night now asks the closure table whether its session was traded before it fetches anything, and on a day that was not, it writes one run log row naming the day, fetches nothing, and exits 0. The row carries an outcome of its own rather than `ok`, which under the closing stage would say the night's counts were recorded, and rather than a failure, because nothing is owed on such a day. Registering the task on weekdays only saves the weekend's rows and is the runbook's business; the night does not rely on it, because a holiday falls on a weekday. A weekday the table cannot place is still refused, since the table exists to refuse rather than guess.

**A feed that pages counts every page, and a page count that grows with the index is a per-name call** Every feed interface counts its own requests and none said what happens when a payload arrives paged. The count includes every page, so a feed that starts paging reports the truth and the limits row stops reading as one. The limit itself is restated to say what it always meant: the count does not grow with the number of names. A payload that pages because it is large is bounded by the payload; a feed whose page count rises as the index does is a per-name call wearing a different name, and it is a defect under the same rule rather than a new exception to it.

**The night's cost is counted in weighted calls against the stated daily allowance** `RUNBOOK.md` states the provider's allowance as 100,000 weighted calls a day and gives the weight of each endpoint, and no code read either figure, so nothing stopped a night spending a month's allowance and nothing would have said it had. Each feed declares the weight of the endpoint it calls, the run log records the night's weighted total beside its request count, and a stage that would take the running total past the allowance does not start. It is denominated in calls rather than in money because this provider sells a flat subscription with a call allowance, which is a different shape from the research provider whose cap is money and whose rate doubles at peak. The local stop exists because the provider's own stop is a rejected rate, which arrives as an unavailable feed and loses the reason.

**The outward-request scan names the files that may hold a client rather than dropping the patterns** `nightly-cost` scans every shipped file for `System.Net.Http`, `HttpClient`, `WebClient`, `HttpRequestMessage` and the socket constructor and reports zero, which is true only while no feed reaches the network. The first live feed turns a green check red, and there were two shapes available. Deleting the patterns leaves a check that reports the absence of a scan as the absence of a client, which is the under-reporting failure `coverage-reported` exists to catch, arriving inside the check that carries the nightly path's own claim. So the exemption is named instead: one file per live feed, listed in the check, with every other shipped file still scanned and the scanned count floored where it is rather than lowered. The list is asserted to hold exactly the feed implementations, so a file added to it that is not a feed fails rather than passes quietly.

**The on-demand feeds are resolved in one place, as the nightly feeds are** The night's six feeds are one record resolved once from configuration, live against a capture, refusing a fixture folder that does not exist rather than resolving to the live path and refusing to fall back to a capture on a live run. The research work reaches five more surfaces, being the fundamentals endpoint, the filings archive, the search tool and the two model lanes, and not one of them is nightly, so none belongs on that record. They get a record of their own, resolved the same way and for the same reason: what a pass ran against is a fact the run log has to carry, and a client constructed inline is a choice nobody can read afterwards, which is where the night's own source selection lived until 2.6 and is why there was no selection. The overnight queue is the one research surface a night reaches, and it takes the local lane through this record rather than holding a client of its own, so a night's model calls are attributable to the same resolution every other call it makes is.

## Levels and the plan

**Levels come from four sources: swings, moving averages, retracements of the last two swings, and heavy volume shelves** The first three are prices the chart has visited or arithmetic on them. The fourth is where the shares actually changed hands, which is the only one of the four that says how many people have a reason to act at a price. A support band holding a large share of the period's volume is defended by holders at their cost; a range above the price where almost nothing traded is one price moves through quickly. A level builder without a volume profile can state neither.

**A heavy volume shelf creates a band of its own and also strengthens one it coincides with** Section 9.1's first step collects every price band holding an unusually large share of the period's volume as a candidate, and a candidate is what a band is merged from, so a shelf with no swing, average or retracement anywhere near it is a band in its own right. Its last step adds a point where a band holds a shelf, which is the ranking half and is a different question from whether it creates. Both readings were available and they produce different band sets, so the level builder is a different component depending which is taken. Creating is the one the four-sources decision already names: a source of levels that could only rank would be three sources and a modifier. It is also the reading the volume argument requires, because the range where almost nothing traded is a statement the report can only make about a band that exists on volume alone. The cost is that a name whose volume is evenly spread produces no shelf band and loses nothing, while a name with one dominant shelf gains a band no other source would have found, which is the case the whole profile was built for.

**The volume profile accumulates over the same sixty sessions as the level window** One window and not two. The shelf is a candidate the level builder merges alongside swings and retracements, and those come from the last sixty sessions, so a shelf drawn from a different span would place a band on volume from sessions no other member of the level table can see. The profile's own share figures would then be shares of a period that is not the period the rest of the chart is drawn from, and two numbers on one page would silently mean different things. The moving averages remain the stated exception, reading the full stored year, and they are exempt because an average is a price rather than a share of a period and the limits row already says so. Written as a decision rather than left to the builder because `SCHEMA.md` gives the profile an as-of date and price bands and never says over how many sessions volume is accumulated, so nothing would have caught the two windows drifting apart.

**The volume profile is twenty bands across the window's own range** A fixed count of bands spanning the window's high to its low, rather than a fixed price width. The count has to be the same for every name because the shelf threshold is one number for all five hundred: "at least twice its even share of the period's volume" means a fifth with five bands and a fortieth with forty, so a threshold stated once and read against band counts that differ per name is a threshold that means something different for each of them. A fixed width in money has the same defect from the other end, putting three bands on a forty dollar name and three hundred on a four thousand dollar one. Twenty is the number section 15.5 already describes when it says a support band can hold a fifth of the period's volume in a twentieth of its price range, so the figure is read out of the document rather than invented beside it, and it is also what a histogram of twenty rows can be read by eye where two hundred cannot. The band edges are rounded to the four places the stored prices carry and every band takes two adjacent members of one array of twenty-one edges, so the bands tile by construction rather than by two roundings agreeing. Written as a decision because `SCHEMA.md` gives the profile a band low and a band high and never says how many bands there are, and section 9.1 settled the window without settling this, so the shelf threshold rested on a number no document held.

**A name with fewer than sixty sessions gets no volume profile** Not a profile over what it has. `share_of_period` is a fraction of the window's total volume and the column carries no denominator, so a row computed over twenty sessions sits in the same table as one computed over sixty and reads identically. That is a figure over a mixed population, which `.claude/rules/writing-tests.md` already forbids stating at all, and it would reach the level builder as a shelf whose share is a share of a different period. The absence is visible instead: the name has no rows and the run log's detail carries the count of names skipped, so a night where a backfill failed shows as a jump in that count rather than as a set of quietly shorter periods.

**A retracement is drawn between the last swing high and the last swing low** The window's most recent high and its most recent low, in whichever order they occurred, with the five fixed fractions taken across the distance between them. The phrase in the architecture, "the retracements of the last two swings", reads equally as the last two swings of any kind, and that reading produces a retracement between two highs or between two lows, which is a fraction of the distance between two prices that no one retraced. A retracement is a partial reversal of a move, so it needs the two ends of a move, which is one high and one low. Where the window holds no swing of one of the two kinds, the name has no retracements that night rather than retracements drawn from a substitute, because a substitute end is an invented move.

**A volume shelf enters the merge as one price, the midpoint of its band** Section 9.1's first step collects "every price band holding an unusually large share of the period's volume" as a candidate, and its key says a candidate is a single price with a reason to matter. Those two sentences describe different things and `SCHEMA.md` settles which: a member carries one kind, one price and one date, so a shelf reaches the merge as one price like every other candidate. The midpoint rather than an edge, because neither edge is where the volume is and choosing one would put the band systematically low or systematically high. The alternative, contributing both edges, is not academic: it makes the resulting band exactly the shelf's own width whenever nothing else is near, and a shelf's width is the window's range divided by twenty, which is an artefact of how the profile is cut rather than anything the price did. Measured on the fixture, two of the three names have a shelf narrower than half a typical day's move, where the two readings agree, and the third has one twice that, where they do not. The shelf's identity is not lost by this: the member record says it is a shelf, the strength step reads the coincidence, and the profile itself still carries the band's edges and its share.

**A member's date is the session its evidence occurred on, and a figure recomputed nightly has none of its own** A swing carries its own session, a touch carries the session that reached the band, and a retracement carries the later of the two swings it is drawn between, that being when the move it retraces ended. A moving average and a volume shelf are figures over the window rather than events, so they carry the as-of date because `SCHEMA.md` requires every member to have one. The consequence is the whole reason this is written down: section 9.1's strength score adds a point for "a member from the last twenty sessions", and an average dated as-of is always from the last twenty sessions, so awarding the point on date alone would hand it to every band holding an average, which is every band near the price on any night. A point every band scores ranks nothing. The point is therefore awarded on the three kinds whose evidence occurred, and the two computed nightly cannot win it.

**Touches strengthen a band and never create one** A session that reached a band is evidence for it, which is how the report cites dated lows that are not swings in their own right.

**A tranche is a support band, and its stop is a daily close below the low edge of the next band beneath it** Close rather than touch, because a name moving six percent on a normal day dips through levels intraday every week, so a stop on a touch is taken out by noise and a stop on a close is taken out by a decision. The next band down rather than just below the tranche, because anything closer than a typical day's move is inside the noise.

**A moving average is a level on the chart and never an anchor for a tranche** A short average follows the price by construction, so a band anchored only on one sits near the price regardless of what the price has done. Measured on the worked example's own bars, the close was within half a typical day's move of its 20-day average on seventeen of thirty-one sessions, so a tranche placed there would make the first condition fire about half the time on nothing having happened. Averages stay on the chart because a reader wants to see them; they simply do not decide where to buy.

**A support band whose low edge is below the price carries a tranche, even when the band contains the price** A band the price is already trading in is exactly where the first step of a staged purchase belongs, so eligibility turns on the low edge rather than the whole band. In the worked example this is the band from 900 to 940 with the price at 933 inside it. The band keeps its full width, because a fill can land anywhere in it.

**The trailing stop is the higher of the band beneath and the last swing low** In a range the stop is the range floor, which is the low edge of the next support band beneath the tranche; in an uptrend it trails, and what it trails to is the higher of that band and the most recent swing low beneath the tranche; in a downtrend there are no tranches. Kaminski and Lo, Journal of Financial Markets, 2014, show a stop reduces expected return under a random walk and can add value where returns trend, so one stop rule for both cases is wrong in one of them. Taking the higher of the two rather than the swing low alone is what keeps the trailing half meaningful. Read literally, trailing the last higher low puts the stop wherever that low happens to be, which on a name that has run a long way is far below the band beneath and is looser protection than the range rule gives, and a trailing stop that can sit below the range floor is not trailing anything. The committed fixture holds the case: MSFT's third tranche has its last swing low a hundred points under the band, and the literal reading would put the stop there.

Supersedes **The stop rule depends on the trend state**, moved below with its reasoning. The code has run this rule since 4.5 and no spec said it: figure 10.1 read that the stop trails the last higher low, the superseded decision said the same, and the phrase "higher of" appeared only in two records, a source comment and an expectation note. A decision is changed only by another decision, so until this one the rule the code ran was a rule the corpus did not hold.

**An exit closer than two typical days' moves is listed but not traded** A move smaller than two ordinary days is inside the noise, so the level is worth naming on the chart and not worth paying a spread to act on.

**The plan places a position and never sizes one** The sizing paragraph is worked arithmetic from a risk budget the reader chooses.

**The second book is keyed to a dated event, and an earnings print is the only kind on file** It has its own setups, its own stops and a horizon of days, and it never merges with the position book, because holding a momentum entry through a print is the thing the separation exists to prevent. What is new is what the book is keyed to. Section 10 said the second book is keyed to dated events, of which an earnings print is one, and left where the other kinds come from to be settled here. They come from the calendar and from nowhere else, so the kinds the book can carry are the kinds the provider files, which today is the print. A dated item a research pass found is a claim with a source and lives in the research store, and it reaches the report's Dates section without ever reaching this book. Supersedes **The earnings trade is a second book**, whose name said earnings where its own body already said the separation is about carrying a momentum entry through a date.

**A ladder row is written for every index member every night** Carrying the trend state, and a plan that states why it is empty where it is empty. Section 18 says a name with no eligible band produces no ladder and the report says why, and a row that is absent says nothing: an empty plan stating its reason and an absent plan are different objects and only the first is readable on a screen. The same shape as the listings row written for every name whether or not a reason fired, and for the same reason. Tonight's label is compared against last night's by the trend-changed reason, so a name whose band went ineligible has no yesterday unless a row was written on the night it had none, and the transition into and out of a downtrend is exactly the one that changes the whole plan. The cost is one row per name per night, which is the order the listings table already accepted.

**The trend state is read from the averages and the last two swings, and a name that cannot be classified says so** Uptrend when the close is above the 50-day average, the 50 is above the 200, and the most recent swing low is above the one before it. Downtrend is the mirror, close below the 50, the 50 below the 200, and the most recent swing high below the one before it. Range when every input is present and neither holds. Not classified, with the reason recorded, when an input is absent: fewer than 200 bars, so there is no long average, or fewer than two swings of the kind the rule reads. The fourth value exists rather than a default because the corpus already refuses this trade elsewhere: an average of sixty bars labelled 200-day is a lie, and a name labelled range because nothing could be measured is the same lie one level up. A name that is not classified carries no tranches, so the label decides a plan and cannot be a guess. The swings are read as of the date through the same reader the level builder uses, so nothing sees a swing confirmed later.

**The trend classifier returns its label to the ladder builder** The catalogue said it returns the label to the facts assembler, which is built at 5.3, a phase after the screen that draws the label. It writes nothing, `LadderBuilder` stays the single writer `SCHEMA.md` already declares for `ladder`, and the facts assembler reads the label off that row when it arrives. A classifier that wrote would give one label two homes the moment a ladder row also carried it, which is the second statement of one fact this corpus has spent three phases removing.

**A tranche's condition is the pattern the price has made at its own band** Figure 10.1 names a fixed list of four and says nothing about which one a given tranche gets. Each is decided by the band's own recent history, tested in a stated order so that exactly one matches: available now, where the close is inside the band; a failed breakdown back into the zone, where a session inside the lookback closed below the low edge and the close is back at or above it; the first close back above the zone after a dip, where the close is above the high edge and a session inside the lookback traded into the band; and the second day after a shock once the first day's low has held, where a session inside the lookback moved more than the shock multiple and the band is the one it landed on. Exactly one, asserted in both directions rather than left to the order a dictionary happens to yield, because a matcher keyed on the first thing that fits answers about everything that fits. A tranche whose band the price has made none of the four at reaches the zone, which is the absence of the four rather than a fifth pattern: a band the price has not come back to still needs a label, and the alternatives are an empty condition or a tranche the plan does not state. The lookback of 10 sessions and the shock multiple of 3 typical daily moves are proposals under the same rule as the setups below.

**Each traded exit sells an equal fraction of what is held, and the top of the ladder trails** An exit is a fraction of a position that already exists, which is scaling out rather than sizing, so it needs no capital figure and does not touch the rule that the plan places a position and never sizes one. Equal fractions over the traded exits, because any other split is a preference dressed as arithmetic, and the skipped exit takes none. The top of the ladder is a trailing rule rather than a price, which is what figure 10.1 already says and is why the fractions divide over the traded exits rather than over five.

**No reading of the fundamentals fires a reason, gates a tranche or draws a panel** The four readings 6.1 asked about, being a computed trajectory, a guide record, earnings quality and a valuation position, are not computed, and no fundamental state reaches a listing or a plan. The reason is the path rather than the merit of the readings: fundamentals are fetched when a name is opened and by nothing the night runs, so a reason or a gate reading them would either read a store whose population is the names the operator happened to open, which puts two names in the same state on different rows and leaves a shadow candidate unevaluated on most name-nights, or fetch them for every member nightly, which is the per-name request the nightly rule forbids at ten weighted calls each. What is given up is stated: no fundamental state can inform tonight's list or tonight's plan, and the numbers table goes on showing the reported quarters for a reader to weigh. A bulk source covering the index in one request would be a new decision and not a reopening of this one. Ruled at 8.0.

**The volume profile claims a price region at the resolution of a day's range** What the profile says is where volume sat as a price region no finer than a day's range, never a price within a session, because each day's volume is spread across that day's high and low and the store holds no finer grain. A published caution holds that a daily volume profile is not a volume profile, and it is about a feature scored against a forward return; narrowing the claim is what puts this outside it. A session whose range exceeds three of its previous session's typical moves is kept in the profile and counted rather than set aside, since setting one aside would move bands and the plans built on them, and the count is what a reader is owed instead: over the 503 names that are members on 2026-09-14 and hold a profile as of it, 85 of 30,168 sessions exceed three typical moves, 532 exceed two and 24 exceed four, with 81 names holding one such session, 2 holding two and 420 none. The multiple is the event book's shock multiple, so the two move together. Ruled at 8.0.

**A moving average may widen a band that a tranche sits on, and may never anchor one** A band is eligible for a tranche only where one of its members is something other than a moving average, which `LadderSeries` enforces and `ABandAnchoredOnAnAverageAloneCarriesNoTrancheAndIsStillAStop` holds over a constructed band set. An average inside an eligible band still sets an edge where it is the outermost member, because a band keeps its full width and a fill can land anywhere in it. The two sentences are not in conflict once said together: what the earlier decision refuses is a band that sits at the price because an average follows the price, and a band carrying a swing, a retracement or a shelf does not. The size of the question was measured before it was answered, over the 223 zones that fired on 2026-09-15: an average widens 35 of them, and 3 would no longer hold the close if a zone were the range of its non-average anchors alone. Taking those edges away is a ladder rule change rather than a reading, so it is registered at 8.6 as the named version `zone edges from non-average anchors only` and scored against this one. Ruled at 8.0.

**A researched precondition attaches to a tranche and never creates one** A tranche can reasonably wait on something no compute component can see, a guide that does not imply a revenue decline being the case that started this. The ladder emits the tranche with its price condition, and a research pass may attach a fundamental precondition carrying its own source, which the claim checker treats like any other claim. The alternative, a fifth condition kind the ladder builder evaluates, would need the ladder to read the research store, which its matrix row does not carry and should not: a nightly component that reads research is a nightly component whose output changes when a paid pass runs. Nothing about a tranche's existence, its price, its stop or its order depends on the precondition; it is a sentence attached to a tranche that already exists.

**The event setups' triggers are proposals until resolved setups can score them** Three setups keyed to the print, each with a trigger, an entry, a stop and a target, and every figure in them is a proposal stated as one on the page, the way section 11 states the six reason thresholds. Twelve numbers invented at a planning pass and written as rules would be twelve constants nobody could later tell from measured ones, and this corpus has no basis to correct them until something has scored a setup. A breakout before the print triggers on a daily close above the immediate resistance band's high edge on volume above the fifty-day average, with the print inside the horizon and at least three sessions away; it enters at that close, stops on a daily close back below that band's low edge, and targets the next resistance band's low edge. A flush after the print triggers where the session after the print closes down by more than two typical daily moves and inside a support band; it enters on the following close once that session's low has held, stops on a daily close below the flush session's low, and targets the flush session's own open. A gap up after the print triggers where the session after the print opens above the immediate resistance band's high edge and closes above its own open; it enters at that close, stops on a daily close back inside the band, and targets the next resistance band's low edge. The book's horizon is 10 sessions from the trigger, and a setup unresolved at that cap is unresolved rather than anything else (owes: The event setups' triggers calibrated from resolved setups).

## How the picks improve

**A setup is scored from its entry, and a target reached before the entry is never a win** A setup starts on the first close at or below its entry zone's top edge, and a target reached before that close is `never entered`: its own column, never a win and never in a rate. The filler scored from the listing night from 5.5 until 8.1, so a name that ran straight up from the listing counted as a win for a purchase nobody could have made at the price the plan named, which is 105 of 147 stored wins over the operator's store on 2026-09-16. A first close that is at or below the zone's top edge and through the stop is entered and stopped on that session, because a fall through the whole plan is a trade that happened and lost. The cap of 63 sessions is counted from the listing night rather than from the entry, since the plan being scored is the one stored that night and it ages with it. Entry is read on closes, as the stop and the target already are, because a daily bar does not say what order a session's prices came in and reading a fill from a session's low would credit an order the store cannot show was filled. Ruled at 8.0, built at 8.1.

**An outcome once decided is never rewritten, and a setup still in play is scored with its plan scaled by its listing session's adjustment factor** A setup's outcome, being a win, a loss, never entered or unresolved at the cap, and a five or twenty-one session outcome with its return once that horizon has matured, are written once and never again. From 5.5 the filler wrote every row of every listing on every night, and two things moved a row that had already been decided. The corporate action refetch restates every stored close before an ex-date while the plan stored on the listing night keeps the scale it was written at, so a two-for-one split scored every setup listed before it as a loss against a stop at twice the price, and a dividend moved a close across a stop, a target or the entry zone's top edge by the share it paid; the actions feed carries dividends as well as splits, and 56 names in the operator's store on 2026-09-15 held a year the refetch had restated. And the bar store keeps one year, so once a listing's own session left it the close its returns and its entry are measured from was gone, and the next fill wrote its matured horizons back to not yet matured and scored its setup over a series starting later. A setup still in play is scored against its stop, its first traded target and its entry zone's top edge, each multiplied by the listing session's stored close over its raw close. That factor is one on the night a listing is written, because nothing has restated the newest session a night stores, so on a later night it is the restatement since, and it puts the plan on the scale the stored series now has, ex-date sessions included. A listing whose close the store no longer holds has nothing to measure from and stays as it stands. The base rate beside a decided row is the one figure the filler writes over it, because it is the universe's figure over every name-night and moves as the window fills. A later change to how a setup is scored applies to the setups decided after it and never to the record already written, as a changed rule opens a new window and keeps the old one. The entry rule's rewrite of every stored outcome was ruled to run first, on the night of 2026-09-16. Ruled by the operator on 2026-09-16, built at the 5.5 correction.

**The candidate family is at most eight and the threshold is divided by it** The maximum family size is a convention and no count of resolved setups measures it, so it is decided rather than carried: with five worthless candidates the chance that one clears an ordinary test is about 23 per cent and with twenty it is about 64, and eight is the size at which the correction stays a correction rather than a bar no candidate could clear. The six live reasons are a family of their own, registered by section 11 before the first listing night, and registered candidates are the other; a candidate promoted to live joins the live family, which restarts the window the live reasons are measured over. Ruled at 8.0.

**A verdict tests a reason's wins against each of its setups' own break-even at a corrected threshold** The null hypothesis is that each resolved setup wins with the probability its own plan demanded, which makes the wins a sum of independent draws with different probabilities, a Poisson binomial. The exact tail is computed from the stored break-evens, because they are stored per row and the arithmetic over a few thousand rows is a dynamic program, and code owns every number the page shows. The binomial at the mean break-even is the conservative approximation and is named rather than used: it is at least as large in the upper tail for a count at or above the mean plus one, which is where a verdict that clears its bar sits, and a test asserts that bound over constructed setups in that range. The threshold is 0.05 divided by the family the reason belongs to, one-sided, since the question is whether a reason beat the bar its own plans set rather than whether it differed from it. The independence the test assumes does not hold, because listings cluster by sector and by date, which is why the minimum is 250 resolved setups over at least 60 distinct listing sessions rather than 250 rows however they arrive. Ruled at 8.0, built at 8.5.

**A reason's share, verdict and both floors are counted over the resolved setups that set a bar** A reason is scored over the setups it produced that resolved as a win or a loss and carry the break-even their entry close set, and over no others. An unresolved setup is left out because it is neither right nor wrong (see: An unresolved setup is never a win), and a loss that entered and stopped on one session is left out because it set no bar, so there is no probability its plan demanded for the test to draw it at. The count against the minimum, the sessions counted against the night floor, the share, the mean break-even, the exact tail and the verdict are all taken over that one set; a page draws a verdict exactly where the test was run, and the count it draws beside the share and inside the dashed outline is that set's count and says so. The row's resolved column still counts every win and loss, since it is a count and not a rate. The alternative was to open the page on every win and loss and test the subset that set a bar, which draws a share beside a denominator it was not computed over and, wherever the two counts straddle a floor, opens the page on a verdict the test withheld. Counting an unresolved setup as a losing draw would punish a reason for trades that never finished, and counting the session it arrived on would let a verdict show over nights on which nothing resolved. Built at the 8.5 correction.

**A stored break-even is measured from the close the setup was entered at, as a percentage beside the figures it is compared with** The bar a setup had to clear is risk over risk plus reward, and 8.2 measures the risk from the close the setup was entered at rather than from the entry zone the plan named, for the reason the return is measured from there (see: A setup is scored from its entry, and a target reached before the entry is never a win): a plan filled at the bottom of its zone risked less and stood to gain more than the same plan filled at the top, and one bar over both scores a trade nobody took. It follows that a setup with no known entry close carries no break-even, which is a setup nobody entered and one that entered and stopped on a single session, so the break-even and the setup horizon's return are present together and absent together, and the share of setups that cleared their bar is stated over the setups that set one rather than over every resolved row. Five of the 93 setups the operator's store had resolved on 2026-09-16 set no bar. It is stored as a percentage because the two figures it sits beside on that row and the share it is tested against are percentages, and a number written in one form beside a number written in another is compared wrongly once and then believed. That is a different form from the plan arithmetic the name page draws, whose break-even is a fraction because it is a number in a plan rather than one set against a rate, and the two are told apart by the column carrying the word and by this entry. Built at 8.2.

**An entry is admitted only inside the plan's own range, and a plan with no entry zone takes its whole range as its zone** A setup's entry close is admitted only where it sits at or above the plan's stop and at or below the lower of its entry zone's top edge and its first traded target, on the listing night and on every session after it, and a plan carrying a stop and a target and no entry zone takes that whole range as its zone. It narrows the entry rule over two shapes that rule's words did not address, and supersedes nothing (see: A setup is scored from its entry, and a target reached before the entry is never a win): a plan with no entry zone, which 8.1 scored from its listing close wherever that close sat, and an entry zone whose top edge sits above the target, which admitted a listing close between the two. Each wrote an outcome carrying a return and no break-even, because the share is undefined for an entry outside the range, and that breaks the pair the break-even is stored as (see: A stored break-even is measured from the close the setup was entered at, as a percentage beside the figures it is compared with). A listing close above the target is a target the price has already passed, which the entry rule calls `never entered` on any later session, so admitting it on the listing night alone scored one close two ways. Refusing such a plan was the other choice, and a refusal inside the forward-returns stage stops the night over a shape a stored plan can take. No stored plan takes either shape: read immutable from the operator's store on 2026-09-18, at schema 27, every one of the 1,928 listings whose plan carries a stop and a first traded target as text carries an entry zone's top edge at or above its stop and below its target, so the rule admits exactly the closes the entry rule admitted over every stored plan. Ruled at the 8.2 correction by the session correcting it, and not by the operator.

**The model writes no entry or exit proposal, and the loop scores only what code computes** The loop's eighth checkpoint stored the model's own entry and exit proposal beside the computed plan and scored it on the same rule, and that asks a model for prices, against the hard rule that no model is ever asked for a number, a date or an estimate. It is dropped rather than carved out. Two readings were available and neither is taken: a model choosing among computed levels by identifier, which is a ranking dressed as a choice and still puts a model in the path a purchase is decided on, and a decision superseding the rule for a stored second opinion never displayed as a fact, which would leave the rule true everywhere except the one place a number decides something. The proposal would also have been the slowest thing in the loop to earn a verdict, since it exists only for the name-nights a research pass reached while the rest of the loop scores every name-night. Ruled at 8.0.

**A condition is judged against the break-even its own plan demands** Every listing carries an entry, a stop and a target, so it states the hit rate it needs to be worth taking. The score for a condition is whether its setups reached the target before the stop more often than that. A forward return over a fixed window mostly measures whether the market rose, which is why the run page shows those against the universe base rate; this score needs no such correction because the bar moves with each setup.

**A reason's record is displayed, beside the reason and never beside the name** Once a reason has enough resolved setups it shows the share that reached the target before the stop, the number resolved, and the break-even those setups demanded, always as the three together. That tells you which of tonight's reasons has a track record while you are reading. It is a property of the reason across every name it has ever fired for, so it is not placed next to a ticker, where it would be read as that stock's chance of going up.

**The record column stays empty until it has earned a number** Below the minimum the display shows how many setups have resolved against how many are needed, and no rate. That will be the state for the first year, because a list of about fifteen names a night accumulates a few hundred setups a year per reason. Lowering the minimum so the column fills sooner would put a figure on the screen that looks like evidence and is not, and a figure on a screen gets acted on.

**An unresolved setup is never a win** A setup that has reached neither its target nor its stop within the time cap is reported in its own column and excluded from the rate. This is the same rule as unexamined never counting as a pass in the harness, applied to trading outcomes.

**Candidate conditions are registered before they are scored, and scored in shadow before they are shown** The register is an append-only table naming each candidate, its rule, its test and the date it was registered, with a stated maximum family size. Rows are never updated or deleted: a retirement is a new dated row naming what it retires, which is the same convention this document uses for superseded decisions. What pre-registration needs is that you cannot quietly change what you registered after seeing the result, and append-only with an insert timestamp gives that without keeping a fact outside the store. A candidate is evaluated and stored nightly like a live condition and appears nowhere until it has cleared the minimum evidence and the corrected threshold. Nothing is adopted on the strength of the idea, and nothing is chosen after seeing which way the data went.

**The significance threshold is divided by the family size, and the divisor is shown** Running several candidates and keeping whichever looks best is the standard way a self-improving system makes itself worse while appearing to learn. Showing the divisor beside the verdict means a later reader can tell how hard the test actually was.

**Adding a candidate later restarts the clock** Registering a new candidate after the family is set inflates the family, so the correction changes and affected verdicts are recomputed. Without this the register could be widened quietly until something passed.

**A registration names an evaluator the code carries, and its version is the pin of every source its evaluation runs through** A registered candidate carries the name of an evaluator, the parameters it will be run with, and that evaluator's version, so the row says what will run rather than describing it. A rule written only in prose is re-implemented by whoever scores it, and what they implement is whatever they read the words to mean, which is the thing pre-registration exists to stop: the registration and the thing tested come apart with nothing able to say they have. The version is a pin rather than a number somebody raises, because a number is a second statement of the same fact and drifts the first time a parameter is tuned in a hurry, where a pin cannot. It is taken over the evaluator's own source and every source that decides a value the evaluator reads or the verdict it returns, from a stored bar's text to the verdict: the money and statistic crossings, the indicator arithmetic and the stage that stores its readings, the builder that hands an evaluation a name-night's readings and names the session before, the shadow column that runs it, the catalogue that resolves its name, and the shared evaluator code that reads a registration's parameters. They are listed beside the version in `CandidateEvaluator`, and `register-append-only` holds the list to the pin, to every shipped file that computes a reading or runs an evaluation, and to the types an evaluation calls. What decides only whether a name-night is evaluated at all, being the register's standing rule, the gap stop and the staleness read, is outside it, because a change there produces a skip or a different set of name-nights, each named on its row, and never a verdict under a rule the register does not name. A pin over the evaluator's own file left the reading of the session before, the parse of a registration's parameters and the arithmetic of the readings free to change what a registered rule fires on while the row still matched; the ladder rules' code version covers every source a replay runs through, as the operator ruled on 2026-09-16, and a registered candidate is held to the same reach. The pin is taken with line endings normalised to LF and any leading byte order mark removed, so the same code pins the same on both machines and on a runner that checked the tree out with either ending, and less the one line carrying the version, because a pin of a file including its own pin never settles. What this costs is that every change to any of those sources, down to a comment, moves every evaluator's version, and each standing candidate is skipped and named as a failure on the listings stage's run log row until it is retired and registered again under the version the code carries, under the same name or another. That is the cost being bought: a row naming code that has moved is a row naming a rule nothing runs, and a night that evaluated it would write evidence about a different rule under the registered one's name. Ruled at the 8.3 correction.

**A candidate stands by the last row naming it, and a name retired and registered again stands once** A candidate stands registered by the last row naming it, a registration naming it or a retirement naming what it retires, ordered by instant and then by row. Reading the first row would make a retirement permanent in a table that can only be appended to, and a set of every name registered less every name retired does the same by another route: a name retired and registered again is in both sets, so it dropped out of the divisor, the run page's count and the maximum of eight while the shadow column evaluated it every night, and a ninth candidate could stand. The same name may be registered again, which is how a candidate whose evaluator moved is registered under the code that now runs, and it stands once, in the divisor and against the maximum, because it is one condition being tried; which registration a night's evaluation belongs to is the one standing when that night started, which the register's own instants say. The divisor, the maximum, the shadow column and the run page read the rule from `CandidateFamily` alone, so no two of them can answer differently. Ruled at the 8.3 correction.

**A register row in the same second as the instant it is compared with is read as after it** `registered_at` is held to the second, as every instant this repository stores is, and a night's start and a window's opening are read off the clock with their fractions. Compared as they stand, a registration made a fraction of a second after a night started is stored in that second, reads as before the start, and is evaluated by a night that began before it was registered. So the question of what stood before an instant takes the instant to the second as well, and a row in that same second is read as after it: a registration there is not evaluated by that night and not counted in that window's divisor, and a retirement there has not yet withdrawn its candidate. The table cannot say which of two things in one second came first, and this is the reading under which no night evaluates a candidate registered after it started. The registrar's own refusals ask what stands as of the moment it read the register, the rows written in that second included, because a registration and its retirement a moment later are both already rows. Ruled at the 8.3 correction.

**Only a missing or moved evaluator fails the shadow column, and a name-night without the readings is a counted skip** A registered candidate the night cannot evaluate on a name is written into that row's `skipped` list with its reason whatever the cause, so the record of the hole is the same either way, and what differs is what the listings stage says about it. A candidate whose evaluator the code no longer carries, or whose evaluator's version is not the one it was registered under, can be evaluated on no name at all: that is a fault in the code, it holds on every row, and a person has to act on it by retiring the candidate and registering it again, so it fails the stage, is named once on the stage's run log row and puts the stage under failures on the run page. A name with no bar for the session, a name whose stored series has a gap, and a reading not available for the name are facts about that name on that night, and the index holds some of each on ordinary nights: a member the day's file carries nothing for, a member with a hole, a member too new to fill the longest average's window. Each is skipped on its own row with a reason saying which, counted by cause on the stage's line, and fails nothing. Counted as failures they would put the stage under failures on every night a candidate stood, and a stage that fails every night is one whose failure nobody reads, so the fault the failure exists for would arrive looking like every other night. The stage's line no longer says a candidate skipped on one name was not evaluated on any name-night, which was false for every candidate evaluated on the rest of the index. Ruled by the operator on 2026-09-16.

**A ladder rule's version is measured beside that rule's live window, and both count against the bound** A version of a ladder rule is opened only while that rule's live window is open, under exactly the parameter names the rule is replayed from, and a live window is not closed while a version of its rule stays open. The live window is the one the night compares against the build's own hash, and a version replays through the same code the live rule runs through, so a version opened with no live window beside it is a measurement whose subject can move with nothing to stop the night: the code changes, the version's scores become scores of something else, and nothing says so. A parameter name the rule does not read is refused because the replay falls back to the live value for a missing name, which would store the live rule's plans under a version's name. Every open window counts against the caps, live ones included, and the caps are per rule: two windows of the merge distance, whose versions replay the level stage at 148 seconds on the night of 2026-09-14, and four of each other rule, whose versions replay the ladder stage at 5. That puts the fullest register the caps admit at one level replay and twelve ladder replays, 208 seconds on a 495 second night, inside the deadline of 900, where the bound as 8.0 wrote it and 8.6 built it, four windows of every rule and fourteen at once with no live window required, admitted four level replays and ten ladder replays, a night of 1,137 seconds, and stated 688 as though it were the end. The alternative was a bound on projected seconds rather than on windows, which would move every time the night's stage durations did and leave a person unable to say in advance whether a version would be admitted; a count of windows can be read off the verb's own list. The caps are the operator's ruling in the third round of the phase 8 plan's review, approved on 2026-09-15: two windows of the merge distance, four of where the stop sits and four of the near-exit skip, each rule's live window counted and no extra replay, ten at once and a night of 673 seconds, with four of every rule rejected at 969. The zone edges rule arrived after that round, from 8.0's ruling on a tranche zone's edges, and carries the cap of the other rules that replay the ladder stage alone, which made fourteen; the trend rule arrived at 10.4 on the same terms and makes eighteen (see: The trend rule is a fifth ladder rule a version replays, and none of its three versions is live). The caps per rule are the ones ruled in 2026-09-15 and the sum is arithmetic over them, so a rule joining the set moves the total and settles nothing here. Three windows of the merge distance would put the night at 836 seconds, 64 inside the deadline on a projection from one night's stage durations, and the bound's operating row is what sets the caps from the scorer's own nights. A version is opened only beside its rule's live window because that ruling counts the live window inside each cap and the plan's enforcement hashes each live rule against its open window. The caps are ruled by the operator on 2026-09-15; the zone edges cap and the live window requirement were written here at the 8.6 correction.

**A version's score counts only for a session after the New York date its window opened on** A score the version scorer writes is flagged `scored`, and counts toward a record, only where the session it scores comes after the date in New York, read through the clock, that its window's opening instant falls on; every other score is `in_sample` and counts toward nothing. The flag compared the session with the window's date in UTC, and a night runs at half past eleven UTC on its own session's date, so a window a person opened after reading that night's list and before midnight UTC counted the night they had just read, where one opened half an hour later on the same New York evening did not. A date is the unit because a version is written by someone who has seen what the market did, and the market's day is the New York session: anything opened on a session's date could have been fitted to it. The instant the session's night ran was the alternative and is not taken, because it counts a window opened that afternoon by someone who watched the session trade, and it moves with when a night happened to run, a night re-run by hand included, where a date belongs to the session alone. What it costs is the session a window opens on, which never counts even for a window opened before the market opened. Ruled at the 8.6 correction by the session correcting it, and not by the operator.

**A backfill scores only a night the store computed, at that night's own price scale, and never rewrites a score already stored** A backfill replays a past session from the bands, trend and typical move that session's night computed and from bars as they stand now, and three things decide whether what it stores is a plan that night could have produced. The bands and trend are read as of that session exactly: a session fetched by a backfill or by a later night as a missed one holds bars and no bands of its own, and reading the newest earlier set replayed its close against another night's bands, so a backfill of a session no night computed is refused and a name its night computed nothing for is left out and counted. The bars, the swing lows and the typical move are divided by the session's adjustment factor, its bar's close over its raw close, and rounded to the store's price places: a refetch after a split or a dividend rescales a name's whole stored year, its swings and its typical move while the stored bands keep the scale their night computed them at, and a night's own bar carries no later adjustment, so the factor is exactly what has moved since and is one on the night itself. And a score already stored for that window and session is kept, because the night wrote it from inputs that agreed when it ran and a later reading of the same session replacing it is a figure rewritten after the fact, which is what the operator ruled against for a decided setup on 2026-09-16 (see: An outcome once decided is never rewritten, and a setup still in play is scored with its plan scaled by its listing session's adjustment factor). A re-run of the night itself still writes its set again, since it recomputes the bands with it. Two alternatives were not taken: leaving out every name an action touched since the session, which after a quarter's dividends is most of the index, and refusing a backfill across any refetch, which is every backfill older than a few nights. Ruled at the 8.6 correction by the session correcting it, and not by the operator.

**A rule version change closes the window with the evidence that produced it and opens its replacement in the same write** Section 13's guardrail says a rule version change is written with the evidence that produced it and the version it replaced. A window closed through the verb took an optional name for what replaced it and no evidence, so a change stored no reason at all, and the name it did store was text nothing checked, which could name a version never opened. A change is one command: it closes the old window writing its evidence and the name of the version replacing it, and opens that version beside the rule's live window, in one transaction, with the rule's cap counted once the old window is closed, so a rule at its cap can still be changed and no replacement is named that the same write did not open. A close with nothing replacing it is refused without evidence as well, because ending a measurement changes what is measured. Opening a version takes none, as a registration takes none: it states what will be measured, and the evidence is what the measurement later produces. The alternative was to keep the close and require its replacement to be open already, which a rule at its cap can never satisfy. Ruled at the 8.6 correction by the session correcting it, and not by the operator.

**The ladder rules' code version pins every source a live ladder rule or its replay runs through** The code version hashed into every window is the pin of every source file the compiled code runs through from where each side reads a ladder rule's inputs and applies it: the level builder's bands, the ladder builder's plan, the scorer's replay and the reads it replays from, and the hash the night compares. A test derives that set from the build's call graph and symbols and holds the scorer's list to it, so a rule or a helper that moves into a file the list does not name fails the check rather than leaving the code a window measures unpinned. The merge distance is one constant the level builder applies and the scorer hashes, so the value a live window carries is the value the night applies. A file is pinned whole wherever the walk reaches a method in it, which pins the swing and trend arithmetic that share files with the records both sides read; the indicator engine, the swing finder, the volume profile builder and the trend classifier are not reached and are not pinned, because they write the stored rows both sides read, and a change there moves the live rule's inputs and every version's alike, so a window still compares two rules over one set of inputs. The cost is stated rather than narrowed: an edit to any pinned file, a comment or a decision citation included, is a new code version, and every night with a live window open then stops at the rule versions step, before the close and the queue, until each open window of every rule is closed and opened again, because one code version is hashed into every rule's window. A pin blind to comments would be a second pinning rule beside the evaluator's, and a pin narrowed to the arithmetic would miss the reads that decide what a replay is given, so the cost is made visible instead: `version --list` names each live window the build no longer hashes to, and the runbook says to close the windows before such an edit merges. Ruled by the operator on 2026-09-16.

**A version of a ladder rule is refused at values its replay would not apply as given, or at its rule's live values** The replay rounds a merge distance multiple to the four places a price carries, counts a near-exit skip in whole typical days, and reads a flag as zero or not zero, so a version at 2.5 typical days replays two, a flag of 0.5 replays as set, and a multiple written to five places replays another multiple: each stores the plans of a value its row does not name, which is the outcome the refusal of an unread parameter name exists to prevent. A multiple is therefore a number above zero, since at zero no two candidates join and two at one price stay two bands, written to at most four places; a skip is a whole number of typical days from zero; and a flag is one or zero. A value no price can hold, not a number or an infinity among them, is refused at the open rather than stopping every night after it, and a multiple the open admits that still overflows a price on a name whose typical move is large stops the rule versions step naming the version to close. A version at its rule's live values is refused, because it would store the live rule's plans under a version's name, and a parameter named twice on the command line is refused rather than read as either value. Two versions at one value are not refused: a flag rule has one value besides its live one, so that refusal would hold it at two windows under the four its cap allows, and two such windows store the same plans under two names rather than a plan either did not produce. Ruled at the 8.6 correction.

**A live reason is added or retired only by a change to section 11 and the code together, and the register holds candidates alone** The six live reasons are section 11's and the code's (see: The candidate family is at most eight and the threshold is divided by it), and none is a row in the candidate register. A register row that withdrew a live reason would shorten tonight's list with no edit to the document, which is the code and the architecture disagreeing with nothing to say so. So a live reason is retired, and a candidate promoted to live, by one change to section 11's table and `ShortlistSeries.Reasons` together; the decision that change writes names the record that produced it, which is its evidence, and section 11's prior text in CHANGELOG is the version it replaced. A live reason is retired only once its record holds 400 resolved setups, the higher floor section 17 states. No page computes that floor: the count it is read against is the resolved count the run page draws beside the reason. The register refuses a live reason's name as a registration, since a candidate under it would give a later retirement two meanings, and refuses to retire a live reason's name that does not stand registered, naming the floor. A candidate that does stand registered is retired like any other even where section 11 and the code already carry its name, because a promotion changes those first and the candidate still has to leave the candidate family: refusing that retirement would keep it in the divisor and in one of the eight places for good. No component computes a candidate's record yet, so none can be promoted on the build as it stands (owes: A candidate's shadow record computed, and a promotion written against it). Decided at the 8.7 correction on 2026-09-18, under the operator's ruling of 2026-09-16 that every judgement the phase 8 sign-off review carried is corrected before the sign-off handoff. The content is that review's judgement, that the refusal the building session made stands and the promoted candidate it trapped is let out, and is not an operator ruling on it.

**A reason's record reads only the rows written under the threshold the code carries, and the rows written under another are kept** Section 13.3 says a rule or threshold is not changed during a window it is measured over, and that a change starts a new window and keeps the old one. A ladder rule's windows are rows (see: A ladder rule's version is measured beside that rule's live window, and both count against the bound). A reason's threshold is a constant in `ShortlistSeries`, and the listing row a reason fired on already stores the value it was evaluated under, `multiple` for unusual volume and `horizon` for earnings soon, so the window is read off the row rather than kept beside it. A reason's record, its track and its verdict, counts a fired reason only where the row's stored threshold is the one the code carries: a calibration that changes a threshold restarts that reason's record at nothing, and every row written before it stays stored as written. Counting every row whatever threshold it was evaluated under would mix setups from two rules into one verdict with nothing on the page saying so, which is the measurement whose subject moved that the guardrail exists to prevent. A row stating no threshold for a reason that has one is read as written under another, since it cannot say which window it belongs to; every listing row the operator's store held on 2026-09-18 states one, 3,530 of unusual volume at 2 and 3,530 of earnings soon at 20. A threshold is a named numeric constant in `ShortlistSeries` written onto its reason's rows under its own name, and `read-surface` fails one that is not. Decided at the 8.7 correction on 2026-09-18, under the operator's ruling of 2026-09-16 that every finding the phase 8 sign-off review raised is corrected before the sign-off handoff.

**Tonight's list selects the stocks worth buying or paying attention to that evening, and the improvement loop exists to make that selection better** The operator ruled on 2026-09-21: "The purpose was to show selection of stocks as of that day which are worth buying or paying attention towards. There is no point in having an improvement loop otherwise." The corpus had held two positions at once. Section 1 said the tool finds which stocks sit at a price their own chart has made significant and is not a screen, and the superseded entry refused any change that decides which stock is better than another. Section 13 meanwhile scores every listing as a trade with an entry, a stop and a target, which is already a judgement that one setup is worth taking and another is not. This settles which stands: the list is a selection, and a change that makes it select better is in scope, including one that orders names against each other. What the ruling does not move is stated here so it is not read as moved. The nightly run stays arithmetic, with no model call and no per-name request (see: The nightly run is arithmetic only). No reading of the fundamentals fires a reason or gates a tranche (see: No reading of the fundamentals fires a reason, gates a tranche or draws a panel). The tool does not trade, and the plan places a position and never sizes one (see: The plan places a position and never sizes one). A reason's record is shown beside the reason and never beside the ticker (see: A reason's record is displayed, beside the reason and never beside the name). A condition is registered before it is scored and scored in shadow before it is shown, and the register is never edited (see: Candidate conditions are registered before they are scored, and scored in shadow before they are shown). What the ruling does not settle is how the list selects: what "worth buying" is measured against, whether the list may rank by a score, the cap on how many names are drawn, the trend guard, what at entry zone means, whether a trend label must hold before it gates a plan, how a day of market-wide volume is treated, and how a changed condition goes live. Those are for the next phase's planning pass to rule, and settling them here would be that pass written under a ruling's heading. Until it rules on the order, tonight's list keeps the order it is built with (see: Tonight's list orders by the reasons that fired and then band strength, which are facts about the chart), whose reasoning rested on the line this entry removes. Supersedes **Improving what surfaces a name is in scope; ranking names against each other is not**, moved below with its reasoning.

**A price that has come down into a narrow buying zone since the previous session is a candidate condition** The operator ruled on 2026-09-22 that "at entry zone" is replaced, as a registered candidate, by arrived and narrow, and that the live reason stays unchanged beside it until the candidate is promoted. Exactly: the previous session's close was above the nearest buying zone's high edge, tonight's close is inside that zone with its low edge at or below the close and its high edge at or above it, and the zone's width, its high edge less its low edge, is no more than 1 of the name's typical daily moves. The multiple 1 is proposed. A close that went from above the zone to below its low edge in one session fell through the zone and is not an arrival. The live reason fires on a close anywhere inside a zone, and on 2026-09-18 225 of the 347 names with a buying zone had their close inside their nearest one, which is standing in the floor rather than arriving at it. Over the nine nights 2026-09-09 to 2026-09-21 the candidate would have fired 14, 9, 2, 2, 8, 5, 3, 6 and 3 times. Evidence: Osler (2000) scores a support or resistance level by whether the price arrives at it and reverses, which is the arrival reading; Chung and Bellotti (2021) find that a level's prior bounces and its age bear on whether it holds, so each verdict records the band's strength and the sessions since its newest member without testing either; Kavajecz and Odders-White (2004) find levels coincide with depth on the limit order book. Judgement: no study found sets the width multiple. One candidate rather than two, because registering arrival and width apart would grow the family the level is divided across. (see: Candidate conditions are registered before they are scored, and scored in shadow before they are shown)

**Unusual volume measured against the night's own median is a candidate condition** The operator ruled on 2026-09-22 that unusual volume relative to the night is registered as a candidate, and that the live reason and its multiple are not changed, because that is a live threshold with a measured record. Exactly: a name's volume ratio, tonight's volume over its fifty-day average, is above 2 times the night's median ratio and above 2 on its own. The multiple 2 is proposed. The median is taken before any name is evaluated, over the members on tonight's session that have a fifty-day average, leaving out any name with no volume tonight, and it is recorded on every verdict so a night can be replayed; it makes no request and no model call. The fifty-day average includes tonight, so above 2 times it is about 2.04 times the prior forty-nine sessions' mean. On 2026-09-18 the median name traded 2.00 times its own average and the live reason fired on 252 of 503. Of the two forms measured, the one ruled fires 8, 4, 8, 5, 8, 17, 8, 18 and 5 times on 2026-09-09 to 09-21, where the live reason fired 8, 4, 8, 4, 8, 17, 8, 252 and 5 by the stored rows, so it differs from the live reason on one of the nine nights and its record will mostly re-test the live reason's. The bare form, above 2 times the median alone, fires on names at 1.6 to 1.9 times their own average on a quiet night. Evidence: Gervais, Kaniel and Mingelgrin (2001) class a day as high volume when it is in the top tenth of the stock's own fifty-day window, forty-nine reference days and the day itself, and the effect holds when each day is ranked by the stock's share of market volume, which is a control for a market-wide day; Campbell and Wasley (1996) measure abnormal volume against a market-adjusted expectation. So each verdict also records the rank of tonight's volume within its fifty-day window. Judgement: the multiple 2 has no research basis. Breakout on volume also reads volume against the fifty-day average and did not share the flood, firing 7 times on 2026-09-18 against 16 the night before, so it takes no candidate.

**A crossing is a candidate condition only where the close sits half a typical move past the edge it crossed** The operator ruled on 2026-09-22, over counts re-derived at 10.3 from the nine stored nights 2026-09-09 to 09-21, that the margin is 0.5 of the name's typical daily move, measured from tonight's close to the edge it crossed: the high edge for a crossing upward and the low edge for a crossing downward, with the typical move the one the ladder reads. At 0.5 the condition fires 18 to 46 names a night, a mean of 30.1 and 271 over the nine nights, which reproduces the 2026-09-20 measurement of the tight setting at 20 to 48; at 0.25 it fires 38 to 89, a mean of 64.0 and 576; and the live reason, which fires on any edge crossed, fires 112 to 176. Power did not choose between them: at 0.5 about 1,896 setups are listed a block and about 1,138 of those resolve with a bar, against the 250 the single-read figure is stated at, and 0.25 holds about twice that. What chose is that 0.5 is the stricter reading of the defect the margin answers, 19% of 1,132 recorded crossings having finished within a tenth of a typical move of the edge and one having closed 9 cents past a level and listed, and that at 0.25 this one reason would draw a large part of the twenty rows the page holds. The margin is settled rather than proposed from here, so it is registered with the candidate and changing it is a new registration (see: Nothing a candidate or a version is registered with changes while it runs, and a proposed number is settled only by a new registration). Not settled by research: no study found sets the margin. Osler (2003) finds stop-loss orders cluster just beyond round numbers and rates move rapidly once they cross them, which bears on what follows a crossing and not on how far past it counts.

**The three candidates are registered at one instant** A candidate's window is tested at a level divided across the candidates standing when it opened (see: The candidate family is at most eight and the threshold is divided by it), so a candidate registered alone before the others would be tested at the whole level rather than a third of it. Registration is one command, run once by the operator after the crossing margin is settled, writing the three rows at one instant, so every window opens with three standing. A later registration does not raise the level an earlier window opened at. Evidence: Sullivan, Timmermann and White (1999) show that simple technical trading rules on the DJIA and the S&P 500 lose their apparent profit once the universe of rules tried is accounted for, which is why what was tried is counted.

**A candidate is judged by a sign-flip test over blocks of 63 sessions, with at least eight blocks** The unit of the test is a block of 63 consecutive calendar sessions keyed on the listing date, and the verdict is a block sign-flip randomization test. Exactly: s_j is the sum, over the setups listed in block j whose outcome window has closed, of the win less the setup's null win probability; T is the mean of the s_j over their standard deviation divided by the square root of q, the count of non-empty blocks; the p-value is the share of sign vectors in {-1,+1}^q, the identity included, whose T is at least the observed. Every sign vector is enumerated, since q is at most 16 at every look. It is the same sign-flip scheme as the restricted wild cluster bootstrap and is never described as equivalent to it. The Poisson-binomial p-value of the live family's test is shown beside it, labelled, and never decides anything. A permutation of outcomes across setups is not the test, because it leaves the sum of wins and the sum of null probabilities unchanged and so always returns 1. The operator ruled L = 63 and q >= 8: setups listed less than 63 sessions apart resolve over shared bars and are not independent, the one-sided floor of a sign-flip test is 1/2^q, so 6 blocks is the least that can reach a third of 0.05, and 8 is the ruled minimum. The length is registered with the candidate and never shortened by the data, only non-empty blocks count, and the block count is shown against its minimum wherever the resolved count is. Eight blocks is 504 sessions, about two years, plus up to 63 for the last to resolve; the 60-session floor stays and no longer binds. Evidence: Kolari and Pynnonen (2010) show that event-date clustering makes standard tests over-reject even at low cross-sectional correlation, and Lopez de Prado (2018, chapter 4) that outcomes whose windows overlap are not independent; Canay, Romano and Shaikh (2017) that a randomization test needs a group of transformations leaving the null unchanged, sign changes for a location null, valid with a fixed number of large clusters; MacKinnon, Nielsen and Webb (2023) describe the close relation between randomization inference and the restricted wild cluster bootstrap with Rademacher weights; Cameron, Gelbach and Miller (2008) that the wild cluster bootstrap with the null imposed holds size where cluster-robust Wald tests over-reject, in simulations of up to 30 clusters; Ibragimov and Mueller (2010) that groups are consecutive observations and the choice of group cannot be delegated to the data; Cameron and Miller (2015) that with few clusters p-values are discrete. No moving-block or stationary bootstrap is the verdict (Kuensch 1989). Judgement: the floor of 8 blocks. The live family's own test is not changed by this decision, and whether it should take this one is an open question. (see: A verdict tests a reason's wins against each of its setups' own break-even at a corrected threshold)

**A candidate's verdict is read only at looks fixed when it is registered, with each look's boundary found over every sign vector its blocks allow** A fixed-level test repeated as setups accumulate promotes a worthless condition far more often than its level says: at a 35% break-even and a third of 0.05, one read at 250 setups has a size of 1.28%, while reading after every three resolved setups reaches 4.9% by 500, 7.4% by 1,000 and 9.9% by 2,000, and three worthless candidates read nightly have about a 21% chance that one is promoted by 1,000. So each register row carries its looks at registration: at 8, 12 and 16 non-empty blocks, information fractions 0.5, 0.75 and 1.0, triggered by the block count alone and never by where the figure stands, with the level spent across them by a Lan-DeMets function of the O'Brien-Fleming type. Each look's boundary is found on the sign-flip null by enumerating every arrangement of the signs of the blocks the look holds, conditional on what those blocks came to: the arrangements that crossed at an earlier look stay marked, and the boundary is the lowest statistic at which the share of arrangements crossing by this look stays within the level the spending function has released. Nothing is drawn and no seed is taken. It replaces the simulation the superseded entry called for, for two reasons: a look holds at most sixteen blocks, so the arrangements are enumerable whole and an estimate of them would be a worse answer to the same question; and a simulation of independent setups would assume the independence within a block that the blocks exist because the setups do not have, while an enumeration over the blocks' own sums holds its size whatever the setups inside a block did to each other. Independent increments are not established under overlapping windows, which is why the joint distribution across the looks is enumerated rather than taken from the canonical form. Under independence at 500, 750 and 1,000 setups the crossing win shares would be 42.0%, 39.6% and 38.4%, overall size 1.52% and power 79.6% against a 4.6-point excess. The first look cannot promote: the level released at a fraction of 0.5 is 0.00071, below the floor of 1/256 that 8 blocks allow, so it can only retire; the earliest promotion is at 12 blocks, 756 sessions, about three years, and the maximum at 16, 1,008 sessions, about four years, each plus up to 63 to resolve. Evidence: Armitage, McPherson and Rowe (1969) on repeated significance tests on accumulating data; Lan and DeMets (1983) on spending functions chosen before monitoring; O'Brien and Fleming (1979); FDA (2019) guidance on adaptive designs, that analysis times chosen from results inflate error and that the maximum is not extended. Judgement: the three looks and their spacing.

**The nightly running figure is monitoring and never the verdict** The operator ruled on 2026-09-22 that the running figure may show on the run page every night, labelled as monitoring. The verdict field shows the last look's result, the looks remaining and what triggers the next, and a nightly figure never changes it. FDA (2019) recommends limiting access to interim results; the operator chose to show it knowingly.

**A look counts only the setups whose whole outcome window has closed** A setup resolved so far is not a random draw of its setups: in a driftless daily walk at a 35% break-even, wins take 22.9 sessions on average to resolve and losses 16.0, so an early record undercounts wins. The store shows it: setups listed 2026-09-09 to 09-18 hold 109 wins and 246 scored losses, 30.7%, against a median break-even of 39.1%. So a look's cut-off listing session is at least the entry window plus 63 sessions back, and it is stored with the verdict. Evidence, by analogy: Hampson and Jennison (2013) on pipeline subjects at interim analyses, and Lindon and Kallus (2026) on outcome-dependent censoring. Judgement: the simulation's figures.

**A setup's null win probability is calibrated from its own plan, and its planned break-even is shown beside it** The operator ruled on 2026-09-22 to calibrate. A setup's planned break-even, its risk over its risk and reward, assumes an exit exactly at the stop or the target, and both are judged on closes, so a close lands past either and the realized exit is not the planned one. Judgement from a simulation: a plan with no edge wins about 38.3% of its resolved setups at a stop one typical move away against a planned 35%, and about 30.5% at three moves, where the 63-session cap works the other way, so a worthless condition with tight stops could clear the planned break-even. So each setup's null win probability is simulated from its plan under the name's trailing volatility, with the 63-session cap and the round-trip cost, and used in the sign-flip test and in the Poisson-binomial beside it; the planned break-even is shown beside it. Before registration the store is measured for realized loss over planned risk and realized gain over planned reward, mean and 90th percentile, by stop distance in typical moves, over live listings and never a candidate's outcomes, and the calibration is pinned. The yardstick's idea stands: each setup carries its own bar (see: A condition is judged against the break-even its own plan demands). Evidence: Broadie, Glasserman and Kou (1997) and Janssen and van Leeuwaarden (2007) give the limiting expected overshoot of a zero-drift Gaussian walk checked at discrete times as about 0.5826 of one step's standard deviation, which is a benchmark and not a measurement of these names; Lopez de Prado (2018, chapter 3) frames an outcome by a profit barrier, a stop and a time barrier, which the yardstick matches. Realized loss in multiples of planned risk is reported beside every verdict and tested nowhere.

**The calibrated null carries a round trip of ten basis points, and thirty is shown as a sensitivity** A cost c raises a setup's break-even by c over its risk and reward, about a tenth of a point to a point at a small investor's costs and up to about seven points at thirty basis points with tight stops. Ten basis points round trip is pinned into the calibration before registration, and thirty is shown beside it. Close-based scoring assumes a fill at the exact close, which no order type guarantees, and the direction of that bias is unknown. Stop-outs on earnings sessions are reported apart, as a hypothesis. Evidence: Hagstromer on the S&P 500's effective spread, about 3.2 basis points in December 2015; Schwarz et al. on retail round trips from about 7 basis points; Frazzini, Israel and Moskowitz on institutional large-cap costs of about 18 to 30 basis points round trip; Arratia and Dorador (2019) that overnight gaps above 0.1% occur on about 78% of days with a mean near zero; Dubinsky et al. (2019) that earnings-day variance is about six times other days over 2000 to 2015, about 2.4 times in standard deviation. Judgement: the choice of ten.

**A candidate counts a setup entered and stopped in one session as a loss** A session that closes through both the zone and the stop is scored as a loss with no figure and no break-even, because the fill was somewhere in the zone and the store does not say where, and the live family's verdict keeps only setups that set a bar, so it drops them. The store holds 8 such losses of 254, and dropping them moves the win share from 30.0% to 30.7%. A candidate's record counts them as losses, with the null taken from the zone's high edge, the worst fill in the zone, and shows their count beside the verdict. Judgement: the high edge. Whether the live family should do the same is an open question.

**Holm's level passes between the candidates by a graph fixed when they are registered, and every verdict shows the lifetime count** The operator ruled on 2026-09-22 to keep a per-window level and show the lifetime count. The level at Holm's first step is 0.05 over the three candidates, each spent across that candidate's looks by its own spending function. A graph carried in each register row passes a promoted candidate's level in equal shares to those still standing, each then tested at its raised level with its own spending function, and a retired candidate's level passes to no one. Only the sign-flip p-value is corrected. Under independence the first promotion's power is unchanged and the second and third reach 32.4% and 42.1% within a read. The per-window level lets lifetime error grow, six candidates each at a third of 0.05 bounding it at 0.10, so beside every verdict stand the step, its level, and the count of candidates ever registered, and the rule is revisited when that count reaches 8. Evidence: Holm (1979) and Romano and Wolf (2005) on step-down control at the same level with more power, the first step as strict as Bonferroni; Maurer and Bretz (2013) on passing a rejected hypothesis's level by a pre-specified graph under group-sequential designs, with sequential p-values as Liu and Anderson (2008) give them, cited as Maurer and Bretz cite it; Harvey, Liu and Zhu (2016) and Bailey and Lopez de Prado (2014) on counting every trial; Tian and Ramdas on online error control. (see: The candidate family is at most eight and the threshold is divided by it)

**The graph steps promoted candidates in the order their promotions were written, and candidates crossing on one read in the order they were registered** The operator ruled on 2026-09-25, on the second phase 12 sign-off review's finding that the run page stepped the graph in the candidates' name order, so two promoted candidates were drawn in name order whatever order their promotions were written in. A promoted candidate's level passed to those standing when its promotion was written, so the promoted step first, in the order their promotion rows were written, instant then row, which is the order their levels passed in (see: Holm's level passes between the candidates by a graph fixed when they are registered, and every verdict shows the lifetime count). Candidates whose records cross on one read and are not yet promoted step after them, in the order their first registration rows were written, an order fixed when they are registered and never moved by their results. The order changes no verdict: a candidate crossing at the lower level crosses at the raised one, and the level the others are left at is the same either way, every candidate standing in a window holding an equal share at every step. It decides which of two is drawn at the graph's earlier step and at the lower level, and the level each record is read at on the page. Rejected: the name order, which a rename would move; and the smaller sign-flip p-value first, the order of Holm's own procedure, which depends on the results and can change from one read to the next.

**A candidate is retired by a futility guideline at its first two looks or by reaching its last look** A candidate whose observed win share is strictly below its null share at its first or second look is retired, and so is one that reaches its maximum look without crossing, each by a new dated register row naming what it retires. The guideline binds nothing: the promotion boundary is computed as if it did not exist. Under independence it retires 61.8% of worthless candidates by the second look and a candidate with a true 4.6-point excess 7.3% of the time; read nightly it would retire that candidate 12.1% of the time, so it is never read nightly. Evidence: FDA (2019) on nonbinding futility, and Lan and DeMets (1983) on conditional power. (see: A candidate stands by the last row naming it, and a name retired and registered again stands once)

**Nothing a candidate or a version is registered with changes while it runs, and a proposed number is settled only by a new registration** The calibration's method, inputs and cap, the cost and its sensitivity, the spending function, the looks, the graph, the method each look's boundary is found by, and the paths and the seed the calibration walks are fixed in the register row or pinned in code before the registration command runs, because the running figure is shown every night and a choice made after it has been seen is a free parameter. A proposed number is settled only by a new registration or a new version, with its own window and counted in the lifetime count, and any reading that motivates it happens at a look. The trend confirmation's two nights may be settled from flip-back counts, which are labels and not outcomes, and a changed count is a new version. The margin by which version A must beat version B is fixed at 5 points before scoring and has no settling row. Every threshold phase 10 chose was chosen on per-night counts, with no forward return and no setup outcome read, which is what keeps three an accurate count of what was tried. Evidence: Harvey, Liu and Zhu (2016) that significance hurdles must rise with how much was tried; Sullivan, Timmermann and White (1999). This entry supersedes the one that named a boundary simulation's draw count and seed among what is fixed: a look's boundary is enumerated rather than simulated, so it has neither, and what the calibration draws is what carries a count and a seed (see: A candidate's verdict is read only at looks fixed when it is registered, with each look's boundary found over every sign vector its blocks allow).

**The trend rule is a fifth ladder rule a version replays, and none of its three versions is live** The operator ruled on 2026-09-22 that the trend guard lands as rule versions and not as a direct edit, because it changes the ladder's shape. Version A: a close below both the 50-day and the 200-day average is a downtrend, whatever the swings say. Version B: the same, only where the 50-day is below the 200-day. Version C: entering downtrend removes buying zones the first night the label reads downtrend, and leaving it restores them only once the new label has held for 2 consecutive nights, read from the stored labels of the nights before; the 2 is proposed. Each is replayed beside the live rule, scored on its own and promoted by ruling. The trend rule takes the cap of four every rule but the merge distance has, and its live window with A, B and C fills it. On 2026-09-18 version A reaches 129 range names closing below both averages, 86 of them carrying buying zones, and version B 35, 30 with zones; the 94 A adds have the 50-day at or above the 200-day. If both clear, B is kept unless A clears by 5 points. Stored listings keep the plan as it stood that night. Evidence, as a prior and not a verdict, since each is a monthly portfolio sort and not a stop-and-target setup: Jegadeesh and Titman (1993) find two-to-twelve-month losers keep underperforming, Jegadeesh (1990) and Lehmann (1990) that one-month losers reverse, George and Hwang (2004) that nearness to the 52-week high predicts returns and does not reverse, and Avramov, Kaplanski and Subrahmanyam (2021) that the distance between the 21-day and 200-day averages predicts returns, more strongly on the long side. B's names resemble intermediate-term losers, where removing zones is supported, and the names A adds are mostly intermediate-term winners in a sharp pullback, where both momentum and short-term reversal argue the zones may be right, which supports B over A. Judgement: version C's one-sided rule and its 2 nights, for which no study was found. Trend labels changed on 210 of 3,523 night-to-night pairs on 2026-09-09 to 09-18, 6.0%, and returned the next night 35 times on 25 names. (see: A ladder rule's version is measured beside that rule's live window, and both count against the bound)

**A trend version is judged by the candidates' test on its difference from the live rule** A version's promotion reads the block sign-flip test on the paired difference, the version's setups less the live rule's per block, at looks fixed when it opens, over closed windows and on the calibrated null. Choosing between A and B is a Reality Check with the live rule as the benchmark, computed on the same enumerated sign vectors, because picking the better of several on the data that scores them needs the best to be tested against the benchmark and not alone. The record of the setups A removes and B keeps is reported under the live rule and tested nowhere. Evidence: White (2000). (see: A candidate is judged by a sign-flip test over blocks of 63 sessions, with at least eight blocks)

**A version's record is read from the blocks frozen as each completed** A version's record is read at 8 blocks of 63 sessions and again at 12 and 16, which is 1,008 sessions and about four years, while `version_score` and `ladder` are kept one year back from the newest stored session. A record computed from those two tables when it is read could therefore hold 3 whole blocks at most against a floor of 8, so its verdict would be withheld for as long as the window stayed open and the page would say it was waiting rather than that it could never arrive. So the one number per block the record reads, the version's excess and the live rule's over that block, is computed on the night the block completes and written to `version_block`, which no retention reaches, and the per-night detail goes on being dropped at a year. The reader joins neither dropped table. The block origin is written with the first block and never recomputed, because retention moves the earliest scored session forward and an origin read again would re-cut the blocks under a record already being read. It is the shape `forward_return`'s calibrated bar and the listing's band strength already have, and the three alternatives were measured off the operator's own store at 399.5 bytes a `version_score` row and 1,386.8 a `ladder` row, projected to 18 windows, 502 names and 1,008 sessions: freezing one row per block is 288 rows and 27 KB, freezing one row per setup would be 7.6M rows and 349 MB, and raising the retention to four years would be 9.6M rows and 4.0 GB against a store of 221 MB on a spinning disk. A block is frozen 63 sessions after its last, so the oldest session it needs is 125 behind the night it completes against a window of about 252, which is 127 sessions of margin. Nothing past the last look is frozen, because the last look is never extended and a seventeenth block is read by nothing. Found at the phase 10 review of 2026-09-23 and ruled by the operator in the prompt that corrects it. (see: A listing records the band strength the old order read, and the three orders are compared over the nights that recorded it) (see: A trend version is judged by the candidates' test on its difference from the live rule)

**A version's record belongs to the window its scores were written under and never to the version's name** Every reader of a version keys on the rule, the version and the instant its window opened, which is the key `version_score` already carries. A version is changed by closing its window and opening another, and the runbook's remedy for a pinned source that moved is to close a window and open it again under the same name, so one name carries rows of several windows and did so twice in the four days to 2026-09-23. Keyed on the name alone, a session scored under two windows is counted twice in the labels a region draws and fed twice into the blocks a record is read over, and the record's origin is taken from the older window. The instant and not the date it falls on, because the remedy run after midnight New York opens a second window on the same date as the first. The surface draws the instant for the same reason. Found at the phase 10 review of 2026-09-23. (see: A version's record is read from the blocks frozen as each completed)

**The order tonight's list is drawn in is compared against the order it replaces, declared before any record is read** Three orders are measured over the twenty each would draw: fired count then band strength, the order the list had before phase 10, which is the benchmark; fired count then the plan's reward to risk, which the operator ruled; and reward to risk alone. Switching needs a Reality Check over both challengers to reject at 0.05, paired by night over the rows each order draws, with the metric and the row count fixed first and the same blocks as the candidates' test, and the chosen order's own p-value is never reported alone. Evidence: White (2000) that when the best of several is picked on the same data the right null is that the best is no better than the benchmark; Diebold (2015) on paired loss differentials with dependence-robust errors, from which the Diebold-Mariano comparison is described; Hansen's SPA is known here from secondary sources only. Research does not settle which tiebreak is better: Chung and Bellotti (2021) find levels with more prior bounces more likely to bounce again, decaying with time, while Osler (2000) found the published strength ratings bore no consistent relation to bounces, the only significant differences being negative.

**A listing records the band strength the old order read, and the three orders are compared over the nights that recorded it** The comparison of the three orders needs every order to hold 8 blocks of 63 sessions with a setup whose whole outcome window has closed, which is about two years, and bars, bands and plans are dropped a year back, so an order recomputed from them would lose its oldest nights before the comparison could ever be read. The listing row is kept, so from 10.1 it records the highest strength of the name's bands on its own night, 0 where the night stored none, and its plan already keeps the first tranche and the first traded exit the reward to risk reads. The comparison reads the nights whose rows recorded the strength, up to the night the run page shows, which leaves out every night written before the 5.4 correction as well. On each night each order draws its first 20 fired rows, and a drawn row whose plan computes no reward to risk has no stop or no traded target, so it is no setup and adds nothing. A setup's whole outcome window has closed once 63 sessions have traded after its listing, the setup cap being counted from the listing session, and the blocks are 63 exchange sessions each, counted from the first recorded night and keyed on the listing's session. The measure, fixed here before any record is read, is the excess the candidates' test sums: per block, the wins among the closed-window setups an order drew less their calibrated null win probabilities, with each challenger's difference from the benchmark taken block by block. The run page states each order's setups, its closed-window setups and its blocks against the floor, and draws no comparison until every order holds 8. Judgement: recording the strength rather than each order's draws, because the draws can be rebuilt from the fired count, the strength and the plan the row keeps, and positions stored beside them would be a second statement of the rule the orders apply. (see: The order tonight's list is drawn in is compared against the order it replaces, declared before any record is read)

**Power is stated for the looks that exist as well as for a single read** A single read at 250 independent setups, at a 35% break-even and one-sided 0.05 over three, needs a 9.45-point excess for 80% power and 6.81 for 50%, and has 23.8% power against 4.6 points and 10.2% against 2.9; at design effects of 1.25, 1.5, 2 and 3 the 80% figure rises to 10.72, 11.66, 13.16 and 16.00 points and the power against 4.6 falls to 18.1%, 15.7%, 13.6% and 10.4%. The looks that exist hold far more setups than 250 and are limited by blocks rather than setups. The first has no power by construction. Judgement from a simulation of block sums at the later looks, with the level split conservatively between them: at design effect 1 the arrived-and-narrow candidate, about 161 resolved setups a block, detects a 4.6-point excess 98% of the time, 80% at design effect 2 and 65% at 3; the volume candidate, about 340 a block, 92% to 100%; a crossing candidate at either margin, over 1,000 a block, about 100%. That assumes the edge is steady across blocks, and heterogeneity would lower it. The figures are re-derived on the calibrated null before registration, re-read at every look with the excess observed, and each verdict shows its estimated design effect, the block-robust variance of the summed excess over the sum of p(1 - p), labelled noisy near 8 blocks. Evidence: Osler (2000) found published levels bounced 60.8% of the time against 56.2% for arbitrary ones, and five days after publication 1.7 points less often than on the day, leaving roughly 2.9 points above arbitrary if that baseline held; the design effect is the actual variance over the variance under simple random sampling (Kish, as Cameron and Miller 2015 give it).


**The three phase 10 candidates are retired when the swing family registers, and each retirement says no result of theirs was read** Arrived and narrow, volume against the night and crossed by a margin were registered on 2026-09-23 to ask whether a tightened reason should replace a live one on tonight's list. From phase 12 no reason chooses the list, so the question they were registered to answer no longer exists. The live filter and its five variants are six, and with the three standing the family would be nine, above its maximum of eight (see: The candidate family is at most eight and the threshold is divided by it). Adding the six evaluators also edits files every registered evaluator's version pins, so the three would be skipped on every name-night as moved. The operator ruled on 2026-09-24 that the three are retired by the command that registers the six, at the same instant, and that each retirement row states that no result of the candidate it retires was read before it: a retirement taken after looking would be a selection on outcomes, and the row saying so is what shows this one was not. So the session that builds phase 12 reads neither the three candidates' records region nor their shadow outcomes before the command runs. Nine candidates will then have been registered in all, and the review of the lifetime count at eight stays with the decision that names it (see: Holm's level passes between the candidates by a graph fixed when they are registered, and every verdict shows the lifetime count).

**A shape acceptance restarts the live filter's edge clock, and after one acceptance while the list is live each further one states the blocks it restarts** The shape clock is due every sixty ordinary nights, and the live filter's first look is at eight blocks, about two years, with no promotion before twelve, about three. An acceptance moves the live settings the live filter's candidate was registered with, and nothing a candidate is registered with changes while it runs (see: Nothing a candidate or a version is registered with changes while it runs, and a proposed number is settled only by a new registration). So an acceptance retires the live filter's candidate and registers the accepted settings in the same write, and that candidate's clock starts again. Unbounded, an operator accepting every proposal would never reach an edge verdict, and the loop meant to say whether the filter works would never answer. The operator ruled on 2026-09-24: the settings ruled from phase 12's counts before the family registers are acceptance zero and restart nothing; one further acceptance, at the first trigger after the list goes live, costs the restart and nothing more, about one block of sixty-three sessions; after that shape is frozen, its proposals still computed and shown, and accepting one requires the command to state the non-empty blocks the live filter's clock has run, which the run page draws beside every proposal and the command refuses at any other count, so what an acceptance costs is a number read before it rather than a consequence found after. A rejection is never bounded.

**The shape proposer moves one setting a gate, nearest first, and never applies what it proposes** At the shape clock's trigger the shape proposer recounts every ordinary night's stored gate readings under other settings, which it may do because it moves counts and reads no outcome, and moves one setting a gate: trend and strength's floor, tried from 0.40 to 0.95 by 0.01; the setup's dry-up ceiling, from 0.50 to 2.00 by 0.05; and the trade's reward to risk floor, from 1.00 to 4.00 by 0.10. It takes the gates in the funnel's order, holding every gate before one at what it has just proposed, and for each tries the values nearest the one held first, taking the first that puts the gate's median count over the ordinary nights inside its band and the lower of two at the same distance. One setting a gate, because a gate moved on two settings at once reaches its band by many pairs and choosing among them is a choice the counts do not make; the nearest, because the smallest move that answers the band changes least of what the ruled settings meant. The pullback's depth, the base's tightness, the breakout's volume, the stop's distance and the market's breadth floor are not moved: each says what the setup or the market is rather than how many pass, and each stays the operator's to rule. A gate no value in its range brings inside its band, and the trigger, which has no threshold, are named as findings rather than forced, since a range that cannot reach a band says the gate or its band is wrong, and that is a ruling. The proposer writes one proposal a version, on the night the trigger is crossed, states the crossing on its run log row and writes neither a version nor the register; after a rejection the version stays open and the next proposal for it waits on sixty more ordinary nights, so a rejected proposal is not asked again of the same evidence. The operator's `shape` command is the only writer of a version and of a proposal's decision: an acceptance, of a proposal or of settings the operator rules, closes the open version and opens the next, named by its place in the order opened, and a rejection writes its reason and nothing else (see: A shape acceptance restarts the live filter's edge clock, and after one acceptance while the list is live each further one states the blocks it restarts).

**A variant of the swing filter is registered as a whole rule and runs on unchanged when the live settings move** Each of the five variants registered beside the live filter states every threshold of the filter, the one it varies and the ones it shares, as numbers in its register row. An acceptance that moves the live settings therefore changes nothing any variant tests, and its record stays a record of its own rule against its own null. What a variant loses is its reading as the live filter with one thing changed, so each variant's entry on the run page names the filter version it was defined against and, once the live filter has moved, the dimensions in which the two now differ. A variant crossing its boundary proposes its own whole rule as the live one. The variants are not registered again when the live settings move, because that would restart six clocks to keep a label current.

**The swing filter's shape is calibrated over its ordinary nights, a night one cause pushes past a quarter and twice its usual share is left out, and each band spans a third to three times what the ruled filter passes** The operator ruled on 2026-09-24 that tonight's list becomes a swing filter, so the six reasons stop choosing it and no reason's threshold is calibrated to a share of the index; what is calibrated is how many names each of the filter's gates passes. The shape clock counts, over the nights stored under the open filter version, how many members pass each gate after the market and every gate before it, with the market gate held open, since the market opens or closes the whole list and is not calibrated by count, and how many of those no exclusion removes. A night is an event night where a gate counted that way, or a reason, whose own median share over every night in the window is below a quarter passes or fires for more than a quarter of the index and for more than twice that median, or where the index's median volume against its fifty-day average is 1.8 or more; the market gate is never read for it. The medians are over every night in the window, event or not, so a night's classification never depends on the classification it decides. The operator ruled on 2026-09-25 that a flood is also more than twice its measure's own median: under the ruled settings trend and strength passes about 23% of the index, and a quarter alone marked eight ordinary sessions of the stored year, seven of them in the July rally, for passing 25 to 29%. An event is one cause pushing something far above its own normal share, and twice its median says so directly for every gate and reason alike, where excepting one broad gate by name would leave the next to be found the same way. Over the stored sessions this marks 2026-09-18 alone, the quarterly expiry on which unusual volume fired on half the index against a median near 2% and the index traded at twice its average. A gate counted alone was the first reading and marks ordinary nights, the setup gate passing 27 to 28% of the index on three of the twelve stored nights against a median of 24%, so the count through the funnel is the one read. An event night is counted, marked on the run page and read by no median. Each gate's median count over the ordinary nights is held against a band fitted to what the ruled filter passes, a third to three times its median over the 52 ordinary sessions of the stored year it was measured on, the low end rounded down and the high end up: 38 to 347 through trend and strength over a median of 115.5, 14 to 126 through the setup over 42, 6 to 56 through the trigger over 18.5, 1 to 12 through the trade over 4, and the list 1 to 9 over 3. The operator ruled the width: shape calibration exists to correct a threshold set wrong and not to follow the market, and the stored nights show the uptrend count falling from 109 to 66 in two weeks, an ordinary change of regime that half to twice would have announced as a mis-setting; with one cheap acceptance while the list is live, a wide band spends it on a real one. A proposal is still written at the trigger, and a band decides only whether it moves a gate. Before a version is open the filter runs on section 17's proposed values and no night counts toward the sixty ordinary nights the calibration waits on; from the first version, the nights under it do. The reasons' shares stay on the run page as context. This supersedes the entry that calibrated each reason to a target share, whose obligation is discharged with it (owes: The swing filter's shape calibrated from its ordinary nights).

**The swing filter's setups are scored on the swing trade's own plan from the listing close, and their first twenty sessions are context** The operator ruled on 2026-09-25 that the trade gate reads the swing trade's own plan, so that plan is the one the filter's setups are scored on. Every swing filter row carrying one, which is every row whose setup found a band, is scored the setup's way with no zone: entered at the night's close, stopped on a close below the setup band's low edge, won on a close at or above the low edge of the nearest resistance band above the close, and unresolved after 63 sessions, the setup's cap, with its planned break-even and calibrated bar written on the night it resolves as the listing's setup has them. A plan whose night's own close does not sit between its stop and its target is one the night could not enter at its close, and it is counted as not scorable rather than scored from a later fill. The same plan's outcome inside 20 sessions is stored beside it as context and read by no verdict: a swing trade of a few weeks is mostly decided inside a month, and a quicker read kept out of every test cannot become a second one. This is where the plan's twenty-session outcome lands under the ruled input, the setup the filter trades being the swing plan's and not the ladder's first tranche. The swing family's records are read over these outcomes, each candidate's setups being the rows it fired on.

**A gate's near misses are the setups it alone rejected, each group read against its own break-even and null and withheld below the block floor** Beside the setups the swing filter admitted, each gate and each exclusion has a group of its own: the rows the one gate or exclusion rejected while every other gate passed and no other exclusion applied, over the rows the open filter version stored. Each group is read the way a candidate's record is, its share of setups reaching target before stop against the mean of its own planned break-evens and of its calibrated bars, and no figure is drawn below eight non-empty blocks. A group is a population and not a test: nothing is promoted or retired on it, and it says which gate is costing setups and which is earning its place. A member the setup gate rejects has no band to stop below and so no plan to score, and the market gate's rows are whole nights, every member passing everything else on a night the market closed. The groups read the gate answers the nights stored and never evaluate a gate again, so no setting moved since changes a figure they state.

## The list

**Tonight's list is built from stated conditions, not a score** The length of the list is itself the reading, and a ranked cutoff is the same length every night so it cannot tell you whether anything happened. A condition is also a sentence you can check against the chart, where a score is a formula you would keep tuning.

**The page shows twenty and states the true count** A hundred-name night says a hundred.

**Condition thresholds are calibrated from your own nights, not from a backfill** The run page records what fired each night, so the distribution that sets the thresholds is the one you actually live with. That names no checkpoint and cannot: nights accumulate while the system runs and no checkpoint accumulates them, so the calibration is an operating obligation with a numeric trigger rather than a deferral to a phase (owes: The six reason thresholds calibrated from the nights they fired on).

**Every forward-return figure is shown against the universe base rate** Most names are higher after a month regardless, so a bare hit rate reports market drift as a finding.

**The base rate is over every name-night, and never over the listed ones** The population is every `listing` row for the window, being one per index member per night whether or not a reason fired, and not the rows where one did. `.claude/rules/writing-tests.md` states the same rule about this tool's own verification, and it is the same arithmetic either way: a figure computed over listed names only is a figure over the wrong population, because a listings row exists for every name. Comparing a reason's hit rate against the rate among names that fired would compare a signal against itself and report zero for a reason that works. The window is the horizon the figure is stated at, so a five-session return is shown against the five-session base rate and a twenty-one-session return against the twenty-one, and the population, the filter and the source are stated with the figure rather than beside it.

**The `setup` horizon has no universe base rate, and the column is null for it** Target before stop is a question about a plan, and a name with no plan that night has no answer to it, so there is no universe-wide figure of the same kind to pin beside it. The column is null for that horizon, as `return_pct` already is, rather than being computed over the name-nights that happened to carry a plan: that would be a base rate over a different population from the one the row is about, which is the thing this pair of decisions exists to refuse. What a setup is judged against is the break-even its own plan demanded (see: A condition is judged against the break-even its own plan demands).

**`base_rate` is stored per row on purpose, and the reason is that the row is what gets read** It is a property of the night and the horizon and it sits on a table whose grain is one row per listing per horizon, so one night's figure is repeated across the index. That is deliberate rather than a grain error. The row is what the run page reads and what a later session scores, and a figure that has to be joined for is a figure that can be shown without it; storing it beside the return is what makes "no forward-return figure without its base rate" a property of the store rather than a habit of the page. The cost is one REAL per row against a table already keeping every listing forever.

**Sessions to a dated event are counted on the exchange calendar and never on stored bars** A dated event is ahead of the night and the store holds no bar for a session that has not happened, so a count of stored bars after the night is 0 for every future event. The shortlist builder counted that way from 5.4 until its correction, and earnings soon fired for every name with any date on file however far off: 399, 477, 474 and 476 rows over the four whole-index nights of 2026-09-09 to 2026-09-14, all at a count of 0, where 30, 31, 28 and 28 names had a print on file within twenty sessions. The universe screen had counted the same fact on the calendar since 5.8, and the count now lives in one place both read. It is the sessions strictly after the night up to and including the event's own day where that day is a session, so 0 is tonight's print, and it still fires: a print filed after the close moves the next session, and tonight's list is the last surface to show the name before it does. A print filed before the open and dated tonight has its move in tonight's close already, which the calendar's `timing` column tells apart and this reason does not read. Where the lower edge sits is a condition threshold, so it is set from the operator's own nights by the calibration of the six reasons, which reads the share of earnings soon's fires at a count of 0 split by timing, and an edge it moves moves the ladder's second book and the name page's next dated event with it (owes: The six reason thresholds calibrated from the nights they fired on). An event past the last date the exchange closure table covers has no count: the reason does not fire, its values say beyond the exchange calendar, and the listings stage names such names on its run log with the table's end. That can hide a print inside the horizon only within about twenty-eight days of the table's end, and the night's closing line names that end on every session within ninety days of it. A member the night evaluates over nothing is not counted either (see: A member the night evaluates over nothing keeps the dated event the calendar holds and says no count was made). Rows written before the correction are kept as written, since a listing records what that night listed. What tells the two apart is the row's shape, a version marker rather than a stamp because the listing has no version column: earnings soon's values carry `next dated event` from the correction on, and breakout on volume's carry `previous close`. The run page's record counts each of those two reasons only off rows carrying its marker, and states the nights each record stands on counted the same way. Over the seven sessions written before the correction, 2,522 rows as the store held them after the night of 2026-09-16, that removes 2,306 fired from earnings soon's record, 46 won, 142 lost and 97 never entered among them, and nothing from breakout on volume's, which fired on none of them. Every surface that draws such a session says so beside it: the tonight, run, universe and name routes, and the report exported from a name page. Not taken: re-running the nights, because the shortlist builder writes the newest night each name holds and a replay of an older session would rewrite the newest rows rather than the replayed ones. Until the 5.4 correction of 2026-09-16 this entry named the tonight, run and universe routes alone, gave its removal as 99 wins and 23 losses, over the six sessions held before the night of 2026-09-15, and said a count of 0 fires without saying why. What it rules is unchanged, being the count's basis, the rows kept as written, the marker and a line wherever those rows are drawn, so it is corrected in place rather than superseded.

**Breakout on volume reads resistance at the previous session's close** A band is resistance to a breakout where its low edge was at or above the previous session's close, and tonight's close above that band's high edge, on volume above the fifty-day average, fires. The role the level builder stores is set against tonight's close, so every band it calls resistance sits at or above tonight's close, and a reason asking whether the close is above one could never be true: from 5.4 until its correction it fired on none of the 2,522 rows the operator's store holds from those nights, and the suite's recompute read the same role and agreed. The bands are tonight's and the side is last night's, which is how crossed a level already reads a band, and clearing the high edge rather than crossing the low one is what separates a breakout from a close that entered a band. Not taken: last night's stored band set with its stored roles, because a band set rebuilt tonight is the one the plan and the other reasons read. Until the 5.4 correction of 2026-09-16 this entry gave 2,018 rows, the six sessions held before the night of 2026-09-15; the rule is unchanged, so it is corrected in place.

**A member the night evaluates over nothing keeps the dated event the calendar holds and says no count was made** A member with no bar for the session, a gap in its stored series or no bar stored at all is evaluated over nothing and fires no reason, which keeps a figure computed across a hole, or off a session the member did not trade, off tonight's list. Five of the six reasons read a bar or a band such a member lacks, and what their values state stands as the 6.0 ruling on a stale member's values left it. Earnings soon reads the calendar and the night, and the calendar holds the member's date whether or not it traded, so its values carry the date the calendar holds on or after the row's session, and in place of the count, not counted with the reason: no bar for the session with the last session stored, the gap's date, or no bar stored. The path handed earnings soon no date at all until this was decided, and a row said not on file for a member whose calendar held a print, which a later reader of the stored values would count as a missing date: a member whose last stored bar is 2026-08-17 stored that on each of the five nights from 2026-09-10 to 2026-09-16, and on the last of them the calendar stage had stored its print for 2026-10-21 before the listings stage ran. Not taken: counting such a member's sessions to its date, because a stale or gapped member with a print inside the horizon would then reach tonight's list with an empty plan and nothing saying why, which is what evaluating it over nothing exists to refuse; and restating all six reasons as not evaluated, because the five that read a bar are already stated truly and a second vocabulary for them would split the stored rows into two shapes with no reader that needs it.

**The swing filter's starting settings are ruled from shape counts before tonight's list switches to it** Phase 12's filter was proposed with every threshold set before any night was measured, and a planning estimate over the eleven stored nights from 2026-09-09 to 2026-09-23 listed no name on nine of them and one on each of the other two, with the market gate at a breadth of 50 percent closed on the last. The operator ruled on 2026-09-24 that the filter is built and every gate result stored for every member while tonight's list stays as it was, and that the code's own counts come back first: each night in gate order with what each step removed, each gate relaxed alone with every other held, the trade gate read from the ladder's first tranche and from a swing trade's own stop and target, and the same counts over the stored year wherever the bars, the stored swings, the indicators and the membership give the readings as of each session, the market gate read at 45 and at 50 percent. The operator rules from those counts the starting settings, which reading the trade gate takes and the five variants, and the list switches after the family registers. The counts read no outcome: a count of names is what the shape clock moves, and nothing in them says whether a name went on to rise. The year's counts inform the settings the list starts on and nothing else; the calibration the shape clock runs at its trigger reads the gate results the filter stored night by night (see: Condition thresholds are calibrated from your own nights, not from a backfill).

**The swing filter opens loose on the swing trade's own plan, and each of its five variants moves one setting to its other side** The operator ruled on 2026-09-25, from 12.2's counts and a re-measure under the three-session arrival window over the store's 53 sessions, that version 1 opens on the strength floor at 0.50, the pullback 1 to 5 typical moves deep, the dry-up below 1.5, the stop 0.5 to 4 typical moves below the entry, a reward to risk of 1.5, the trade gate reading the swing trade's own plan and arrival inside three sessions, with the market floor at 50% and the tightness, the breakout's volume and the earnings window at section 17's proposed values. Section 17's values as proposed listed no name on the median session and at most one, empty on 49 of 52 ordinary sessions, which leaves the shape clock nothing to tune and the edge clock almost nothing to score; the ruled values list a median of three names, from none to eight, empty on 5 of 52, a list that selects. The list will rarely approach the twenty the page can draw, and that is the filter's selectivity rather than a fault. The trade gate reads the swing trade's own plan because it is the trade the list recommends, entry at tonight's close, stop at the setup band's low edge and target at the nearest resistance band's low edge, where the ladder stages a position over tranches and scoring from the first tranche's zone would measure a different trade; it also passed more names and left fewer sessions empty. The live filter is the loose side and the family tests the other, since tightening is the direction worth testing: the family is the live filter and five variants, each a whole rule with every threshold stated and one setting moved to its other side, a reward to risk of 2 against the live 1.5, a pullback 1 to 3 moves deep against 1 to 5, the market gate off against on, strength in the top third, 2/3, against 0.50, and one-session arrival against three, six candidates tested at 0.05 over 6 (see: A variant of the swing filter is registered as a whole rule and runs on unchanged when the live settings move). The dry-up's variant first proposed gave its place to one-session arrival, because the counts show arrival moving the list more than any other setting, while the dry-up at a live 1.5 is near its loosest. Version 1 still takes one further acceptance at the first trigger after the list goes live, so none of these numbers is final (see: A shape acceptance restarts the live filter's edge clock, and after one acceptance while the list is live each further one states the blocks it restarts).

**Version 2 of the swing filter lowers the market floor to 45% before the list's first live night, and nothing else moves** The operator ruled on 2026-09-25, after version 1 listed no name on the evening of 2026-09-24, that the market gate's floor is 45%, every other setting version 1's. Evidence: version 1 replayed over the 53 stored sessions from 2026-07-13 to 2026-09-24, reading the store and writing nothing, listed a median of 3 names a night, at most 8 and none on 5 sessions at 45%, and none on 7 at 50%. The two floors differ only on 2026-09-23 and 2026-09-24, breadth 49.8% and 47.6%, the only sessions of the 53 on which it fell below 50%, where at 45% the replay lists four names and three; every name the replay listed was a pullback. The gate still stands aside in a market clearly below its long average and no longer on one hovering at the line (see: The swing filter opens loose on the swing trade's own plan, and each of its five variants moves one setting to its other side). Version 2 opened at 2026-09-25T14:04:07Z through the shape command, before the family's first night, so it restarted no block: the live candidate registered for version 1 was retired and version 2's registered at that instant, and ten candidate conditions have now ever been registered. The five variants keep the 50% floor they were registered with and run on unchanged, so on a night breadth sits from 45% to 50% the live filter can list where the four variants that hold a market gate cannot, and the run page's edge half names the floor as a setting the live filter has moved since (see: A variant of the swing filter is registered as a whole rule and runs on unchanged when the live settings move). This was the acceptance version 1 had left while the list is live, so every further one, the shape proposal at sixty ordinary nights included, states the blocks it restarts (see: A shape acceptance restarts the live filter's edge clock, and after one acceptance while the list is live each further one states the blocks it restarts). Section 17's 50% stands as the proposed value the code carries.

**Arrival is a trigger that first fired within the last three sessions, and the trade is read from tonight's close** A pullback's trigger is a close above the previous session's high, or a close back inside or above the band after a session below its low edge; a breakout's is the first close above the band it cleared. The operator ruled on 2026-09-24 that arrival is the trigger not having fired the night before, and on 2026-09-25, from the counts, that it counts inside a window of three sessions: the trigger passes where its event happened tonight or on either of the two sessions before, having not happened on the session before that one, each read off that session's stored gate result, so a name whose trigger first fired yesterday lists tonight as well. The counts showed arrival as the gate that binds: at the loosest settings measured, the list's median over the stored year was one name a night with the one-session window, three with the window of three, and seven with arrival set aside. The window admits no stale entry, because the trade gate reads the swing trade's own plan from tonight's close, so a name whose trigger fired two sessions ago and has since run from its band fails reward to risk on tonight's price. A session in the window with no stored result, where the answer turns on it, cannot say, and the gate fails and names it rather than passing on the absence. The window is a setting, three proposed, and the one-session window is one of the family's variants, so the loop measures whether same-day arrival does better rather than assuming it. It was settled before the family registers, because after that a change to the trigger's code moves every candidate's pin and restarts about two years of each one's clock. For a breakout the rule is the one it already had, and the pullback's own window, a high made within the last twenty sessions, still bounds how long a name can sit in its zone.

**The swing filter's results are replayed for the sessions before its first stored night, for the trigger's arrival alone** The operator ruled on 2026-09-25, after version 2 opened at a 45% market floor with 2026-09-24 the only night the filter had stored. A pullback's trigger arrives where it first fired within the arrival window, read off the stored results of the sessions before the night, and the sessions before the filter's first stored night held none, so every pullback on the list's first nights failed its trigger for want of them. The `filter-history` verb replays the filter for sessions before its first stored night, from the bars, bands and plans the store holds as of each session and under the open version's settings, through the replay the filter counts run, and stores each member's result under the version `replayed`, which is no filter version. The trigger's arrival reads a replayed result as it reads a night's, and nothing else reads one: no clock counts a replayed session, the shape clock keying its window on the open version and the edge clock and the near misses on theirs, no page draws one, and the filler scores no replayed plan. A session that already holds results and the night's own session are refused whole, and the night that draws a session replaces whatever the session held with its own. Rejected: writing a trial copy's seeded results into the store, which no declared writer wrote; and running the older nights again, which the night refuses because its corporate action refetch would cut a refetched name's later bars (see: Arrival is a trigger that first fired within the last three sessions, and the trade is read from tonight's close).

**Tonight's list is the swing filter's, and an evening is listed by the rule that listed it** The operator ruled on 2026-09-24 that tonight's list becomes a swing filter, and on 2026-09-25 opened its first version on the swing trade's own plan. From the first night whose filter step records its session, tonight's list is the names the swing filter passes: every gate passed, the trigger arriving inside its window and no exclusion applying, ordered by the trade's reward to risk, higher first, then relative strength, then the setup band's strength, then the ticker, each a fact about the chart or the plan and none a judgement about the company. Each row carries its gates with the values that decided them, and the six reasons are still computed and stored for every member every night and stand beside each row as context. A night the market gate closed draws no row and one line with the breadth and the gate's floor, and a night no name passed draws one line with how many reached each gate, so an empty list says why it is empty. The night records the rule for its session in the swing filter's step, once the filter has stored its rows and before any later step can stop the night, and only where the filter stored rows; the night close is its only writer. An evening with no such record was listed by any of the six reasons firing, which is every evening before the switch, and it is drawn as it was listed, in the order it was drawn in (see: Tonight's list breaks a tie in fired count by the plan's reward to risk, and a row with none is drawn after every row with one and says why). A night run again over a session draws that session again and records its rule, so a session is listed by the rule of the night that drew it last. Every surface that says whether a name was on the list reads each evening by its own rule and names it: tonight's list and its header, a name's page, its listing history and its walk, the universe's last evening and strip, the run page's overlap, the facts retention, the overnight queue, which takes the filter's names first in its order, and the night's own request, which asks for the first name the filter passed and for none on a night none passed. This narrows three entries rather than superseding them, since each still describes code that runs: the list is still built from stated conditions and not a score, the five gates being the conditions (see: Tonight's list is built from stated conditions, not a score); the page still draws twenty and states the true count, the count the filter listed (see: The page shows twenty and states the true count); and the fired-count order still draws every evening the reasons listed. The ruled filter passed a median of three names a night over the stored year, so the list will rarely approach twenty, and that is its selectivity rather than a fault.

**The dated screens open from the swing filter's first night, and an evening before it is not drawn** The operator ruled on 2026-09-25, reading the evening of 2026-09-23 drawn with the 340 names its reasons fired: "Lets start from 24th as real night", with the evenings before it made unavailable on tonight's page and on the run page. On a store the swing filter has listed, both screens open from the first night it listed: a date before it draws one line saying the record starts on that night, with a link opening it, and the calendar on either screen holds the nights from that one on. A store the filter never listed draws every evening as before. Nothing is deleted. Every night's row for every member stays, which the reasons' records, each name's listing history and the scored setups are read over, and the name page's earlier evenings and listing history are unchanged; the swing filter's replayed answers for the sessions before its first night stay too, read by the trigger's arrival alone and drawn by no page (see: The swing filter's results are replayed for the sessions before its first stored night, for the trigger's arrival alone). The evenings before the first night were listed by a rule the list no longer uses, and drawn beside the filter's they read as the filter's own history (see: Tonight's list is the swing filter's, and an evening is listed by the rule that listed it).

## Presentation

**The momentum panel is context a reader weighs, and nothing computes with it** The panel draws the relative strength index and the moving average convergence readings beneath the chart with their neutral rules, and no component decides anything from them: no reason, no band, no plan, no gate. It is the only part of the technical half with no other purpose, and it keeps that one. Its neutral rules, fifty on the index and zero on the histogram, are reading conventions the page draws rather than thresholds any component applies, so they are not constants a check pins or figures a measurement settles. The one reader outside the name page is the facts file, which copies every indicator the engine wrote, so prose may state a reading and section 5's convention for it and may never present one as a signal or a reason to act. What is given up is that no momentum state can fire a reason or gate a tranche; giving the panel a job later is a candidate condition like any other, registered before it is scored and scored in shadow before it is shown. Ruled at 8.0.

**One application, not a file per report** The nightly run stores data and renders nothing. Reports are rendered from stored data when read, so a layout change costs nothing and no file on disk goes stale.

**A press writes a request and starts the worker's drain as a process of its own, and every pass waits for the off-peak hours** A press on a screen writes one row to the request store, as it did, and then starts the worker's `drain` verb as a process of its own and returns, so the read surface still writes the ask and never the research. The operator ruled on 2026-09-23, "I dont want nightly script to do it. I want the queue to be processed as soon as i queue them", after their first request waited from 19:01 to 19:09 UTC for a drain typed by hand; offered a watcher that finds a request, a task that looks every five minutes and the page starting it on the press, they chose the page. Every reason the superseded entry gave for a queue survives, because the request is still written first: the queue screen lists it, a request nobody has claimed can still be taken out, and a press that started nothing is told apart from one whose pass died, since the row says which. The surface holds no reference to the worker and writes nothing a pass writes: it starts the worker's own verb by path, as the runbook shows it run by hand, with the store it reads named in the environment. The drain runs from a copy of the worker's build output made for it, so a drain still writing when the night builds that output again holds none of it and cannot stop the night, and a second drain started while one runs takes only what the first has not claimed, since a claim is one write only one drain can make. A drain started inside a peak window, or reaching one between passes, waits for the pricing's first off-peak instant before its next pass, on the operator's ruling of the same evening, "off peak hours it is for everything" (see: Queued work runs off-peak, and every schedule is written in UTC). What is given up is that a press now sets a paid pass going with no second step. Supersedes **A request the page writes and the worker drains is what starts a pass, and the read surface writes the ask and never the research**, moved below with its reasoning.

**A pass is started only by a request carrying the name page's own header** The control spends money, and the read surface answers anything that can reach the machine's address, which includes a page on any other site a browser on this machine opens: a form that page submits to the control's address arrives looking like a press. So the page's own script sends the press with a header of its own, and the surface refuses a request without it before it reads the name. A form submitted from another site cannot carry a header, and a script on another site that tries to add one makes the browser ask the surface first whether that origin may send it, which the surface never answers, so the request is not sent. The spend cap still bounds what any pass can cost, and that is the reason this is a refusal at the door rather than a matter left to the cap: a pass the cap allows is still a pass nobody asked for. A token issued with the page was the alternative, and it defends against the same request with state the surface would have to keep, where the header needs none. (see: The name page's control starts the worker's research verb, and the read API writes nothing it starts) (see: The spend cap is a stop, not an allowance)

**The watch list is the operator's own, up to twenty names of the index, on a page of its own** The operator ruled on 2026-09-25, asking who makes the watch list, which section 15.7 drew above tonight's list and no store could fill: "I think it would be a good idea to have a watchlist section where users can add stocks from sp500", then "I am thinking watchlist should be an entire new page", holding up to twenty. The watch list is a store of its own, a name and the instant it was added, which the read surface writes on the operator's press alone, from the watch list page or beside a name on its own page, each press refused without the page's own header as every press is (see: A pass is started only by a request carrying the name page's own header). A name is added only while it is a member of the index and while fewer than twenty are held, and a refused press writes nothing and says why. The page draws each name for the newest night from that night's own rows, whether or not the list holds it, with what the swing filter said of it, and tonight's page draws one line counting the names with a link to it. It decides nothing: the night, the list, the queue, the report the night asks for and every record read no row of it, so a name watched is a name followed and never a name promoted.

**A single report can still be exported as a self-contained file** Handing one report to someone outside the tool is a stated use, and the exporter renders through the same code path.

**A screen reads and renders, and computes nothing** No screen calls a model, makes a network request, or works out a level, a stop or a rate. Every number on every screen is already in the store. A rendering layer that computes is a second implementation of the same arithmetic, and two implementations of one rule disagree eventually. It also means a screen can be rebuilt or restyled without any chance of moving a number.

**Marks are defined once and every screen draws from that list** Seven named marks, each a function from stored values to an SVG string, with no script needed to render one. Nothing on any screen is a one-off drawing. That is what lets the exported single report carry the same pictures as the application, from the same data through the same code, rather than two renderers drifting apart.

**Support and resistance own two hues and nothing else uses them** Green is a level below the price and orange is one above it, on every screen and in every mark. A day's price change in particular does not get them, because a green day beside a green support band is one colour meaning two things. Direction is carried by the sign on the number and by which side of a zero rule a bar sits on, and every band carries its role in words as well, so hue is never the only channel.

**Not yet measured is drawn as a dashed outline, never as a pale value** Anywhere a figure has not earned a number, the space carries its count against its minimum inside a dashed outline. A pale number reads as a small one, and the distinction between a small value and an absent one is the difference between a screen doing its job and a screen with no evidence.

**Every figure carries a plain-language key** A short paragraph beneath each picture explaining how to read it is the reason someone outside the domain can follow it.

**The plan figure is one vertical price column with the current price marked in it** Everything above the marker is a sale and everything below is a purchase, which is legible without reading a caption.

**Distances are stated as typical days' moves** "Two typical days away" is understood immediately where an ATR multiple is not.

**Every part of a page states where it came from and as of when, and a written section when it was written rather than which model wrote it** Supersedes **Every part of a page states where it came from and as of when**, moved below with its reasoning. Computed tonight, fundamentals as of a filing date, research as of the date it was written, and the key under each figure as of the close it explains. The model that wrote a section stays in the store and on the element the section is drawn in, and the page's words do not name it: the operator ruled on 2026-09-18 that no model's name is shown on a page, and on 2026-09-19 that a page says only when a section was written. A section is read for what it says, and which model wrote it is a setting of the lanes rather than a fact about the company; where a local draft is not good enough, the page's option to have the paid model write it again is what serves the reader.

**A written section is broken into parts only where its own prose says where each part ends** A model's prose is drawn as the claim checker accepted it, and a section a reader has to search is drawn as one they can scan wherever the prose itself says how. Two sections are drawn as parts. The two cases are two labelled halves where the section is the two paragraphs the writer is asked for. The risks are one part to a risk with what would confirm it set beneath it, where the writer either broke them into paragraphs or opened each on an ordinal and the word risk, and whatever stands before the first part opens that part rather than being dropped. Where the prose says nowhere that a part ends, the section is drawn whole: a boundary the page guessed at would put one risk's words under another's, and nothing a reader could see would say so. The parts are the prose cut and never edited, reordered or reworded, so they join back into the section as it was written, and that is asserted over every section of a pass rather than over the two shapes the page recognises, which is what makes a bad cut a failure rather than a different-looking page. Measured on 2026-09-20 over every sample the repository can reach: the paid lane's risks are ten risks in one paragraph each opening on an ordinal for one name and five paragraphs for another, and the local lane's two answers for that second name are four risks in one paragraph opening on none, which is the section drawn whole. Ordering the risks by how severe each one is is not this and is not done: a severity is a judgement the writer states or nothing does, and a page that sorted them would be ranking on a figure no model wrote and no code computed. (see: Code owns every number) (see: A figure is drawn at the places it is read at, and its element carries the stored value whole)

**A refused draft's own words are kept on the row and drawn on the evidence page, and never on the name page** A section the claim checker refused twice is stored with the reason, which carries one part per refused sentence: the rule that refused it, and the offending text after it. For most rules that text is something the checker extracted, a figure, a date, a window or a citation marker, and it is the most useful thing on the line. For two of them, a sentence naming no document and a cause naming the session of no move the facts file holds, it is the draft's own sentence, and on the name page it is not drawn: a reader there is asking what is missing and why, and the paragraph a model wrote and the checker threw away answers neither. Repeats are collected under their rule rather than drawn again, since one rule refusing six sentences is one thing wrong six times. The row keeps the whole reason and the run page draws it whole, that being the evidence page (see: Every part of a page states where it came from and as of when, and a written section when it was written rather than which model wrote it). Measured on 2026-09-19 over the nine sections then reaching a page: eight were unchanged by this and the ninth fell from 407 characters to 147.

**Every region is a card that states where its figures came from and how to read them** Each region of a screen is drawn as a card on one paper palette. A region computed from the nightly store is ruled across its top in slate and stamped with the night its figures are from. A section written by research or taken from a filing is ruled in plum-grey with the day it was written or filed in a column at its left, because a filed figure or a written paragraph can be weeks older than the chart beside it and a reader has to see that before reading it. Every picture has a key beneath it that says how to read it and closes on what to take from it. The app and the exported report carry one stylesheet, which opens with the rule each colour token is held to, and both palettes are designed rather than one inverted, the choice stamped on the root element and remembered between visits with the machine's preference as the default. A card holds a region's markup as the marks and the regions write it and adds words and layout only, so a card can be restyled without a figure in it moving. Adopted on 2026-09-19 from the design the operator approved. (see: A screen reads and renders, and computes nothing) (see: Every part of a page states where it came from and as of when, and a written section when it was written rather than which model wrote it) (see: Every figure carries a plain-language key)

**Tonight's list breaks a tie in fired count by the plan's reward to risk, and a row with none is drawn after every row with one and says why** The operator ruled on 2026-09-22 that tonight's list orders by how many reasons fired, then by the reward to risk the night's own plan computes from its first tranche, higher first, then by ticker in code-point order. A row whose plan computes none, having no tranche, no stop on its first tranche or no traded exit, is drawn after every row with one in its fired-count group and states why in the plan's own words; on the four nights 2026-09-16 to 09-21 that was 593 of the 1,463 fired rows, read off each row's own night's plan. The ratio is read off the plan the listing kept that night and worked by the ladder's own arithmetic, rounded as the ladder stores it, so an earlier night is ordered by that night's plans and the figure a row draws is the figure the night's plan holds. Reward to risk is a fact about the chart and not a probability of anything, so the rule that no screen shows a number beside a ticker that could be read as its probability of rising stands, and no screen orders names by anything read off the company. The order of the whole list is measured rather than assumed (see: The order tonight's list is drawn in is compared against the order it replaces, declared before any record is read). Research does not settle which tiebreak is better: Chung and Bellotti (2021) find that levels with more prior bounces are more likely to bounce again, decaying with time, while Osler (2000) found published strength ratings bore no consistent relation to bounces. Supersedes **Tonight's list orders by the reasons that fired and then band strength, which are facts about the chart**, moved below with its reasoning.

**The masthead carries the last stored close and the session it is from** The design's masthead shows a name's price, and no screen fetches, so the price is the last close the store holds, stated as of the close of the session it closed on, beside its change on the day off the two newest stored closes and the company's name, sector and industry as the membership row holds them. A live quote would be a request a screen made and a figure the store never held (see: A screen reads and renders, and computes nothing). Ruled on 2026-09-19.

**The sizing arithmetic states the risk a share carries and holds no account of the reader's** The approved design drew the sizing from an account and a risk per plan labelled as the reader's settings, with a share count worked from them. The store holds neither, and a plan places a position and never sizes one (see: The plan places a position and never sizes one), so the page states the risk a share carries from the entry to the stop and says the budget divided by it is the reader's, and draws no account, no budget and no share count. That is what keeps the name page's refusal true, that it does not say how much to buy. Holding the reader's settings would be a store the read surface writes, and it writes none. Ruled on 2026-09-19.

**A reason's record is drawn in two states, and a bare number beside a ticker is a prohibition rather than a third** Section 15.11's table has four rows, and the last of them, never shown, is not a state a record can be in: it is what no screen draws. So a record is drawn in one of two states, a dashed outline carrying its count against the minimum, or the share, the number resolved and the break-even together, and its unresolved setups are the track's own segment in either. Ruled on 2026-09-19. (see: A reason's record is displayed, beside the reason and never beside the name) (see: Not yet measured is drawn as a dashed outline, never as a pale value)

**Tonight's reason totals are counts, and a reason's record is the run page's** Section 15.7 drew tonight's reason totals with the reason track, whose segments are outcomes, and a setup listed tonight has none yet, so the track drew one dashed segment per reason and said nothing a count would not. Tonight's totals are each reason's count of tonight's fired names, drawn in the track's own geometry as a bar out of the fired count with the count written on it. How a reason's setups have ended is its record on the run page, read against the base rate. Ruled on 2026-09-19.

**Selecting a row draws its plan beneath the list and is no navigation** Section 15.7 draws the selected name's plan and levels so that checking a plan needs no navigation. At the page's width the region cannot sit beside twenty rows, so selecting a row, anywhere on it but its links, puts the name in the hash, draws its plan beneath the list on the same page and brings it into view, and back returns to the row. No second screen is opened and the list is not left, which is what the rule asks. Ruled on 2026-09-19.

**A figure is drawn at the places it is read at, and its element carries the stored value whole** A screen draws a stored figure rounded to the places a reader reads it at: a price, a ratio and an amount per share to two places, an amount of money and a count of shares in its scale, as $96.2B and 190.3M, a fraction as a percentage to one place, as 75.0%, and a multiple to one place. The element a figure is drawn in carries the stored value whole in a data attribute, and the checks read that, so every figure on a screen traces to a value the store holds and no rounding becomes a value of its own. Rounding a figure to be read is not the arithmetic a screen is barred from, since nothing is worked out that the store does not hold, and the places are the ones the claim checker already reads prose at, where $109.4 billion is a rounding of 109417000000 (see: Every number in written prose must exist in the facts file). A number inside a sentence the nightly store wrote, an event setup's trigger among them, is read the same way. Prose a model wrote is drawn as the claim checker accepted it, because a page that rewrote its figures would draw a sentence nobody checked. The redesign's plan asked for this and the design correction drew it for the fact strip and the marks alone, so the numbers section printed revenue as 96221000000.00 and margins as 0.749753 until the operator found it on 2026-09-19. (see: A screen reads and renders, and computes nothing) (see: Code owns every number)

**A name's listing history states what followed each evening it was listed and forms no rate for the name** Section 15.9 asks for the evenings a name was on the list and what happened five and twenty-one sessions later. Each evening is drawn as a row of its own, each result beside the universe base rate its row carries (see: Every forward-return figure is shown against the universe base rate), and nothing sums them: a count of wins or a share for the name would be a record beside the ticker, which is refused (see: A reason's record is displayed, beside the reason and never beside the name). A horizon that has not matured says so. The close is the stored bar's for that session, the series the chart draws: a reason that fired on the calendar carries no close of its own, and a year refetched after a corporate action moves every earlier close together, so NVDA's close for 2026-09-09 reads 223.42 beside a chart that draws 223.42, where the listing stored 223.1699 before its year was refetched the next night. The strip covers the sixty sessions the universe screen's strip does. It is drawn as a card after the plan rather than as a tab, since the page is one column of cards.

**A pass the page starts is watched until it ends and the page redraws as each section lands** Section 15.12's last step says a pass asked for from the page shows progress and its sections stream in as they are checked and stored. A pass writes a row for each component as it works and its own row last, and the prose writer stores each section as it writes it, so what a pass has done is already in the store while it runs and needs no new write to be watched. The page asks a read address every three seconds for where the pass stands and draws the answer beside the control, and each time one more section has landed it asks for the page again and redraws it in place, keeping the reader where they were. The reply to the press carries the instant the surface took before starting the pass, and the page hands that back with every ask, so the rows read are that pass's and not an earlier one's; a run carrying no rows yet is one that has started, and a run carrying the pass's own row is one that has ended, at which point the page stops asking. It stops in three other cases: the reader leaves the page, the address answers with anything but an answer, and ten minutes pass, which is above the longest pass the run log has recorded. The shape of a pass's run name is stated once, where the worker that writes it and the surface that reads it can both reach it, because the pass's own row, which is the only row carrying the name it was for, is written last. Two alternatives were rejected. Holding the connection open and pushing each section down it would make the read surface keep state per reader and would leave a pass unwatchable the moment the page was reloaded. Writing a progress row from the surface would make the read surface a writer, which it is not (see: The name page's control starts the worker's research verb, and the read API writes nothing it starts). (see: A screen reads and renders, and computes nothing)

**A name's page for an earlier night is what the store held that night** Section 15.9 gives the name page a second route carrying a date, and what that page draws is the evening's own figures: every read it makes is bounded by that night, so the bars stop there, the bands, the plan and the profile are the ones that night computed, the filings are those filed by then, and a written section is the newest version accepted on or before it. The evening drawn is the newest the listings hold on or before the date asked for, so a Saturday, a holiday and a night that never ran each answer with the evening before rather than with nothing, and the page says which evening it drew and links to tonight's. Two things the page does not do. It offers no control and states no spend: a research pass writes about the company now, not about that evening, and a staleness verdict is a statement about today. And it does not restate what followed as it was known then: how a setup resolved is something the store learned after that night, and the listing history draws it as it stands, beside evenings the page has bounded to that night. The walk stays in the evening, each neighbour linked on the same date, so a pass through one evening's list is one pass. Bounding the reads rather than replaying the rules is what keeps the page a read: replaying would be a second implementation of the arithmetic and would produce figures the store never held (see: A screen reads and renders, and computes nothing).

**A screen is read at the width of the screen it is read on** The column every screen is drawn in is the width of the screen, up to a ceiling that is the widest picture a screen draws with the card's own padding and the page's gutter around it. Above that ceiling a line of prose runs past what an eye follows; below it the column is the screen's own width less the gutter, so a narrow screen is filled rather than shown a page laid out for a wide one. A table cannot be narrowed past its columns, so every table a screen draws is read in a box of its own that scrolls sideways rather than moving the page. A picture can be narrowed, and is: the chart and the volume profile beside it are drawn at a size of their own, which is what the ceiling holds, and below the ceiling the pair is scaled down together, each keeping its own ratio and both anchored at the top, so a price is at one height in both at every width. Anchoring them in boxes of a fixed height instead put a price fourteen points higher in the profile than in the chart at a column of 974, which is the fault this rule is written against. A long run of characters with nowhere to break, a document's address among them, breaks instead of pushing the page. The operator found both halves on 2026-09-19: the column was fixed at a width narrower than the screen it was read on, and on a narrow screen tonight's reason totals and the run page's records ran off the right-hand edge with a row of the page behind them. Drawing the chart smaller than the space it is read in was the alternative and was what the page did, at seven tenths of its size, so a year of candles was read at seven tenths on every screen whatever the screen could show. (see: Every region is a card that states where its figures came from and how to read them) (see: Marks are defined once and every screen draws from that list)

**A researched name is one holding an accepted section besides the key under each figure** The researched list, the line under a researched name in the universe table and the masthead search's labels count a name as researched when it holds a section the claim checker accepted other than the key under each figure, dated by the newest such section. The key does not count because the overnight queue writes it again each night for every name (see: The key under each figure is dated by the night whose figures it explains, written for every name each night, and drawn only beside that night's figures), and on the operator's store on 2026-09-19 it stood for 502 names where one name, NVDA, held research. Counting research passes off the run log was the other form, and it would call a name researched on a pass that wrote nothing. Every name is also found from a search box in the masthead of every screen, by its ticker or its company's name, which the operator asked for on 2026-09-19 because a researched name could be reached only by knowing its ticker or finding it in the universe table.

**The queue page states when each request will be written** For every request it lists, the queue page states when its pass will start or started and when it is expected to end or ended, in New York's time with the offset named and UTC beside it. A request asked in a peak window says the instant the window ends; one behind other requests says its estimated start, the median duration of the passes the store holds, stated with how many passes that is over, and a store holding none says it cannot estimate rather than guessing. The operator asked on 2026-09-23 that a reader know fully when a report will be generated, after their first request sat in the queue with no screen saying whether anything would write it.

**A name's group is its industry where at least five other members share it on the session, and its sector otherwise, and every surface that uses it says which and how many** One rule, declared once and used by the move table and the peers table, so the two never disagree about who a name's group is. Five is the operator's proposal of 2026-09-23: an industry of fewer members makes a median of a name or two, which reads as a comparison without being one, so a smaller industry gives way to its sector. The name itself is never in its own group, a group holding nobody says so rather than drawing an empty median, and the floor is a constant section 17 pins from 11.5.

**A large move is shown beside its group's median move over the same sessions** The median of the members' moves from the close before the move's first session to the close of its last session, with members missing either close left out and counted. A median needs no market capitalisation, which the store does not hold nightly, and it is the same shape as the night's own median the volume-against-the-night candidate compares a name with. The move annotator writes it on the move row it already owns, so a move and the median it is read against are one row and cannot drift apart.

**Peers are shown by price alone, in section 2 beside the move table** The columns are the close, the distance below the stored year's high, the 60-session return, the trend state and the distance row mark. No multiple and no yield, because a peer's fundamentals exist only if someone opened it, which is the population problem the fundamentals ruling names (see: No reading of the fundamentals fires a reason, gates a tranche or draws a panel). Rows go in ticker order with the name's own row marked, so the table lists and does not rank. The two readings are the move annotator's, written in a table of its own, on the operator's ruling of 2026-09-23: the indicator engine is one of the sources every candidate evaluator's version pins, and a reading written there would move the versions the three candidates registered that day stand at.

**A page ranks no company as an investment, and a rank it draws is a return's place among the members' returns** Phase 12's second gate reads relative strength, each member's return over 63 and over 126 sessions placed among every member's, and the list orders by reward to risk, then relative strength, then band strength. A name's page opened by saying it does not rank the name against any other, and hiding the rank there would hide the input that put the name on the list. The operator ruled on 2026-09-24 that the page draws both returns and each one's place, and that its refusal and section 15.14's say one thing in one wording: no page ranks a company as an investment, and the one kind of rank a page draws is a return's place among the members' returns, which is a fact about the chart. The sentence is held once in the code that draws it, so the page and the two sections cannot come to say different things.

## Where it runs

**The whole system is a checkout and one database file** Everything the system owns lives in a single database, including the text of every source document it has fetched and the candidate register. There is no directory of loose artefacts to keep in step with it, so moving an installation means copying the code and one file.

**A writer waits up to ten minutes for another, and a pass stores what it fetched in one write** The night, a research pass started from a name page and the overnight queue all write the one store file, and it takes one write at a time. The driver a connection is opened through waits thirty seconds for another writer before a statement fails, and on 2026-09-18 that failed a night: a pass run beside the overnight queue stored the 7,206 documents it had fetched one to a write, each a disk sync on the operator's spinning disk, which held the store for about twenty minutes with no turn the queue's writes could take, and the queue failed on "database is locked" after writing the key for 207 of the 431 listed names. The other direction was open too: a night's stage holds its write for minutes on that disk, the levels for 218 seconds at the longest, so a pass started during one would have failed on its first write. So a pass and a theme pass store the documents they fetched in one write, as the facts assembler has stored its files since the first night over the index, and every connection the worker and the store open waits up to ten minutes for another writer, longer than any stage the night has measured, except in the sources a ladder rule's code version or a candidate evaluator's version pins: an edit there moves the version the open windows and the registered candidates are measured under, and those sources are the night's own stages, which beside a pass wait for one write. Past the wait a statement fails, naming the lock. The read surface is not a writer and keeps the driver's own wait.

**Nothing is written against one operating system** The runtime, the database and every provider call already behave the same on Windows and on macOS. Two things would break that if left implicit, so they are decided here: no path is hardcoded, the data directory being configuration; and the nightly run is a single command that an external scheduler invokes rather than a service that schedules itself, because scheduling is the one genuinely platform-specific piece. The model endpoint is a URL in configuration for the same reason.

**The local and paid boundary is a property of the machine, not of the design** The split in section 13 is set by how much context the local model can hold, so different hardware moves it, and a machine with more memory available to the model can take more of the work locally. The comparison against the fixture is re-run after a hardware change rather than the earlier boundary being assumed to still hold.

**Windows is verified on the operator's machine, and the hosted runners are macOS and Linux** The suite ran on a hosted Windows runner on every push and every pull request from 0.4 until 2026-09-15. By then that leg took from 15 to 33 minutes, against about a minute for the same suite on the operator's Windows machine and two on the hosted macOS runner, and it had grown from about three minutes at phase 4 while the other two stayed flat, so every merge waited on the one leg. The operator ruled it removed on 2026-09-15. Windows stays a supported platform and is verified where the nightly job runs: `tools/ci.ps1` on the operator's machine, green over the tree being merged, and recorded in the checkpoint's PROGRESS entry in those words, which `two-platform` reads. What is given up is a Windows run on a clean image for every push. A fault that shows only on a fresh Windows install, a tool the operator's machine happens to have or a default it has changed, is now found on that machine or not at all, and a change merged without anyone running the script has nothing on Windows behind it, which is why the merge rule names the run. macOS keeps its hosted leg and Linux its case-sensitivity job, and a hosted Windows leg returning is a new decision rather than an edit to the workflow.

**Every entry written since the hosted Windows leg was removed records the Windows run, whatever the entry lands** `two-platform` read the Windows run off the entries that land a checkpoint (see: Windows is verified on the operator's machine, and the hosted runners are macOS and Linux), which left a merge whose entry opens "Not a checkpoint entry" to the merge rule alone, and no check reads the merge rule. Such merges carry code: the 6.0, 7.0 and 7.2 rulings and the 7.0 and 8.0 planning passes each changed files under `src`, 8.0's under a decision of its own, so what an entry was called decided whether its code owed a Windows record. The merge rule names the run for every merge, so the check reads every entry written after the last one a hosted Windows leg ran for, a planning pass, a ruling, a correction and a sign-off among them, split out of the record by the reader `DuePoints` lands checkpoints from, so what an entry is cannot be read two ways. It moves no verdict: over the record at 44b7f6d each of the 23 entries written after that one says `tools/ci.ps1` ran green, 3 of them landing no checkpoint. Ruled at the 8.0 correction.

## Process

**A frozen fixture per checkpoint, and the harness decides sign-off** Live data changes nightly and cannot be diffed; the committed fixture can. UNEXAMINED never counts as a pass.

**The fixture is diffed on each stage's serialised output, of which the facts file is one** Prose varies between runs and layout changes constantly, so diffing rendered output would fail for reasons that do not matter and pass over reasons that do. That argument is about the rendered page and holds; naming the facts file as the thing diffed narrowed it to one stage that does not exist until 5.3, leaving every expectation from 1.2 onward with nothing the record said to diff against. Each stage serialises what it produced, the membership set, the stored bars, the refusal record, the indicator rows, and the fixture diffs that. Not the store itself: diffing rows couples every expectation to schema shape, so a migration adding a column breaks expectations about values that did not change, and the fixture stops being a check on behaviour. Supersedes The fixture is diffed on the facts file, not on a rendered page.

**A research pass is recorded per section call, keyed on the canonicalised request** A pass has to replay without a network or no fixture covers research at all, and section 19.2 asked for a recording model endpoint without saying what a recording is. It is what every other outward call here already is: a captured request and response pair committed with the fixture, named in the manifest, and read by the parser the live client uses. Two things about its shape are choices a later session could reverse invisibly. It is keyed on the request rather than on the order the calls were made, because a pass that writes its sections in a different order must still replay, and a sequence-keyed recording passes on the run that produced it and fails on every other. And there is one record per section call rather than one per pass, which is what per-section dating in the research record already requires and what lets one section be run on two models over one evidence set (see: A research record is written and dated per section, not as a whole). The key is a hash of the lane, the section, the model identifier, the identifiers of the documents handed in and the prompt text, so a recording answers the request it was made for and nothing else. A replay asked for a request the recording does not hold refuses by name rather than reaching the network, which is the property the recorded feeds already have by holding no client.

**Every phase opens with something to look at** The first checkpoint of each phase renders a page. A report about row counts is not visible output.

**Facts are declared once and cited by descriptive name** Behaviour lives in this document, data shapes in section 16, and no component, decision, invariant or check carries a code number. Citations must match a name exactly, so a paraphrase fails loudly rather than resolving to the wrong thing.

**Specifications are edited clean; the changelog records what they said before** No document carries struck-through text. A superseded decision moves to section 22 with its reasoning.

**A transaction taken for speed is not asserted, and a scan that would need twelve exemptions is not written** Five stages write in a loop and open a transaction, and the question the phase 5 sign-off left open was whether a check should assert that they do. One of the five carries a correctness property and is asserted as one: `ForwardReturnFiller` interrupted partway leaves some horizons written and a base rate belonging to none of them, which is the shape the refetch's atomicity was already ruled on, so a behavioural test induces the interruption and the stage is asserted to leave nothing behind. The other four are speed at a row count the committed fixture cannot reach, and there is no reading of them a text scan can make: all sixteen writers loop, so a scan keyed on the loop reports every one of them and a scan keyed on the transaction reports none, and what distinguishes a stage that needs one is the number of rows it writes, which appears in no file's text. A scan reporting sixteen findings of which twelve are not defects is made green by exempting twelve by name, and an exemption list doing all of a check's work is the under-reporting failure `coverage-reported` exists to catch, arriving inside a check nobody needed. So the check is not written and this entry is the reason, which is the form the uncited decisions were closed on at 3.0: a citation cannot be placed at a rule that does not exist, and a check cannot be written for a property no instrument reaches.

**The pass that plans phase 8 carries the phase 7 sign-off's repairs and its rulings' code** A planning pass builds none of the phase it plans, and this one still edits shipped source: the phase 7 sign-off carried repairs that are a line of code each, and four of the rulings 8.0 owes are a behaviour rather than a sentence. The operator ruled on 2026-09-15 that they land in the planning pass rather than being spread across the loop's checkpoints, because a repair parked in a checkpoint that does not need it is a checkpoint doing two jobs, and a repair left to a later pass runs on every night until then. The exception is named here so a later session reading that planning a phase builds none of it, against a pass that touched shipped source, finds the rule rather than an inconsistency. What it does not license is building a checkpoint of the phase in the planning pass: no checkpoint of phase 8 is built by 8.0, and the pass that makes these edits may not sign the phase off.

**Code a pass that lands no checkpoint carries into shipped source meets the done conditions a checkpoint's code meets** The pass that planned phase 8 carried the phase 7 sign-off's repairs and four rulings' code under a decision that said what that exception did not license, building a checkpoint, and not what the code still owed (see: The pass that plans phase 8 carries the phase 7 sign-off's repairs and its rulings' code). Its entry opened "Not a checkpoint entry", so its code owed no derived expectation and no Windows record any check read, and three of the phase 8 sign-off review's findings arrived that way: a prompt still asking for a quarter over the twelve-month figures the pass began carrying, a branch of the sentence the pass wrote for a name with no facts file that no test asserted, and the worker's reading of a named session against its store that no test ran. So code a planning pass or a ruling carries into shipped source is held to what a checkpoint's code is held to, and its entry records the same: the suite green with its count, the Windows run, a derived expectation added to the fixture or a row carried by name, a mutation chosen by a rule stated before its run and shown red, and the entry written before the run that verifies it. A correction to that code is labelled for the checkpoint the pass belongs to, as `Phase 8 / 8.0 - correction` is. What such a pass lands does not change: a planning entry still opens "Not a checkpoint entry" and lands its checkpoint and never its phase. Ruled at the 8.0 correction.

**A verdict note states no count of the row's own parts** A note in the harness's placement map says what the check asserts about a row, and until 8.0 some of them also counted the row's cells, the stores a component declares or the columns a table has. A count typed into a note is read against nothing: the phase report prints it whatever the row holds, which is how the read API's note said twelve cells read over a row the series state read had made thirteen, and how two notes said all thirteen cells blank over rows that had grown. So a note states no such count, the count lives in the row where it cannot go stale, and a guard refuses a number standing next to cells, stores, columns or reads in any note. Read over all 346 notes when the rule was written, nine stated such a count, and 61 others carry a number that is a rule figure the row itself states or a population of the fixture, which the claim's own check reads. Ruled at 8.0.

**A written rule never blocks a real fix** If a rule in this document prevents correcting a genuine defect, the rule is the defect and is amended in the same pass, with the change stated plainly.

**A due point names a checkpoint that exists wherever its phase has been detailed** The reader that decides whether a due point is in the plan accepted the phase alone, so any point inside a phase the plan has passed whether or not its checkpoint was written. That fallback is argued for and stays where it is argued: a later phase gets its checkpoints only at the previous phase's sign-off, so requiring a checkpoint by name would forbid the check roster from naming anything past the phase in hand. The argument does not reach a phase whose checkpoints are already written. Inside a detailed phase the point exists or it does not, and a due point naming one that does not is an obligation owed at a place nobody created and a claim out of scope until a checkpoint that will never land. Found at the phase 5 sign-off, where "5.8" read as in the plan before 5.8 was written and the suite stayed green on a checkpoint nobody had created. Detail is read from the plan itself, being any checkpoint heading inside the phase, rather than from a list of detailed phases kept beside the reader, which would be a second statement of the same fact and would go stale the moment a phase is planned.

**Phase 6's carried items are built as phase 7, and the improvement loop is phase 8** The phase 6 sign-off carried three items to 7.0, which was then the improvement loop's planning pass, and the first of them, the operator's ruling on a suspect name's own surfaces, was committed under 7.0 on 2026-09-14 by the clause in CLAUDE.md that files work under the checkpoint that owes it, while that phase had not started. The operator read the label as saying phase 7 had begun, and ruled the same day that the carried items are phase 7: that ruling at 7.0, the due-point reader's repair at 7.1 and the phase report at 7.2. The improvement loop moves to phase 8 with every checkpoint, due point, placement, roster row and line on a page that named it. The plan moved to meet the label rather than the label to meet the plan, because history already on `main` is not rewritten, and a label between two phases is not one the plan can carry: 6.5 already names the staleness judge. This rules phase 7 and changes no convention; the clause stands.

**The solution file takes the SDK's current format** The .NET 10 SDK writes `.slnx` and no longer writes the older `.sln` by default, so the solution is `EquityBrief.slnx`. Pinning the older format means every session that regenerates the file has to remember a flag to get it, and a format the tool has stopped writing drifts out of support with nothing announcing it. The corpus names the file in the layout block and in the Commands table and the suite finds the checkout by looking for it, so a format change is a rename in three places and the third one fails loudly if the other two are missed.

**The report generation the operator asked for is built as phase 9, and phase 8's sign-off is owed after it rather than before it** The operator asked on 2026-09-20 for three things a name's page cannot do: ask for a report from the row on tonight's list rather than from the name's own page, a request that outlasts the press rather than a process started beside the read surface, and a statement at the head of the page of which lane would write one. None of the three is a defect and no checkpoint owes any of them, so they are a phase rather than a correction (see: Phase 6's carried items are built as phase 7, and the improvement loop is phase 8). The merge section owed a phase's sign-off before the next phase's plan, and the operator ruled the same day that phase 9 opens first. What moves is the order of the two and nothing else: the sign-off stays owed on phase 8 as a whole, is named as outstanding in the ruling's own entry, and is discharged by nothing phase 9 does. The order moves because a sign-off needs a session that has committed no code while building does not, so holding the work behind it spends a session at the point the operator wants the page working, and the sign-off reads the same record whether it runs before phase 9 or after. The cost is named rather than left to be found: phase 8's defects stay unfound for as long as the sign-off waits, and one phase 9 builds on top of is dug out of more code than it would have been.

---

## Previously decided

**A candidate's verdict is read only at looks fixed when it is registered** A fixed-level test repeated as setups accumulate promotes a worthless condition far more often than its level says: at a 35% break-even and a third of 0.05, one read at 250 setups has a size of 1.28%, while reading after every three resolved setups reaches 4.9% by 500, 7.4% by 1,000 and 9.9% by 2,000, and three worthless candidates read nightly have about a 21% chance that one is promoted by 1,000. So each register row carries its looks at registration: at 8, 12 and 16 non-empty blocks, information fractions 0.5, 0.75 and 1.0, triggered by the block count alone and never by where the figure stands, with the level spent across them by a Lan-DeMets function of the O'Brien-Fleming type. The boundaries are derived on the sign-flip null by simulation over stored listings, with the method, the draw count and the seed pinned, because independent increments are not established under overlapping windows. Under independence at 500, 750 and 1,000 setups the crossing win shares would be 42.0%, 39.6% and 38.4%, overall size 1.52% and power 79.6% against a 4.6-point excess. The first look cannot promote: the level released at a fraction of 0.5 is 0.00071, below the floor of 1/256 that 8 blocks allow, so it can only retire; the earliest promotion is at 12 blocks, 756 sessions, about three years, and the maximum at 16, 1,008 sessions, about four years, each plus up to 63 to resolve. Evidence: Armitage, McPherson and Rowe (1969) on repeated significance tests on accumulating data; Lan and DeMets (1983) on spending functions chosen before monitoring; O'Brien and Fleming (1979); FDA (2019) guidance on adaptive designs, that analysis times chosen from results inflate error and that the maximum is not extended. Judgement: the three looks and their spacing.

Superseded on 2026-09-22 by **A candidate's verdict is read only at looks fixed when it is registered, with each look's boundary found over every sign vector its blocks allow**. Everything this entry ruled stands: the looks, their spacing, the spending function and the first look being unable to promote. What changed is how a look's boundary is found, from a simulation over stored listings to an enumeration of every arrangement of the blocks the look holds, which is exact at sixteen blocks and assumes nothing about what the setups inside a block did to each other.

**Nothing a candidate or a version is registered with changes while it runs, and settling a proposed number is a new registration** The calibration's method, inputs and cap, the cost and its sensitivity, the spending function, the looks, the graph, and the boundary simulation's method, draw count and seed are fixed in the register row or pinned in code before the registration command runs, because the running figure is shown every night and a choice made after it has been seen is a free parameter. A proposed number is settled only by a new registration or a new version, with its own window and counted in the lifetime count, and any reading that motivates it happens at a look. The trend confirmation's two nights may be settled from flip-back counts, which are labels and not outcomes, and a changed count is a new version. The margin by which version A must beat version B is fixed at 5 points before scoring and has no settling row. Every threshold phase 10 chose was chosen on per-night counts, with no forward return and no setup outcome read, which is what keeps three an accurate count of what was tried. Evidence: Harvey, Liu and Zhu (2016) that significance hurdles must rise with how much was tried; Sullivan, Timmermann and White (1999).

Superseded on 2026-09-22 by **Nothing a candidate or a version is registered with changes while it runs, and a proposed number is settled only by a new registration**. What this entry protected stands word for word; the list of what is fixed before a registration names a boundary simulation's draw count and seed, which the boundary no longer has.

**Tonight's list orders by the reasons that fired and then band strength, which are facts about the chart** Section 15.7 orders tonight's list by how many reasons fired and then by band strength, and section 15.14 said no screen ranks names by anything other than distance to a level, so one of the two had to change; the approved design keeps the first. How many reasons fired and how strong a band is are, like the distance to a level, facts about a name's own chart, and none of the three says one company is better than another, which is the line the scope decision draws (see: Improving what surfaces a name is in scope; ranking names against each other is not). So the universe orders by distance to a level, tonight's list by reasons fired and then band strength, and no screen orders names by anything read off the company rather than its chart. Ruled on 2026-09-19.

Superseded on 2026-09-22 by **Tonight's list breaks a tie in fired count by the plan's reward to risk, and a row with none is drawn after every row with one and says why**. What this entry protected stands: the fired count leads, and no screen orders names by anything read off the company rather than its chart. What it decided, band strength as the tiebreak, is replaced by the plan's reward to risk, and each listing now records its band strength so the order this entry chose is the benchmark the new one is measured against.

**Improving what surfaces a name is in scope; ranking names against each other is not** The test for any future change is whether it decides which chart is worth opening or which stock is better than another. A scoring loop makes that line easy to cross by increment, so it is stated here rather than left to judgement.

Superseded on 2026-09-21 by **Tonight's list selects the stocks worth buying or paying attention to that evening, and the improvement loop exists to make that selection better**. What this entry protected stands where that entry names it: no model and no fundamentals in the decision, no sizing, and a reason's record never beside a ticker. What it decided, that deciding which stock is better than another is outside the tool, is replaced, because the operator ruled that the list exists to select the stocks worth buying or paying attention to, and a selection of that kind is such a decision.

**The name page's control starts the worker's research verb, and the read API writes nothing it starts** Section 15.9 puts a control on the name page that writes a name's research, and the page is served by the read API, which holds no reference to the worker and writes no store a pass writes. The worker is the one writer, and a pass is one of its verbs as the fundamentals fetch is. So the control asks the read API to start the worker's research verb for that name as a process of its own, from the checkout the read API runs in, and the read API returns at once: it hands over the ticker and the options the operator chose, reads nothing the pass writes and waits for none of it, and the page reads the pass's rows off the run log as they land. The ticker is refused unless the index holds it, and the arguments go as a list and never through a shell. Two alternatives were rejected. A request store the read API writes and the worker reads would make the read surface a writer, and a control that only showed the command would be a control that writes nothing. (see: One application, not a file per report)

Superseded on 2026-09-20 by **A request the page writes and the worker drains is what starts a pass, and the read surface writes the ask and never the research**. What this entry protected stands: the read surface holds no reference to the worker and writes nothing a pass writes. What it decided, that the surface starts the worker's verb as a process of its own, is replaced, because a process leaves no row and the operator asked for a queue that can be read and taken from.

**A request the page writes and the worker drains is what starts a pass, and the read surface writes the ask and never the research** A press on a screen writes one row to a request store and returns, and the worker takes the oldest request nobody has started and runs its pass. The operator asked on 2026-09-20 for a report to be queued from tonight's list, for a screen showing what is queued, and for a report that has not been generated to be taken out again, and none of the three is possible against a process started beside the read surface: a process leaves no row, so nothing can list it, nothing can withdraw it, and a press that started nothing is indistinguishable from one that started something that died. The superseded entry rejected a request store by name, on the ground that it would make the read surface a writer, and that ground is met rather than waved through. One writer per store per operation is per operation: the surface inserts a request and withdraws one nobody has started, the worker claims a request and says what it came to, and no operation has two writers. The surface still holds no reference to the worker and still writes nothing a pass writes, which is what that entry was protecting: a request is an ask, and the research it leads to is the worker's. What is given up is that a press no longer produces a pass by itself, so a queue nothing drains is a queue that sits, and the screen says so rather than a reader inferring it from a report that never appears. Supersedes **The name page's control starts the worker's research verb, and the read API writes nothing it starts**, moved below with its reasoning.

Superseded on 2026-09-23 by **A press writes a request and starts the worker's drain as a process of its own, and every pass waits for the off-peak hours**. What this entry protected stands: a press writes a request before anything else, the queue screen lists it and takes back one nobody has started, and the read surface holds no reference to the worker and writes nothing a pass writes. What it decided, that nothing on a screen starts a pass and the worker drains the queue only when a person runs the drain, is replaced, because the operator asked for a request to be processed as soon as it is queued.

**Every part of a page states where it came from and as of when** Computed tonight, fundamentals as of a filing date, research as of a date and a model.

Superseded on 2026-09-19 by **Every part of a page states where it came from and as of when, and a written section when it was written rather than which model wrote it**. Everything this entry decided stands but the model's name, which the store and the element keep and the page's words no longer carry.

**The overnight queue writes the local lane's sections that rest on no document, and the paid model is for names you get serious about** After the nightly arithmetic, the local model works through the names on tonight's list whose research is missing or stale, in order of how many reasons fired, and writes the local lane's sections that rest on no document, at no cost. The night fetches nothing for a name: step 17 is carved out of the zero-model-call rule alone and reaches the local model and nothing that fetches (see: The night's zero-model-call rule bounds the arithmetic, and the overnight queue is carved out of it by name). A section that rests on documents rests on ones a pass fetched and stored, and the source store ties a document to a name through nothing but a section citing it, so the queue hands the writer no document and asks it for none of those sections. In this machine's lane what the queue writes is the key under each figure. What the company sells and the segment commentary are written by the pass an open starts, which fetches the company's own filing, so a listed name nobody has opened opens in the morning with its key drafted and the rest written when it is opened. A name whose only outstanding work is a section resting on documents is not queued at all, rather than counted every night as a pass that could write nothing. Handing the writer the documents a name's stored research already rests on was the alternative, and it was not taken: a stale section rewritten tonight from documents older than the event that made it stale is dated after that event while reading nothing the event produced, so the judge would read it as research that stands (see: Research goes stale on an event dated after the section was written, and a news spike is dated by the session it began). Opening anything else, or pressing refresh on a name whose local draft is not good enough, runs a paid pass on demand, and the record already stores which model wrote each section, so the report says which you are reading and offers the better one. Supersedes **The overnight queue writes a free first draft; the paid model is for names you get serious about**, moved below with its reasoning.

Superseded on 2026-09-19 by **The overnight queue writes the local lane's sections that rest on no document for every name, and the paid model is for names you get serious about**. Everything this entry decided stands but two things: the queue works through every name rather than the names on tonight's list, which the 6.10 correction of the same day built and this entry should have followed then, and the page says when each section was written rather than which model wrote it.

**The key under each figure is written for each night's facts file** The key under each figure explains the night's figures, the close, the averages, the levels and momentum among them, and those move every session, so a key written on an earlier night states figures the page no longer shows: NVDA's, written on 2026-09-14, quoted a close of 210.96 on a page whose close was 219.34. So the key is written for each night's facts file: a pass on a later day writes it again, the overnight queue writes it again for every listed name whose key was written on an earlier night, and a key the queue has not written again is drawn beside the night it was written for, as every written section is. This is not a timer. The event is the night's facts file, which is the only thing the key is about, and every other section still stands until a filing, a passed earnings date, a news spike or the operator (see: Nothing expires on a timer). It costs the local model a pass a listed name a night, which 6.10 measured at 3.35 seconds on average, inside the queue's hour for the whole index, and nothing is spent.

Superseded on 2026-09-19 by **The key under each figure is dated by the night whose figures it explains, written for every name each night, and drawn only beside that night's figures**. Everything this entry decided stands but three things: the queue writes the key for every name rather than for the listed ones, a key is dated by the night of the facts file it was written from, and a key written for another night is not drawn beside a night's figures.

**Guidance is stored as management's own prose and never parsed into a figure** Section 4 asks for the guided quarter from the earnings release exhibit, and what the exhibit carries is a sentence. Measured at 6.2 over the most recent results announcement of twelve index members: all twelve filed an exhibit, five state guidance under an emphasised heading whose whole text ends in the word outlook or guidance, four of those five state at least one numeric range in the prose under it, and at least two of the other seven state guidance inside an ordinary paragraph or a quotation with no heading anywhere. No filing carries it in a structured field, which is why it is read from the exhibit at all. So a heading locates the passage for five of twelve and a pattern over the prose produces a figure for four, and a guided-quarter column filled that way would be empty for two thirds of the index while reading as a company that guided nothing. The rule this corpus already holds is that a figure is computed from stored data or copied from a payload with the filing date it came from, and a range extracted from a sentence by pattern is neither: it states no population and the same pattern that finds it in one filer's wording misses it in the next (see: Code owns every number). So the archive stores the exhibit's passage, its heading, the document it came from and the date it was filed, and the numbers section shows management's own words rather than a figure. A passage no heading located is recorded as not located and never as guidance not given, because the exhibit is the evidence for which of the two it was and the seven filers above are mostly the first. What a guide-against-actual reading would need is a figure, and that is where a later pass may reverse this with more filers behind it (owes: The computed fundamental panel, and whether a fundamental state may fire a reason or gate a tranche).

Superseded on 2026-09-18 by **Guidance is stored as management's own prose, and the facts file carries each figure the passage states as the claim checker reads it**. Everything this entry decided stands but that no figure from the passage reached the facts file: each figure the claim checker reads in the located passage now does, as the checker reads it, and still never as a guided value.

**A research pass reads a name's news inside each stored move and since the company's own filing, and hands each section the documents code picks from it, the company's own filing first** Supersedes **A research pass hands each section the documents code picks for it, the company's own filing first**, moved below with its reasoning. A pass fetches more than any section can be handed. 6.8 measured one name's stored year at 656 articles and 3,353,269 characters of their text in one response, which is past what a section's prompt should carry and past what a reader would want a section resting on, so what each section is handed is a rule, and code applies it, because choosing the evidence is choosing what a sentence may say (see: Code owns every number). What the company sells and the segment commentary are handed the company's own results release, from the filings archive. The cause of each large move is handed, for each stored move, the documents published inside it that name the fewest companies and then the earliest, at most two a move. The sections built across the evidence, being the dated calendar items, the two cases, the risks and the short version, are handed the company's own filing and the documents published on or after the day it was filed that name the fewest companies and then the newest, at most six, and where the company has no filing a pass can read, the newest of the quarter back from the night. How many companies a document names is read off the listings the provider attributes it to, which says how much of it is about this company: of KEYS's 656, 207 name no company but KEYS, the widest names 50, and the company's own filing names one. A pass reads the news those windows hold and no other: the span of each stored move, and the days from the company's own filing to the night, overlapping spans read once. It read the stored year as one request until 6.11's production run asked for MSFT's, where the provider still had more after twenty pages of a thousand articles, the most a query reads; the feed refused the window with an error the pass did not catch, so the pass stopped with nothing on its row, and a large company's year stored whole would be tens of thousands of documents nothing hands a section. A window the provider has more of than a query reads is refused on the feed's side as it was, is recorded on the pass's row as unread, and the sections are written from the windows that were read. Two alternatives were rejected. A judgement of relevance by the model would put the model in charge of its own evidence, and a filter on the company's name in the headline matched 338 of KEYS's 656 and cannot be written for a company named with a common word. (see: The model never fetches; components fetch and hand it documents) (see: A cause of a move rests only on a document published inside that move)

Superseded on 2026-09-18 by **A research pass reads a name's news from the last three months alone, inside each stored move and since the company's own filing, and hands each section the documents code picks from it**, which keeps this entry's rule and adds the three-month bound the operator ruled, a move's episode read from its newest documents, the days since the filing read in stretches, and a title naming the company ranked first.

**A claim is a sentence, and every sentence in a researched section names the document it rests on** The two decisions above say every number in prose exists in the facts file and every researched claim names a stored source, and neither says what a claim or a number is when a checker reads a paragraph, so a later session could draw either line anywhere and nothing would show it. A claim is a sentence. Every sentence of a researched section carries a citation, written as a D and the document's position in the section's own source list in square brackets, and the citation has to resolve to a stored row admissibility admitted. A position rather than an id, because a model copying a thirty-two character id into prose is a model given the chance to get one character wrong. The key under each figure is the one section held to the number rule alone, because the lane table says it is a fixed explanation over known values, and asking it to cite a document would be asking it to invent a source for arithmetic.

A figure is a rounding of a value the facts file holds, at the precision the prose itself states: half a unit at the last decimal the prose writes, scaled by the prose's own unit word, so $109.4 billion is a rounding of 109417000000 and $110 billion is not. A percentage is also tried against a value stored as a fraction, which is how the margins are held. The match is on magnitude and not sign, because the sign of a move is carried by the words around it and a signed match would refuse every "fell 3.2%" written from a stored fall, and it is on existence rather than attribution. Some numbers are not figures. A four-digit year standing alone carries no claim about a value. A quarter or half label, a fiscal year, a time of day and an ordinal from first to tenth say which period or which place, and a wrong period is caught by the figure beside it failing to match. The length of a window, the 200 of a 200-day average, has to be a number the facts file carries in the name of a figure or as a count it holds. A date has to be one the file holds. The S&P 500 and the twelve months a filing reports over are named as the two phrases whose digits and words are neither. A number written in words from eleven up, or a scale word standing on its own, is refused as a figure nothing can match, while one to ten are ordinary language and are not read, which is a stated cost: "two segments" where there are three passes.

The checker writes two outcomes and two waypoints. A first refusal is `rejected` and the writer may try once more as the next version; a second consecutive refusal on the same as-of date is `fallback` and the section is left out with both attempts' offending text kept on their own rows. A researched section whose source list holds nothing admitted goes to `fallback` on its first check with the reason that no admissible source was found, because a rewrite cannot create a source. A version following a fallback is a fresh first attempt.

The reader was measured before anything rested on it, over the four captured articles, the two filed releases and 411 articles from one live news request, being 15,412 sentences and 11,727 figures, and two findings from that measurement are the reason this entry states its limits in numbers. The sentence reader holds: of the 15,412, 700 are three words or fewer and the ones read are real short sentences and page furniture rather than splits at an abbreviation. The measurement found three misreads and each is repaired and asserted: a percentage before the word week read as a window, a time of day read as two figures, and a strength of 2 matching 200% through the fraction reading. And the number rule is a strong test for a figure stated with decimals and a weak one for a whole number. Against AAPL's facts file of 38 values, 1,392 of the 11,727 figures, written by nobody with that file in mind, pass by coincidence: 1,301 of 7,939 whole numbers, 76 of 2,091 at one decimal, and 14 of 1,547 at two, some of which are AAPL's own figures. A model inventing "revenue grew 12%" passes where any value near twelve exists. That is not repaired here, because what would repair it is attribution, a figure tied to the fact it rounds, and the prompt that could require prose to name its facts is written at 6.6, which is where the first model-written sections exist to measure it against (owes: A whole-number figure tied to the fact it rounds, measured against written sections). (see: Every number in written prose must exist in the facts file) (see: Every researched claim must name a stored source document)

Superseded on 2026-09-18 by **A claim is a sentence, every sentence in a researched section names the document it rests on, and a window written in words is read as its number**. Everything this entry decided stands but one sentence: a number in words from eleven up was refused whatever followed it, where a window written in words is now read as its number and held as a window in digits is.

**A registration names an evaluator the code carries, and its version is a hash of that evaluator's own source** A registered candidate carries the name of an evaluator, the parameters it will be run with, and that evaluator's version, so the row says what will run rather than describing it. A rule written only in prose is re-implemented by whoever scores it, and what they implement is whatever they read the words to mean, which is the thing pre-registration exists to stop: the registration and the thing tested come apart with nothing able to say they have. The version is the hash rather than a number somebody raises, because a number is a second statement of the same fact and drifts the first time a parameter is tuned in a hurry, where a hash cannot. It is taken over the evaluator's source with line endings normalised to LF and any leading byte order mark removed, so the same code hashes the same on both machines and on a runner that checked the tree out with either ending, and over that source less the one line carrying the version, because a hash of a file including its own hash never settles. What this costs is that every change to an evaluator, down to a comment, is a new registration retiring the old one rather than an edit, and that is the cost being bought: a row naming code that has moved is a row naming a rule nothing runs, and `register-append-only` fails one rather than letting a night evaluate it. Ruled at 8.3.

Superseded at the 8.3 correction by **A registration names an evaluator the code carries, and its version is the pin of every source its evaluation runs through**. The evaluator named on the row, the parameters, a pin rather than a number, the normalisation, the version line left out and a change being a new registration all stand. What changed is how far the pin reaches: this entry took it over the evaluator's own file, which left the builder's reading of a name-night, the parse of a registration's parameters and the arithmetic of the readings free to change what a registered rule fires on with the row still matching.

**A suspect name's retries are bounded, and a name whose retries are spent stays suspect and named on the run page until another action lands on it** Ruled at 6.0, which owed it from the phase 5 sign-off and was found at 6.11 not to have written it. A name whose refetch fails is marked suspect and asked for again on the nights after, with or without an action of its own, and nothing bounded how many: a failure that lasted made one per-name request on the nightly path on every night it lasted, which is the cost property the night is built around repeating with no end. So a suspect name is asked for again on at most five nights after the one that marked it, counted on its series state row, and a refetch that fails on a night an action lands on the name starts the count again, so one action costs at most six requests however long its failure lasts. Five, because every refetch already carries the retries a transient fault needs inside one night (see: A feed is tried three times with a doubling backoff, and the night has a deadline it cannot move), so a failure that survives them on six nights running is not one the seventh clears, and five nights are a week of sessions, which an outage would have to outlast. The figure is reasoned rather than measured: the operator's store holds eight runs of the check, started from 2026-09-09 to 2026-09-11 UTC, and none marked a name suspect. What a name whose retries are spent is left as is the other half, and it is ruled here because a bound alone would replace a loop nobody bounded with a stop nobody sees. It is not asked for again until an action lands on it. It stays suspect with the reason its last refetch failed for, and its figures are computed over its stored series as they were on the nights it was asked for. And the check writes its stage as partial on every night the name stays so, naming it with when it was last asked for and why, so the run page's stale and failed region lists it on each of those nights rather than on the nights it was asked for alone. Withholding its figures, as a gap withholds a name's, was not taken: section 18 has never said a suspect name's figures are withheld, a name is computed over its stored series from the night it is first marked, and a bound on its retries is not where that changes.

Superseded at 7.0 by **A suspect name is asked for again on the five nights after it is marked and weekly after that, and its own page, its row on tonight's list and the run page say so until a refetch succeeds**. The five nights, the count on the series state row, a new action starting it again, and the name kept suspect and named on the run page stand. What changed is what a name whose retries are spent waits for and where else it is seen: this entry left it unasked until another action landed on it, which for a name paying no dividend may never come, and named it on the run page alone, while its name page, its row on tonight's list and its exported report drew its figures with nothing beside them.

**A research pass hands each section the documents code picks for it, the company's own filing first** A pass fetches more than any section can be handed. 6.8 measured one name's stored year at 656 articles and 3,353,269 characters of their text in one response, which is past what a section's prompt should carry and past what a reader would want a section resting on, so what each section is handed is a rule, and code applies it, because choosing the evidence is choosing what a sentence may say (see: Code owns every number). What the company sells and the segment commentary are handed the company's own results release, from the filings archive. The cause of each large move is handed, for each stored move, the documents published inside it that name the fewest companies and then the earliest, at most two a move. The sections built across the evidence, being the dated calendar items, the two cases, the risks and the short version, are handed the company's own filing and the documents published on or after the day it was filed that name the fewest companies and then the newest, at most six. How many companies a document names is read off the listings the provider attributes it to, which says how much of it is about this company: of the 656, 207 name no company but this one, the widest names 50, and the company's own filing names one. The window a pass reads is the stored year whatever the last record's date, because a cause rests on a document inside a move up to a year back and the name's year of news is one request. Two alternatives were rejected. A judgement of relevance by the model would put the model in charge of its own evidence, and a filter on the company's name in the headline matched 338 of the 656 and cannot be written for a company named with a common word. (see: The model never fetches; components fetch and hand it documents) (see: A cause of a move rests only on a document published inside that move)

Superseded at 6.11 by **A research pass reads a name's news inside each stored move and since the company's own filing, and hands each section the documents code picks from it, the company's own filing first**. What each section is handed stands. What changed is the window a pass reads: this entry read the stored year as one request, and a large company's year passes the most pages a query reads, so a pass reads the news inside the windows the rule hands a section from.

**The overnight queue writes a free first draft; the paid model is for names you get serious about** After the nightly arithmetic, the local model works through the names on tonight's list whose research is missing or stale, in order of how many reasons fired, and writes their narrative sections at no cost. In the morning those names open complete. Opening anything else, or pressing refresh on a name whose local draft is not good enough, runs a paid pass on demand. The record already stores which model wrote each section, so the report says which you are reading and offers the better one, and no new machinery is needed for either path.

Superseded at 6.10 by **The overnight queue writes the local lane's sections that rest on no document, and the paid model is for names you get serious about**. The order of reasons fired, the cost of nothing and the paid model on demand stand. What changed is what a listed name opens with in the morning: this entry said the queue writes a name's narrative sections and that such a name opens complete, which was written before a section had to rest on a document a pass fetched, and the night fetches none.

**The local lane's scope is a setting, and the overnight queue writes whatever is in it** Section 13 assigns work per section rather than per pass, so which sections the local model owns is configuration. On a machine with 24GB of graphics memory that is the short-prompt work: naming what caused a move, pulling the segment figures out of a filing, writing the figure keys. Synthesis is not in it, because a model small enough to leave room for the whole evidence set on that card is the case already named as producing something fluent that says nothing. On a machine that can hold a large model with a long context, synthesis moves into the local lane by changing the setting and the overnight queue starts writing it too. The build does not change between those two machines, and the fixture comparison rather than the memory figure decides whether a section should move.

Superseded at 6.10 by **The local lane's scope is a setting, and whatever it holds is written for nothing**. The lane as a setting, and the comparison deciding what moves, stand. What changed is the sentence saying the overnight queue starts writing synthesis once it moves into the local lane, which a section resting on documents cannot be at night.

**A paid call is priced from the provider's own token counts at the rate its own timestamp falls in** A cap denominated in money needs a price for every call, and the price is the provider's arithmetic rather than this system's: its published rates, per model, for cached prompt tokens, uncached ones and output, halved outside two fixed peak windows. Read from the provider's page on 2026-09-13 at 14:57 UTC and captured before anything rested on it, the windows are 01:00 to 04:00 and 06:00 to 10:00 UTC on weekdays, and the rates are stated beside the models in the feed that calls them. A call's price is its response's own counts at those rates, doubled where the response's own creation instant falls at peak, because the provider's clock decides what it bills and this machine's does not. Thinking is billed as output and the provider counts it inside the completion, so nothing is added for it. A model with no price in the build is refused at startup rather than called, because a call recorded as costing nothing is spend the cap cannot see, and a response carrying no counts is refused for the same reason. The prices are the provider's and move when the provider moves them; the page says so, and the feed's rates are what a change edits. The provider's name, its models, its rates and its wire sit in its one feed file, because the scan that keeps a model off the night reads every shipped file for the provider's name and carves only the files that are model feeds. (see: Code owns every number) (see: Queued work runs off-peak, and every schedule is written in UTC)

Superseded at 6.7 by **The research model is named only in configuration, and a call is priced at the configured rates its own timestamp falls in**. The pricing rule stands entire: a call's own counts at the rate its own creation instant falls in, reasoning inside the output, a model with no price refused at startup, a response with no counts refused. What changed is where the provider, its models and its rates are written. This entry put them in the provider's one feed file, and the operator ruled on the day it landed that the research model is to be switchable to any model, which a feed named for one provider carrying that provider's rates as constants is not.

**Two models for two jobs, and the research model is DeepSeek V4** A local model on the operator's GPU writes prose from numbers, which is small and free. DeepSeek V4 does research, which is a large-context job a local model does badly. Chosen on cost: a research pass is a few cents rather than a few tens of cents, which puts a year of normal use in the tens of dollars. The tier, Pro or Flash, is left to the fixture comparison rather than decided here, because the difference between them is a few dollars a year and the only thing worth choosing on is the quality of the two-cases section.

Superseded at 6.7 by **Two models for two jobs, and which research model answers is configuration**. The two jobs and the reason for splitting them stand, and so does the choice of provider on cost, which is now the value the shipped configuration holds rather than this entry's name. What the name cost was that changing model would have contradicted a decision, so a setting the operator is meant to change could only have been changed by writing a new entry.

**The night runs at a fixed UTC instant set after the provider posts the day's bulk file** Superseded 11 September 2026, at the phase 5 sign-off, by the decision that keeps the instant while nights succeed and moves it on a night refused for a file not yet posted. What was superseded is how the instant is settled: this entry set it at the posting hour plus a margin, with the hour measured from live fetches, and a scheduled fetch can only bound that hour from above, so the measurement it waited for could not arrive. The form, a UTC instant registered with whatever scheduler the machine has, stands in the decision that replaced it. The reasoning was: Section 14 scheduled the night with Task Scheduler after the US close, which named one platform's mechanism and a local time, against two hard rules at once. The schedule is a UTC instant, registered with whatever scheduler the machine has, and the application neither knows nor asks which. The instant is the provider's posting hour plus a margin, and the posting hour is measured from live fetches rather than taken from documentation, because the first thing a live feed can be asked is when the file it fetches actually appears. This entry read "measured from live fetches at 2.1" until the phase 5 sign-off, which is where the fetch first ran and not where the hour is settled: 2.1 and 2.6 each bound it, and what settles it is nights that ran on the schedule, carried as an operating row rather than at any checkpoint (owes: The provider's posting hour for the day's bulk file, measured from live fetches). The rule this decision states is unchanged, so the entry is corrected rather than superseded; what was stale is a pointer inside its reasoning and not the ruling it makes. The form is decided here and the hour is a measurement, which is why the hour is not in this entry: a number nobody has measured, written into a decision, is the shape that gets cited later as though it were settled.

**News arrives in one dated feed request and is attributed to names locally** Superseded 10 September 2026, at 5.0, by the decision on one dated query paged to cover the day, because 2.5's measurement showed one day of news exceeding one request at the provider's limit. The name said one request and the day needs several, which is what was superseded; the attribution to names locally stands in the decision that replaced it. It was deleted rather than moved here in the commit that superseded it, and was restored at the phase 5 sign-off with its reasoning as it stood. The reasoning was: The feed is queryable by date with no ticker, which was probed on the operator's key at 1.5 rather than assumed: one request over a date range returns articles for the whole market, every row carries a `symbols` array naming the tickers it is about, and the response shape is identical whether or not a ticker was asked for. So a night makes one request and fans the rows out to names in code, and the per-name cost of news is zero calls rather than one call per name. The alternative, a request per ticker, would put five hundred calls on the nightly path for the same articles and would break the rule that a night costs the same for fifty names as for five hundred. The row also carries the article text, so the source document a claim rests on arrives with the row rather than needing a second fetch, which is what makes admissibility judgeable at all without spending again.

**The stop rule depends on the trend state** In a range the stop is the range floor; in an uptrend it trails the last higher low; in a downtrend there are no tranches. Kaminski and Lo, Journal of Financial Markets, 2014, show a stop reduces expected return under a random walk and can add value where returns trend, so one stop rule for both cases is wrong in one of them.

Superseded at 5.0 by **The trailing stop is the higher of the band beneath and the last swing low**. The reasoning stands entire and what changed is the trailing half: "it trails the last higher low" names a price and not a floor, and 4.5 found that three of the four fixture names are in an uptrend and the literal reading puts their stop inside the band the first tranche sits on. Found by the phase 4 sign-off, which read the code against the corpus and found the corpus had no such rule.

**Three source lists, not one, each with a review date** Filings and company releases need no list at all, since they are fetched by ticker. A company-news list governs reporting about a company. A separate industry list governs theme material, because the publishers who write about memory contract pricing are not the ones who write about quarterly earnings. Splitting them keeps a theme search from returning earnings coverage and the reverse. Every list carries a review date, because sites are acquired, paywalled and abandoned, and a list nobody revisits silently narrows what the system can see.

Superseded at 1.7 by **Two source lists govern the open web, and the licensed feed is a channel rather than a list**. Its body always described two lists and said filings need none; the name said three, which is contradiction J. The measurement added the second half, that the lists govern the search tool rather than the licensed feed.

**The earnings trade is a second book** It has its own setups, its own stops and a horizon of days, and it never merges with the position book, because holding a momentum entry through a print is the thing the separation exists to prevent.

Superseded at 4.0 by **The second book is keyed to a dated event, and an earnings print is the only kind on file**. The reasoning is intact and carries over whole: the separation is about carrying a momentum entry through a date, which the body said and the name did not. What the name cost was a question nobody could answer from it, being whether the book may carry an event that is not a print, and section 10 had to defer that to 4.0 in the same paragraph that cited this name. The replacement answers it in the name: the book is keyed to a dated event, and the kinds it can carry are the kinds the calendar holds.

Superseded, kept with the reasoning that made them right at the time. `no-superseded-citation` fails any citation resolving to a name below this line.

**The fixture is diffed on the facts file, not on a rendered page** Superseded 8 September 2026. The reasoning was that prose varies between runs and layout changes constantly, so diffing rendered output would fail for reasons that do not matter and pass over reasons that do. That reasoning is intact and carries into the decision that replaced it. What fell was the scope: the facts file is written by the facts assembler at 4.3, and phases 1 through 3 produce expectations with no facts file in existence, so as written the rule was false for three phases and a session reading it would have taken a decision that does not apply yet for one that does. It fell to The fixture is diffed on each stage's serialised output, of which the facts file is one.

**Fundamentals hand-entered per quarter in a per-name file** Superseded 3 September 2026. The reasoning was that four updates a year per name, by an operator who reads every print anyway, beat a model extracting numbers from a press release on both cost and accuracy. It fell to the universe decision: at five hundred names that is two thousand edits a year. The provider carries reported quarters, balance sheets, ratings and the earnings calendar, and the filings archive carries segment tables and guidance, so the file is no longer needed. An optional overlay file for a name you follow closely remains available for anything a provider cannot supply.

**One self-contained HTML file per report, written nightly** Superseded 3 September 2026. The reasoning was that such a file opens anywhere and diffs cleanly. It fell to the single-page application decision, which removed nightly rendering entirely. The property it protected is preserved by the exporter.

**A 250-bar level window** Superseded 4 September 2026. It was written into v0.1 as a limit and then contradicted by v0.1's own reference report, which used fifty bars. The window is now sixty sessions, which is what the reference did, and the moving averages read the full stored year.

**Five years of stored history** Superseded 3 September 2026. The reasoning was that several years of nights would let condition thresholds be calibrated across different market moods. It fell to two facts: the report needs one year, and a few weeks of nights across five hundred names already gives tens of thousands of name-nights, with the run page recording the distribution as it happens.

**Nothing is pre-warmed** Superseded 5 September 2026. The reasoning was that researching names in anticipation would pay for nights you did not open them, which is the trade rejected everywhere else in the design. It fell to hardware: a machine with enough memory to run a large model locally makes anticipation free, so the objection was to spending money in advance rather than to doing work in advance. Paid research is still strictly on demand.

**A quarterly research budget allocated across names** Superseded 4 September 2026, the same day it was proposed. The reasoning was that research refreshes slowly, so a fixed number of passes a year could be bought and allocated by a rule. It was a schedule wearing different clothes, and it was rejected because it would pay for names nobody opens. Research now runs only on demand.

**A three-tier split, with researched sections only for watch-list names** Superseded 4 September 2026. The reasoning was that per-company research could not be automated at scale. That was wrong: the sources are reachable, and the constraint was cadence and spend rather than availability. Any name can be researched; the question is only whether you asked for it.

**A crossing is a candidate condition only where the close sits a margin of the typical move past the edge it crossed** The operator ruled on 2026-09-22 that the candidate requires the close to sit at least 0.25 of the name's typical daily move beyond the edge it crossed, measured from tonight's close: the high edge for a crossing upward and the low edge for a crossing downward, with the typical move the one the ladder reads. The margin 0.25 is proposed, and before anything is registered the counts at 0.25 and at 0.5 go back to the operator, because 0.25 lands at 38 to 87 a night on 2026-09-16 to 09-18 where the 2026-09-20 measurement of the tight setting gave 20 to 48, which 0.5 reproduces. The defect it answers: 19% of 1,132 recorded crossings finished within a tenth of a typical move of the edge, and one closed 9 cents past a level and listed. Not settled by research: no study found sets the margin. Osler (2003) finds stop-loss orders cluster just beyond round numbers and rates move rapidly once they cross them, which bears on what follows a crossing and not on the margin.

Superseded on 2026-09-22 by **A crossing is a candidate condition only where the close sits half a typical move past the edge it crossed**. The condition this entry states is unchanged, and so is the reason for having a margin at all. What changed is the number: the counts it sent back to the operator were re-derived at 10.3 and the operator settled the margin at 0.5, so the margin is no longer a proposal and no longer carries an open question about which of the two it should be.

**The numbers section shows the dividend the provider files, on the newest filing alone** The forward annual rate, the forward yield as the provider states it, the payout ratio, the ex-dividend date and the pay date, all from the provider's SplitsDividends part, as the analysts' ratings are read (see: The fundamentals row carries the analysts' ratings the provider files, on the newest filing alone). No yield is computed at tonight's close. A name the provider files no dividend for gets one line saying so. It is presentation in an existing table, which the fundamentals ruling leaves standing.

Superseded on 2026-09-24 by **The numbers section draws the dividend the provider files from the newest filing alone, and nothing for a company paying none**. Everything this entry ruled stands but one sentence: a name the provider files no dividend for drew one line saying so, and on the operator's ruling of 2026-09-24 it draws nothing, while a row holding no dividend part still says so.

**Section 4 of the architecture defines the report, and nothing outside the corpus does** The eleven sections, what produces each, and whether each costs money are stated there. Sections are added, removed or reordered by amending that table, and `architecture-conformance` checks the code against it. A hand-written example illustrates the rules and is never the thing to be reproduced: where an example and a rule disagree, the rule is right.

Superseded on 2026-09-24 by **Section 4 of the architecture defines the report region by region, and nothing outside the corpus does**. Everything this entry ruled stands but two statements: the report's eleven sections, which the page outgrew region by region while the table stood unchanged, and `architecture-conformance` checking the code against the table, which no check did, so nothing read the table against the page until the operator found it out of date.

**A reason's threshold is calibrated to a target share of the index over ordinary nights and a night a usually quiet reason floods is left out** The operator ruled on 2026-09-23 that section 11's thresholds are calibrated to a target rather than against a distribution no rule turns into a number. Each reason's median nightly share of the index is to be at or below 2%, about ten names, and the share firing any reason at or below 6%, about thirty, both proposed: a reason firing on a large share of the index has setups whose base rate is the universe's, so its measured edge comes out near zero by construction, and the list draws twenty, 4% of the index, so a target far above that leaves the tie-breaker choosing. A night on which a reason fires for more than a quarter of the index, while that reason's own median nightly share is below a quarter, is an event session: it is counted, marked on the run page and left out of the sample, because its setups share one cause and are closer to one observation than to hundreds. The ruling first named any reason above a quarter, and the eight nights from 2026-09-09 to 2026-09-18 marked every one of them by it, at entry zone firing on 39 to 47% of the index each night, so no night would have been ordinary and the calibration could never have started; the operator ruled the same evening that a reason above a quarter on its ordinary nights is flooding by its threshold, which is what the calibration moves, and marks no session. Once sixty ordinary nights of listings written under the current code are stored, each threshold is moved once, to the value that puts its median nightly share at its target, as a new decision citing the measurement and opening a new window (see: A reason's record reads only the rows written under the threshold the code carries, and the rows written under another are kept), and a reason no threshold brings to its target is reported as such rather than forced (owes: The six reason thresholds calibrated from the nights they fired on).

Superseded on 2026-09-25 by **The swing filter's shape is calibrated over its ordinary nights, and a night one cause floods is left out**. What this entry protected stands for the gates: a count that floods says little, a night one cause floods is counted and left out, and a median is read over the nights a rule was written under. What changed is what is calibrated, from each reason's share of the index against a target to each gate's count through the funnel against a band, because the reasons no longer choose the list, and the event rule's medians, now over every night in the window rather than over the ordinary ones.

**The swing filter's shape is calibrated over its ordinary nights, and a night one cause floods is left out** The operator ruled on 2026-09-24 that tonight's list becomes a swing filter, so the six reasons stop choosing it and no reason's threshold is calibrated to a share of the index; what is calibrated is how many names each of the filter's gates passes. The shape clock counts, over the nights stored under the open filter version, how many members pass each gate after the market and every gate before it, with the market gate held open, since the market opens or closes the whole list and is not calibrated by count, and how many of those no exclusion removes. A night is an event night where a gate counted that way, or a reason, whose own median share over every night in the window is below a quarter passes or fires for more than a quarter of the index, or where the index's median volume against its fifty-day average is 1.8 or more; the market gate is never read for it. The medians are over every night in the window, event or not, so a night's classification never depends on the classification it decides, and over the stored nights this marks 2026-09-18 alone, the quarterly expiry on which unusual volume fired on half the index and the index traded at twice its average. A gate counted alone was the first reading and marks ordinary nights, the setup gate passing 27 to 28% of the index on three of the twelve stored nights against a median of 24%, so the count through the funnel is the one read. An event night is counted, marked on the run page and read by no median. Each gate's median count over the ordinary nights is held against a band, proposed: through trend and strength 50 to 100 names, through the setup 20 to 60, through the trigger 8 to 40, through the trade 5 to 35, and the list 5 to 30, each step keeping between about a fifth and two thirds of what reaches it, so the list can land inside its band whichever gate binds. Before a version is open the filter runs on section 17's proposed values and no night counts toward the sixty ordinary nights the calibration waits on; from the first version, the nights under it do. The reasons' shares stay on the run page as context. This supersedes the entry that calibrated each reason to a target share, whose obligation is discharged with it (owes: The swing filter's shape calibrated from its ordinary nights).
Superseded on 2026-09-25 by **The swing filter's shape is calibrated over its ordinary nights, a night one cause pushes past a quarter and twice its usual share is left out, and each band spans a third to three times what the ruled filter passes**. What this entry protected stands: the count through the funnel with the market held open, the medians over every night in the window, an event night counted and read by no median, and sixty ordinary nights under one version. What changed is that a flood must also be more than twice its measure's own median, and that the bands, proposed at about a fifth to two thirds of what reaches each gate, are fitted to a third to three times what the ruled filter passes.

**Arrival is the first night a name's trigger fires, and a setup it has held for several nights does not stop it** A pullback's trigger is a close above the previous session's high, or a close back inside or above the band after a session below its low edge; a breakout's is the first close above the band it cleared. The operator's text first read arrival as the setup not having passed the night before, and that and the trigger almost never hold together, because a name falls into its zone on a down day and closes back above the prior high on a later one. The operator ruled on 2026-09-24 that the setup is a place and the trigger the event: arrival is the trigger not having fired the night before, read off that night's stored gate result, so a name may sit in its pullback for several nights and lists on the first night it closes back above the previous high. The pullback's own window, a high made within the last twenty sessions, already bounds how long it can have sat there. For a breakout the rule is the one it already had.
Superseded on 2026-09-25 by **Arrival is a trigger that first fired within the last three sessions, and the trade is read from tonight's close**. What stands: the setup is a place and the trigger the event, each read off the stored results, a session that cannot say fails the gate, and a breakout's trigger is the break. What changed is the window, from the session before alone to the three sessions ending tonight.
